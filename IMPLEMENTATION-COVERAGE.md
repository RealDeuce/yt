# Native implementation coverage

This is the implementation ledger required by `IMPLEMENTATION-HANDOFF.md`.
It measures the C17/OpenDoors candidate against the completed byte-pinned
3.6G analysis.  It does not reinterpret the analysis coverage ledgers as
native implementation evidence and does not combine unlike inventories into
an overall percentage.

## Status rules

| Status | Meaning |
|---|---|
| `missing` | No exact native C owner and exact native test are mapped yet. Existing nearby code does not change this status. |
| `candidate` | A named C owner exists, but comparison and/or exact native testing is incomplete. |
| `verified` | The declared bounded component has an exact native test or fixture and matches the final analysis for that scope. |
| `explicitly deferred` | The handoff or user explicitly deferred this scope; it is not silently counted as done. |

Any row promoted to `verified` must name the implementing C function and the
specific native test or fixture. A whole-program smoke test cannot verify an
unrelated child root, qualified transfer, failure prefix, or presentation
family.

Session proof is compositional. A monolithic interactive-session transcript
is not required: bounded deterministic native fixtures may prove connected
components, while presentation, persistence, input/runtime, RNG placement,
and physical adapters are proved independently and joined through their
declared seams.

## Final-analysis denominators

The generated companion ledgers pin these independent final inventories:

| Axis | Ledger | Denominator | Current native disposition |
|---|---|---:|---|
| Reachable address-owned roots | `implementation-coverage/roots.tsv` | 168 | 167 roots are `candidate`; the `LOCAL.EXE` entry is `explicitly deferred` by user direction; none are `missing`. |
| Qualified transfers | `implementation-coverage/transfers.tsv` | 7,368 | 7,303 transfers are `candidate`; 64 `LOCAL.EXE` transfers plus the post-OPEN wait are `explicitly deferred`; none are `missing`. Current mapped slices include the five YT-INIT confirmation-decision/decline-terminal transfers, the 38 YTCONFIG startup/menu/LOF-terminal transfers in `0030..0539`, all eight Genesis-editor transfers, all 14 Headquarters-relocation transfers, all 85 scalar-options/exit transfers, all 71 planet-editor transfers, all 37 port-editor transfers and all 74 alias-editor transfers, the 59-site sound-call inventory (with one qualified-dead edge), movement danger scanning, team radio audit, shared press-any-key handling, both computer profit reports, all seven incoming Info-panel calls plus the complete 33-edge team/captain resolver and complete 49-edge outer Info-panel body, the complete 88-edge active-spy scan/report/pause/movement body including its private finding-header helper, all 12 shared low-time-warning helper edges, ten shared centered-line helper edges and the complete four-edge attention helper, the planet-production updater with its eleven callers, the complete 143-edge nearest-port body plus its incoming call, the 48-edge shared date-helper body (including two invariantly dead edges) plus mapped startup, port-updater and maintenance-entry calls, the 29-edge ordinary-port updater/report pair with all six incoming calls, the complete ten-edge shared port-owner/treasury row, all 102 qualified fixed-price commodity-trade edges, the 106-edge ship-computer activation/help/prompt/early-dispatch/filter front, all 41 command-7 avoid-list edges, the 29-edge command-2 port-report wrapper, the bounded command-S selected call/return plus mode-one heading/sound/trailer calls, all 105 shared current-sector/adjacent-scanner body and return transfers, the 62-edge cruise-missile full-player scan/damage/persistence continuation, the 24-edge linked-planet front through ground dispatch, the 32-edge linked-planet damage/destruction arithmetic-and-control slice and its six ordered direct/news calls, the eleven-edge cruise route-status/failure prefix at `6D04..6D7F` and eight-edge route/footer continuation at `6E0D..6E12` and `809F..80B9`, the complete 68-edge Planet Transfer body including its qualified-dead alternate sound arm, the complete 21-edge Planet Bank body, the complete 35-edge Planet Productivity body, the 63-edge linked-planet landing caller/permission helper plus its inventory/menu join, its 38-edge ground-assault child, its 33-edge zero-link creation arm, the complete 109-edge Planet Thrusters controller/one-hop composition, the complete 90-edge anonymous sector-mine body plus both five-edge caller continuations, the complete 15-edge direct fighter-kill wrapper, the complete 44-edge ordinary-movement parent, the complete 74-edge direct player-Attack parent, the complete 112-edge deployed-fighter Attack body and 47-edge Bribe body, the complete 53-edge emergency-warp child plus four black-hole caller/join edges, the complete 23-edge Xannor retaliation wrapper, the complete 15-edge startup lockout root, the exact main loader, main-error, serial-call, two identity-normalization and `0409` startup-prefix transfers, the 35 logical configuration-validation/player-cache/disruption transfers beneath the startup loader, and its exact `45F7` handler registration, and the complete 25-edge player counterlaunch wrapper, all 53 shared Clearance-sale edges, all 63 Earth report/front-end edges in `YT:[6CB3,7401)`, the complete 257-edge Earth-purchase composition (151 caller-body, 83 Lottery-helper, and 23 Anti-Cloak-helper edges), the Xannor sector-arrival caller plus all 63 body/helper/return transfers, both Xannor player-arrival callers plus all 70 body/helper/return transfers, the Xannor planet-arrival caller plus all 57 body/helper/return transfers, seven Xannor route-next/exhaustion transfers, seven route-completion/protected-sector-retarget transfers, four retained-hunt-player gates, five group-20 terminal-increment gates, three group-loop/persistence transitions, and seven final group-persistence gates/back-edges, the five exact logical KEY registrations plus the 60 F4/F9/F8/F10 body/call/return transfers and terminal F5 END beneath the separately deferred physical local KEY-delivery seam, all 16 main-error-dispatch body/call/jump transfers, all 49 shared-error-dispatch body/call/resume/terminal transfers, the maintenance entry/player-aging/port/planet/Wanderer/Xannor-home/top-player-hunt/ordinary-target/group-extraction/regeneration/headquarters-reclaim/headquarters-relocation/revenge-slot prefix through `30EA`, 56 logical DORINFO/serial-startup branches around the separately deferred post-OPEN wait, 39 decoded RMT-INIT handoff/UART/status transfers, and 75 RMT reset/progress/completion transfers including every shared-helper presentation call, both bounded-dot predicates and the exact completion-delay back edge. |
| Presentation families | `implementation-coverage/presentation.tsv` | 150 families owning 3,815 sites | 143 families are `candidate`, six site-free catalogs are `verified`, the `LOCAL.EXE` family is `explicitly deferred`, and none are `missing`. |
| Literal references | final `player-output-coverage.json` pinned by `manifest.json` | 2,060 | Credited through their owning presentation component, never independently by string-search resemblance. |

`implementation-coverage/manifest.json` pins the SHA-256 of every imported
analysis artifact and refuses regeneration if the completed denominators
change. Regenerate with:

```sh
python3 tools/update_implementation_coverage.py
```

All 389 qualified `RMT-INIT.EXE` transfers now have candidate native owners.
This includes the complete entry/serial/status controller, three distinct
date-observation boundaries, deterministic world/file loops, every reset and
progress call, the helper bodies and returns, and completion/delay/terminal
tail. Candidate status does not waive the still-listed raw-state, alternate,
physical-failure, or Win32 prerequisites.

## Executable ownership

These are entry-level candidates only. Their listed tests prove the narrower
baseline stated in the handoff, not whole-program compatibility.

| Legacy executable | Native owner | Current evidence | Status | Principal prerequisites |
|---|---|---|---|---|
| `YT.EXE` | `src/main_yt.c::main`, `src/yt_door.c`, `src/yt_session.c` | bounded numeric, data, sound, presentation, asset, score, install, and utility fixtures; session behavior is proved compositionally | `candidate` | complete every rooted component and edge plus their presentation, persistence, input/runtime, RNG, failure, and physical-adapter seams; no monolithic session fixture |
| `YTMAINT.EXE` | `src/yt_maint.c::yt_maintenance_run` | root `YTMAINT:0073`, all child roots, and all 938 qualified transfers are candidate. `test_clean_install` physically joins startup, every maintenance phase, final marker/scoreboard, close, completion and durable results; exact bounded fixtures pin each component, output family and early failure cuts | `candidate` | one deterministic shipped-scale whole transcript/effect digest, authoritative raw BRUN/FIELD/file/FOR/frame/CLOSE-all/END state and every non-atomic failure prefix |
| `YTCONFIG.EXE` | `src/main_ytconfig.c::main`, `src/yt_config_output.c` | all startup/common-menu, Genesis, Headquarters, scalar-options, planet-editor, port-editor and alias-editor families and all 416 executable-qualified transfers are candidate. Focused fixtures pin the five working normalizations, binary path, exact menu/prompt/dispatch/terminal rows, Genesis prompt/BEEP/range state, Headquarters numeric/prompt/diagnostic bytes, every scalar prompt/rejection/exit branch and boundary, and all three nested editor output/state families. Process fixtures pin J/X redraws and silent mutation, missing-file create/diagnostic/delete, Genesis blank/invalid/valid state, the fractional Headquarters four-record transaction, complete disk-backed scalar, sparse-planet and 299-record port sequences, and the hidden-sentinel/list/fractional-cancel/YTNAME-rewrite/all-match player propagation transaction with exact raw overlays | `candidate` | add raw BRUN loader/FIELD/GET/PUT/INPUT#/EOF/DATE$/INKEY$/PRINT/frame/CLOSE/END state, malformed alias-table domains, local framebuffer, child joins and every staged failure prefix |
| `YT-INIT.EXE` | `src/main_yt_init.c::main`, `src/yt_init.c`, `src/yt_platform.c::yt_platform_spawn` | confirmation/decline and complete accepted install in `test_clean_install`; `test_utilities` pins the pre-input preparation split, exact one-byte `Y`/`y` admission and all five qualified `06B5..06CF` decision/decline transfers, distinct epoch and maintenance clocks plus one headquarters draw before scoreboard input, the fresh port-finalizer clock and 17 total successful clock observations, 31,297-draw world, 8,379-event current presenter tape/FNV, the raw `0B09` MBF32 sector-offset row, 2,003 verification rows, 1,000 port rows, logical PLAY and both sides of the first destructive presentation/PUT boundary. The qualified `23DA` RUN edge is candidate: POSIX uses `execv`, while Win32 now waits for YTMAINT and terminates with its status instead of exiting immediately after launch; the composed fixture requires maintenance completion and durable results before return. A strict MinGW C17 object compile also pins the corrected Win32 private serial-state declaration and replacement branch. | `candidate` | canonical operator editor/echo/full framebuffer, raw runtime/FIELD/BRUN RUN state, remaining output/file/loader failure prefixes and direct Win32 execution |
| `RMT-INIT.EXE` | `src/main_rmt_init.c::main`, `src/yt_init.c`, `src/yt_rmt_door.c` | all 389 qualified transfers have candidate owners; fixtures cover standalone/remote entry, serial/status, normalization/old-prepass, three date observations, deterministic world/file control, exact reset/progress tapes, bounded delay, destructive cuts and physical POSIX completion | `candidate` | raw BRUN node/register/INPUT/PRINT/FIELD/cache/framebuffer state, a joined deterministic physical whole tape, Win32 physical output and remaining destructive failure prefixes |
| `PORTNAME.EXE` | `src/main_portname.c::main`, `src/yt_portname.c`, `src/yt_file.c::{yt_database_open,yt_database_read,yt_database_random_get,yt_database_random_close}` | exact application transcript/parser/control, raw transaction, injected-RNG and process fixtures in `tests/test_utilities.c`; a missing final physical port record now follows BRUN's successful zero-filled GET, receives the name/length overlay, is PUT, and reaches the normal close/PLAY/completion tail. The shared update-or-create adapter executes the mode-4 BRUN OPEN retry/create/temporary-close/reopen/device machine. Its typed random CLOSE adapter now covers missing file 1, ordinary/device success, every first DOS error byte, the mandatory one-shot ignored-result retry, registry removal before ERR 70/57, and retained unregistered-handle residue. PORTNAME joins checked CLOSE at startup, the accepted close/redundant helper close pair, and the completion-before-close-before-logical-PLAY tail; a final-close fault retains the complete success-row prefix and suppresses PLAY. | `candidate` | physical BRUN framebuffer/editor/Ctrl-C/default-fatal and remaining console/END adapters; public CLOSE_NO_ARGS registry/fixed-console behavior, all generator/provider fault cuts, and raw FIELD/status/error-router state |
| `LOCAL.EXE` | `src/main_local.c::main` (limited owner only) | limited `test_utilities::test_local` smoke fixture | `explicitly deferred` | user-authorized deferral: exact console/menu/DORINFO/SHELL/local-framebuffer compatibility is not required; retain for optional future OpenDoors local-personality work |

## Shared prerequisites

| BRUN regular-random `LOF` | `src/yt_file.c::{yt_database_random_lof,yt_random_file_lof,yt_database_set_lof_provider,yt_radio_file_size,yt_radio_file_next_record}`, `src/main_portname.c::main`, `src/main_ytconfig.c::main`, `src/main_rmt_init.c::{read_handoff,main}`, `src/yt_init.c::write_rmt_auxiliary`, `src/yt_session.c::{lockout,registration_file_size}`, `src/yt_maint.c::{maintenance_radio_append_bytes,yt_radio_compact,invalidate_radio}` | a typed live-handle transaction performs the exact current/end/restore seek order, retains the current/end prefix at each failure cut, maps all 255 DOS error bytes at all three ordinals to ERR 52, validates provider observations, restores the native cursor on success, and takes the no-seek device position-window arm. PORTNAME and YTCONFIG file 1, both RMT-INIT file-1 classifiers at `0066` and `27C2`, all five RMT Genesis-message sites at `219D..224F`, gameplay lockout file 5 at `9B6A`, all three supported YTMAINT radio sites at `67D3`, `6EF3`, and `7D7C`, the shared gameplay radio file 5 owner, and registration file 4 now use this one transaction instead of pathname metadata or separate `ftell` approximations. The RMT handoff retains its empty random handle through the standalone decision, while a nonempty handoff closes and reopens sequentially for only the first line; old `YTDATA.DAT` remains on the measured handle through config hydration and preprocessing. Each Genesis record and maintenance append recompute aligned `LOF / 86 + 1` on one live random handle before PUT, retaining their respective FIELD widths. Maintenance compaction and expired-player invalidation each take one live `LOF / 86` bound before their numbered GET loops; invalidation also retains the shipped preliminary record-1 GET. Lockout retains its measured update-or-create handle through the zero/nonzero decision, then performs checked close before any sequential reopen. Exhaustive component fixtures are in `tests/test_data.c::test_database_random_lof`; radio fixtures additionally pin cursor restoration and record selection through the production wrapper, and existing utility/session fixtures join the zero/nonzero callers. | `candidate` | only the separately deferred LOCAL file-1 caller remains unconnected; add raw MBF64 dirty-zero mantissa, handler/file-block scratch, unrestricted external cursor carrier and caller-specific error-router prefixes |

The plasma resolver's connected native frontier now extends through the
`YT-SUB:B713..BC7D` planet-impact continuation.
`src/yt_game.c::yt_projectile_plasma_fighter_run` owns the
deployed-fighter owner, encounter, DOUBLE damage, result/news,
fresh-persistence, Headquarters, and footer dispatch transaction.
`src/yt_game.c::yt_projectile_plasma_mine_run` then owns selector five, both
binary-attacker news rows, the SINGLE/DOUBLE damage loop, bold result, fresh
mines-only persistence, and footer dispatch. Their provider fixtures pin every
owner route, loop back-edge, output and callback failure prefix.
`src/yt_game.c::yt_projectile_plasma_dispatch_run` owns the initial and
post-player scan entries, every cached-player comparison, terminal-plus-one
write, pre-increment footer gate, and the exact planet/next-hop/footer
partition. Its fixture pins initial selection, retained-counter resumption,
fractional terminal and planet-link behavior, and corrupt cache bounds.
`src/yt_game.c::yt_projectile_plasma_player_run` owns `B2B1..B500` and the
survivor branch `B68E..B6C8`: three distinct fresh player reads, foreground
five and restoration, selector two, continuous fighter-then-shield RNG,
strict clamps, retained binary-attacker/fresh-victim output, both
news-before-direct pairs, and shields/fighters-only raw persistence. Its
fixture pins both loop back-edges, clamp arms, survivor and killed routes,
negative energy retention, unrelated-record preservation and every injected
failure prefix.
`src/yt_game.c::yt_projectile_plasma_killed_run` now owns `B500..B6D9`:
fresh victim read, self/non-self row selection, mines/scanner-only victim
persistence, both blink rows, nonzero carried-mine deployment, self dead/cache
state, the death-selector-three-salvage child order, and the positive-mine
reload versus footer/retained-counter partition. Its fixture pins binary
victim rows, self bypass, positive/zero/negative mine distinctions, sub-one
reload, raw player/sector preservation, corrupt cache bounds, and every
injected callback failure prefix.
`src/yt_game.c::yt_projectile_plasma_planet_run` now owns `B713..BC7D`:
updater-return `P(1)` retention across the independent planet GET, retained
binary launch attribution, hit/news/selector-two order, one shared SINGLE
quantum and one draw per damage iteration, strict stock caps, productivity
reporting before a fresh six-field/ground/owner overlay, canonical-zero
planet destruction and sector unlink writes, destruction output/sound/news
order, surviving-ground reporting, and the sub-one footer versus next-hop
partition. Its fixture pins multi-draw survival, all-zero destruction,
updater-cache-only loop admission and rejection, embedded-NUL identities,
fresh patterned-record overlays, unrelated-byte preservation and every one
of the 17 callback failure prefixes. The compiled REDIM/far-array/string and
dependency-internal physical runtime boundaries remain separately candidate.
`src/yt_game.c::yt_projectile_plasma_footer_run` owns the shared
`BC80..BCB6` suffix as three independently fallible direct rows: empty,
`Plasma bolts dissipated.`, empty. Its provider fixture pins all payloads,
row roles, short-circuit order and each output failure prefix, while the
route fixture joins both route exhaustion and impact-driven footer entry.
Raw compiled string-copy and direct-output dependency prefixes remain
separate runtime obligations.

`src/yt_game.c::yt_startup_configuration_run` now owns the logical
`YT-SUB:BE6E..C0DB` startup configuration transaction behind the existing
OPEN/FIELD/GET boundary. It compatibility-uppercases the counted binary
scoreboard path, performs the zero-Headquarters PUT before changing the
process value, applies all seven process-only validations in compiler order,
executes the one-shot player cache with its initial FOR test and immediate
one-cell cloak repairs, and consumes the two unconditional disruption draws
with the recovered SINGLE rounding sequence. The provider fixture pins both
Headquarters lanes, empty/embedded-NUL/overlong paths, local-mode CINT
failure, guard and zero-iteration routes, negative/valid/above-one cloaks,
fractional loop terminal, unrelated raw record bytes, and every logical
callback failure prefix. `src/yt_session.c::yt_session_run` also now computes
the six commodity bases with a distinct launch RNG before initializing the
post-`RANDOMIZE` gameplay source, rather than consuming those six draws in
the configuration loader. The logical `BCC5` installation of handler `45F7`
before the first provider operation is now pinned across every injected
callback failure. The raw BRUN handler slot/frame/ERL/retry residue,
OPEN/FIELD/GET/PUT/string state, physical partial-I/O, and corrupt
out-of-native-cache-domain boundaries remain separate prerequisites.

| Component | Native owner | Native evidence | Status | Required before promotion |
|---|---|---|---|---|
| YTCONFIG player name/alias editor | `src/main_ytconfig.c::{main,edit_aliases,fixed_contains,write_output}`, `src/yt_config_output.c::{yt_config_compose_alias_entry,yt_config_compose_alias_menu,yt_config_compose_alias_key_echo,yt_config_compose_alias_list_header,yt_config_compose_alias_list_row,yt_config_compose_alias_pause,yt_config_compose_alias_blank,yt_config_compose_alias_number_prompt,yt_config_compose_alias_invalid,yt_config_compose_alias_edit,yt_config_compose_alias_confirmation,yt_config_compose_alias_response_echo,yt_config_compose_alias_cancel,yt_config_compose_alias_saved,yt_config_alias_selection_in_range,yt_config_alias_pause_after}`, `src/yt_names.c::{yt_names_load,yt_names_write,yt_names_split}`, `src/yt_game.c::{yt_game_read_player,yt_game_write_player}`, `src/qb.c::{qb_cint,qb_title_case}` | N hides physical YTNAME row zero, reports the logical count, folds the one-key submenu, lists rows with the 79-equals header and shipped pause predicate, preserves raw-zero and fractional selector behavior, clips before comma replacement/title normalization, and loops invalid confirmation or N cancellation back through the current edit. The composer follows the actual immutable blank descriptor at `1264` and after `Canceled!` at `1399`; the generated upstream detail labels calling those values aliases are stale. Y confirmation rewrites every YTNAME row before scanning all 50 player records, then case-sensitively replaces every fixed-field substring match regardless of active/name-length state. Focused fixtures pin exact output/predicates. A disk-backed fixture proves the hidden sentinel, zero-player BEEP, list/pause, invalid and fractional selection, cancellation, blank retry, rewrite bytes with DOS EOF, three-of-50 propagation overlays, nonmatches, unrelated bytes and complete restoration. All 176 presentation sites and 74 qualified transfers are candidate. | `candidate` | add exact local LOCATE/framebuffer reduction, raw BRUN INPUT#/EOF/static-array-overflow/INKEY$/INPUT/CINT/FIELD/GET/PUT/sequential-output/frame state, malformed YTNAME domains, all non-atomic failure prefixes and physical endpoint behavior |
| YTCONFIG port-name editor | `src/main_ytconfig.c::{main,edit_ports,write_output}`, `src/yt_config_output.c::{yt_config_compose_port_search_prompt,yt_config_compose_port_search_echo,yt_config_compose_port_match_prompt,yt_config_compose_port_response_echo,yt_config_compose_port_not_found,yt_config_compose_port_end_list,yt_config_compose_port_wait_prompt,yt_config_compose_port_replacement_prompt,yt_config_compose_port_confirmation,yt_config_compose_port_cancel,yt_config_compose_port_saved}`, `src/yt_game.c::{yt_game_read_port,yt_game_write_port}`, `src/qb.c::{qb_cint,qb_ascii_upper,qb_title_case}` | O now emits the exact instruction/blank/search prompt and raw input echo, scans only logical records 2..300 in ascending order, performs case-folded substring matching, and owns the exact match prompt, folded one-key echoes, post-response blanks, no-match/end-list rows, replacement heading, quoted confirmation retry, cancel/save rows and wait prompt. Replacement title-normalization precedes the 41-byte cap. Focused fixtures pin all output bytes and key folds. A disk-backed 299-record fixture proves blank search, missing search, multi-match end-list, cancel, empty replacement, invalid confirmation retry and one successful raw name/length overlay while all other records remain byte-identical and are restored. All 73 presentation sites and 37 qualified transfers are candidate. | `candidate` | add binary fixed-name/INPUT descriptors, raw INKEY$/INPUT/CINT/FIELD/GET/PUT/frame state, corrupt name-length/record domains, editor EOF distinctions, every staged failure prefix and physical local framebuffer |
| YTCONFIG sparse planet-name editor | `src/main_ytconfig.c::{main,edit_planets,write_output}`, `src/yt_config_output.c::{yt_config_compose_planet_entry,yt_config_compose_planet_menu,yt_config_compose_planet_key_echo,yt_config_compose_planet_number_prompt,yt_config_compose_planet_list_header,yt_config_compose_planet_list_row,yt_config_compose_planet_pause,yt_config_compose_planet_blank,yt_config_compose_planet_invalid,yt_config_compose_planet_protected,yt_config_compose_planet_edit,yt_config_compose_planet_confirmation,yt_config_compose_planet_response_echo,yt_config_compose_planet_cancel,yt_config_compose_planet_saved,yt_config_planet_selection_in_range,yt_config_planet_selection_protected,yt_config_planet_pause_after}`, `src/yt_game.c::{yt_game_read_planet,yt_game_write_planet}`, `src/qb.c::{qb_cint,qb_title_case}` | P now preloads only sparse logical slots 1..75, retains the active-count/index distinction, and composes the exact loading/count, empty-game BEEP, one-key submenu, folded echoes, 79-dash list, peculiar pause predicate, invalid-range/empty-slot BEEPs, quote-bearing protected-name row, repeated old-name edit display, replacement normalization, confirmation retries/cancel loop, save row and silent wait. Focused fixtures pin exact bytes and predicates. A disk-backed 75-record fixture proves empty-game return, four-slot list/pause, raw 1/75 protection, the 1.4 bypass into physical slot 1, invalid confirmation, cancel/re-edit/save, the sole name/length overlay, all other record nonmutation and restoration. All 155 presentation sites and 71 qualified transfers are candidate. | `candidate` | add binary fixed-name/INPUT descriptors, raw INKEY$/INPUT/CINT/FIELD/GET/PUT/frame state, corrupt length/record domains, multiple pause layouts, every staged failure prefix and physical local framebuffer |
| YTCONFIG scalar options and exit | `src/main_ytconfig.c::{main,numeric_edit,edit_maintenance,edit_scoreboard,write_output,store_config}`, `src/yt_config_output.c::{yt_config_compose_scoreboard_prompt,yt_config_compose_scoreboard_too_long,yt_config_compose_scalar_prompt,yt_config_compose_scalar_rejection,yt_config_scalar_blank_unchanged,yt_config_scalar_valid,yt_config_compose_exit}`, `src/yt_config.c::yt_current_date_serial` | The sequential I/J/K/A/X/B/C/D/E/F/G family now uses the exact CR-only scoreboard, numeric, maintenance and terminal composers. Pure predicates preserve blank-versus-VAL distinctions, inclusive SINGLE bounds, misleading printed ranges, the immutable blank row before B/C/E rejection diagnostics, and deliberately silent rejection paths. A disk-backed process fixture traverses path length/defaulting, silent J, blank/invalid/valid numeric branches, accepted fractional persistence, maintenance blank/reprompt/Y residue, every redraw, and X; its expected 137-byte record changes only the proven fields and the test restores the original world. All 75 presentation sites and 85 qualified transfers are candidate. | `candidate` | add binary scoreboard INPUT beyond NUL-free host-line limits, raw movable-string/VAL/MBF/GET/FIELD/LSET/PUT/DATE$/frame state, CINT overflow for J, every physical failure prefix, CLOSE-all/END detail and the final local framebuffer |
| YTCONFIG startup, common menu and dispatch prefix | `src/main_ytconfig.c::{main,redraw_repairs,write_output}`, `src/yt_config_output.c::{yt_config_prepare_menu_working,yt_config_compose_menu_prompt,yt_config_compose_command_echo,yt_config_compose_missing_data,yt_config_compose_exit}`, `src/yt_config.c::yt_current_date_serial`, `src/qb_format.c::{qb_str_single,qb_print_single}` | startup retains the five ordered working-only repairs without a PUT, while redraw separately clamps initial holds, reloads record 1, repairs zero Headquarters, and observes the current date. The composer uses the corrected actual PRINT value ordering from upstream `0fe41956`: immutable blanks and version, constructed A/I/L strings, raw numeric PRINT spacing, mutually exclusive G/J rows, N/O/P rows, X/blank/prompt, first-byte `AND DFh` echo and post-echo blank. Native fixtures pin the canonical 607-byte menu/prompt, alternate/binary rows and 3-byte echo; a physical J/X cycle compares both full redraws and durable silent toggle, while the empty-file fixture pins its exact single-BEL/CR diagnostic and deletion. The presentation family and all 38 source-owned `0030..0539` transfers are candidate. | `candidate` | add authoritative raw loader/FIELD/GET/PUT/string/DATE$/INKEY$/PRINT cursor/frame/CLOSE-all/END state, physical partial-output and operation failures, redraw write cuts, empty-poll scheduling, first-P joined child, noncanonical raw runtime domains and an exact local framebuffer reduction |
| YTCONFIG Genesis editor | `src/main_ytconfig.c::{main,edit_genesis,write_output,store_config}`, `src/yt_config_output.c::{yt_config_compose_genesis_prompt,yt_config_compose_local_beep,yt_config_genesis_valid}` | the L branch emits the two exact help rows, a binary caller-supplied descriptor row, and the 67-column semicolon prompt before the input lane. Blank cancels before VAL; a rounded threshold below 50 emits one separate local BEEP, while equality and all greater values write only configuration offset 105 and return to the common redraw. Focused fixtures pin the canonical 176-byte dynamic prompt, final column, strict boundary and effect count. Physical invalid/valid cycles pin exact prompt bytes, one BEL adapter event versus none, unchanged 137-byte invalid state, the sole four-byte MBF32 valid overlay, redraw value and restored test world. All 16 presentation sites and eight qualified transfers are candidate. | `candidate` | add raw INPUT editor/VAL/MBF temporaries, GET/FIELD/LSET/PUT and menu-return state, blank/EOF and conversion/runtime failures, physical local BEEP/framebuffer behavior, partial output/write cuts and corrupt numeric domains |
| YTCONFIG Headquarters relocation | `src/main_ytconfig.c::{main,edit_headquarters,write_output,store_config}`, `src/yt_config_output.c::{yt_config_compose_hq_prompt,yt_config_compose_hq_diagnostic,yt_config_hq_in_range}`, `src/yt_game.c::{yt_game_read_sector,yt_game_write_sector}` | H now retains the SINGLE upper-bound subtraction instead of truncating it for display/comparison, emits the raw numeric Headquarters row and two-space positive upper bound through QuickBASIC PRINT formatting, keeps keyboard input separate, and owns exact range/occupied diagnostics. The accepted controller CINT-selects the physical candidate but carries the raw accepted value through the sector-1/config overlays. A disk-backed `8.6 -> sector 9` fixture pins Xannor-owned fighter admission/merge, the old sector's three ordered dirty-zero clears, candidate owner/fighters/planet link, raw sector-1/config values, all unrelated bytes, redraw row, and complete restoration. All 17 presentation sites and 14 qualified transfers are candidate. | `candidate` | add blank/invalid/occupied process nonmutation, ports/fractional occupancy and corrupt CINT domains, authoritative raw INPUT/VAL/MBF/FIELD/GET/PUT frame state, all four partial-write/failure prefixes, physical framebuffer and endpoint failures |
| Xannor Headquarters victory and three callers | `src/yt_session.c::{attack_deployed_committed,missile_sector,plasma_sector,xannor_victory,xannor_victory_file,session_present_text,session_wait,session_sound,append_news_bytes,radio_append_bytes,apply_player_credit_mutation,mutate_player_credits_observed}`, `src/yt_text.c::{yt_text_input_open,yt_text_input_read_line,yt_text_input_close,yt_text_sequential_play_run}`, `src/yt_game.c::{yt_xannor_victory_run,yt_credit_mutation_run,yt_player_stored_name,yt_xannor_victory_winner}` | direct and missile callers retain their saved-owner-`-1` predicates while plasma intentionally has no owner gate. The body owns foreground seven, initial file-4 close, case-resolved sequential open, dynamically sized exact binary `LINE INPUT` records, immediate per-record presentation, EOF observation and final close, followed by raw pause, consumed 99-second wait, direct blank, blink-one bold bonus, synthetic-queue clear, fresh credit award through the shared raw GET/SINGLE-add/`INT_S`/PUT owner, three selector-two sounds, binary-safe news/radio triplets, and the fresh logical-sector-21 metadata PUT. The injected file transaction pins the complete shipped five-record 14-event tape and every callback failure prefix; the native stream fixture pins case resolution, empty records, embedded-NUL discard, CR with optional-LF consumption, retained bare LF, DOS EOF, unterminated records and a 600-byte row without a host line cap. A physical composition now opens the generated lowercase asset through `XANNORHQ.TXT`, joins the real stream and presentation reducers, and pins the exact 230-byte plain and 240-byte ANSI streams, five local line events and ANSI color state. The injected body transaction pins the complete 21-event order, all 18 fallible dependency prefixes, binary stored-name/winner bytes, the `16000001 + 16000000 -> 32000000` SINGLE witness, unrelated player/sector preservation, raw current-player metadata, and the post-award/post-sound name-conversion failure. Existing fixtures separately pin wait, sound, news, radio and record adapters; all focused upstream victory/world/caller/missile tests pass. The presentation family and 22 relevant caller/body/return edges are candidate. | `candidate` | add the full joined victory framebuffer/effect tape, raw runtime/file-block/cache/queue residue, operating-system read/close faults and physical append/PUT/endpoint failure prefixes |
| Shared no-cancel port-name editor | `src/yt_session.c::{command_buy_port,command_rename_port,port_rename,port_name_row,port_name_prompt,port_name_edit,port_name_blank,port_name_confirm,port_name_write,session_0317,session_031f,session_0345,session_a8d2,session_present_text}`, `src/yt_game.c::{yt_port_name_editor_run,yt_port_name_display_row,yt_port_name_prepare_candidate,yt_port_name_confirmation_prompt,yt_port_name_overlay}`, `src/qb.c::qb_title_case_n` | the shared helper emits the exact current-name/keep/instruction rows and raw prompt, uses the case-preserving editor, canonicalizes the full response before the 41-byte cap, falls back to the independently cached binary name, suppresses confirmation only when both are empty, and accepts only explicit compatibility-uppercase Y. Blank/N restart the whole attempt; invalid input retries the raw confirmation through A8D2. Acceptance overlays only fixed name and MBF32 length in the active FIELD and performs one raw PUT; the later purchase owner/treasury write preserves those raw name bytes. The provider-driven transaction independently supplies cached name and active FIELD, and its native tape pins binary fallback/decline/re-entry, both-empty suppression, the 42-byte cached fallback versus 41-byte FIELD asymmetry, every dependency cut, overlay-before-failed-PUT residue and the sole durable write. Native helper fixtures also pin trim/case/cap boundaries, embedded-NUL FIELD bytes and unrelated-byte preservation; all 13 focused upstream component tests pass. The presentation family, all 19 body transfers and both incoming calls are candidate. | `candidate` | join the bounded transaction to authoritative raw pager/editor/queue/endpoint/frame state, including invalid-confirmation retry composition; add corrupt stored length/record/runtime domains, exact local framebuffer, physical partial PUT and remaining adapter failures; command-B and command-N remain separate candidate families |
| Ordinary main-command shell front end | `src/yt_session.c::{command_shell,show_help,instruction_offer,session_quit_confirm,display_sector,registration,show_ship,reload_player,session_02fc,session_0317,session_02db,session_031f,session_0357,session_present_text}`, `src/yt_game.c::yt_main_shell_dispatch`, `src/yt_input_model.c::yt_input_yes_no_candidate` | entry now resets pager count, hydrates once, emits the direct blank before clearing scanner scratch, composes the cached-time `031F` prompt, and uses the uppercase `0357` editor. Dispatch preserves the shipped whole-string `X`/`S`/empty exceptions, then first-byte `W)+ABCFLMPQTD$GN` and `V/I/Z/?` aliases, including invalid `!`, `Xjunk`, and `Sjunk`. Empty uses `0317(<Display>)` and the shared mode-zero `1627..1656` current-sector scanner before the zero-byte back-edge into a fresh prompt; invalid uses queue-clearing blink/bold `02DB`; `Z` uses `02FC(<Instructions>)` and the shared question; Info no longer performs a redundant hydration. Help owns foreground six, the exact eleven 40-column direct rows, one `0317` plus six `02FC` narrative rows, and the fresh prompt. Native fixtures pin the complete 1,212-byte plain and 1,242-byte ANSI help cycles plus invalid, empty, instruction-default and quit-cancel branches. The accepted-Q fixture carries the exact 92-byte ANSI or 72-byte plain main prompt/Q/helper prefix, four local rows, color tape, line-count-zero and retained `14:59` state to the oracle's named `YT:0206` normal-exit handoff. Accepted instructions now consume the physical 24,736-byte `YTINSTR.DOC` through the production viewer at saved foreground two: all 542 rows, 23 prompts, 566 B05D samples, 569 local rows, exact 26,713-byte ANSI and 25,977-byte plain streams, cleanup and restored caller colors are pinned. All 19 focused upstream shell cases pass. The site-free presentation family and all 62 address-owned front-end edges plus the four shared scanner-wrapper call/return/back-edge transfers are candidate. | `candidate` | add a complete injected scanner-to-A41C/FIELD/cache/editor/queue/low-time transaction, preceding accepted-question-to-viewer composition, Info/version child joins, local/corrupt modes, carrier cuts, asynchronous pager interleaving, physical framebuffer and every dependency/failure prefix |
| Main command-N port-rename cycle | `src/yt_session.c::{command_shell,command_rename_port,port_rename,reload_player,display_current_sector_cached,display_sector_one,session_02db}`, `src/yt_game.c::{yt_port_rename_record,yt_port_name_prepare_candidate,yt_port_name_overlay}`, `src/qb.c::{qb_mbf32_truth,qb_brun_random_record_number,qb_cint_mode}` | command N now independently hydrates the player, reads sector then port, uses raw exponent truth for the no-port gate, computes the absolute and relative port cells with the compiled SINGLE intermediates, selects the physical GET record through the distinct BRUN 24-bit floor conversion, tests owner before Earth, and applies live CINT mode only to the stored name length. It emits the exact three `02DB` refusals, invokes the shared no-cancel editor, and sends every ordinary result through one cached current-sector scanner followed by the command loop's single fresh hydration and prompt. Native reducer fixtures pin positive fractional BRUN truncation and the nextafter-one Earth rounding boundary; all 37 focused isolated/joined upstream tests pass. The presentation family and all 15 wrapper edges are candidate. | `candidate` | add one full active-prompt/body/editor/scanner/reentry raw FIELD/cache/output transaction, retain the uncoerced player-record cell, cover corrupt current-sector/link/length/record domains, exact framebuffer and every dependency/physical failure prefix |
| Main command-B port-purchase cycle | `src/yt_session.c::{command_shell,command_buy_port,earth_report,port_update,port_report_capture,port_owner_row_capture,port_rename,reload_player,display_current_sector_cached,display_sector_one,session_0317,session_02db,session_02fc,session_a8d2,radio_append_bytes}`, `src/yt_game.c::{yt_port_rename_record,yt_port_purchase_price,yt_port_purchase_seller_credit,yt_port_purchase_buyer_credit,yt_port_name_overlay}`, `src/qb.c::{qb_mbf32_truth,qb_brun_random_record_number,qb_cint_mode}` | command B independently hydrates the buyer, performs the raw no-port gate and BRUN physical-port selection, joins Earth report-only or ordinary updater/report state, preserves the early owner and final report name, computes the shipped SINGLE/DOUBLE price, and emits the exact owner, price, affordability, offer, decline, sold, seller-transfer and success rows. Accepted persistence is seller then personal radio, optional shared editor, fresh title/treasury overlay, and fresh buyer debit/count; every ordinary result then takes the cached scanner and single fresh-prompt hydration. Native arithmetic and FIELD fixtures pin the MBF narrowing/order and unrelated-byte preservation; all 66 focused buy/Earth tests pass. The presentation family and all 45 wrapper edges are candidate. | `candidate` | add one complete injected active-prompt/raw-FIELD/cache/report/editor/seller/radio/title/buyer/scanner/reentry transaction, stale owner/name/cache variants, uncoerced player-record state, corrupt cells/record aliases, exact ANSI/plain/local framebuffer and every dependency/physical failure prefix |
| Main command-G whole-universe Genesis cycle | `src/yt_session.c::{command_shell,command_genesis,reload_player,session_0317,session_02fc,session_02db,session_a8d2,session_present_text}`, `src/yt_game.c::{yt_genesis_confirmation_prompt,yt_genesis_insufficient_rows}`, `src/qb.c::qb_compat_upper_n`, `src/yt_text.c::yt_text_write`, `src/yt_platform.c::{yt_platform_sibling_program,yt_platform_spawn}` | command G now performs its independent body hydration before output, retains the pre-body cached trader name for the raw prompt, emits the exact prophecy, cutoff, decline, insufficient and success rows, applies the cutoff after valid input, uses SINGLE `STR$` values and SINGLE shortfall subtraction, and writes `COMMAND$ CR LF EOF` before replacement launch. Ordinary branches return directly to the loop's single fresh hydration/prompt without a sector scan; successful replacement has no native continuation. Native fixtures pin binary prompt/shortfall composition and the canonical 297-byte plain/321-byte ANSI accepted bodies; all 23 focused upstream body/cycle tests pass. The presentation family and all 21 wrapper edges are candidate. | `candidate` | add one complete injected active-prompt/raw-FIELD/cache/input/file-registry/RUN/fresh-prompt transaction, exact disabled/decline/insufficient native bodies, alternate endpoint/local framebuffer state and every hydration/output/file/RUN failure prefix |
| Planet Ground Forces action and redraw cycle | `src/yt_session.c::{planet_menu,planet_garrison,reload_player,session_031f,session_036f,session_02db,session_02fc,session_present_text,session_sound}`, `src/yt_game.c::{yt_planet_garrison_prompt,yt_planet_garrison_after,yt_planet_garrison_overlay,yt_planet_garrison_player_overlay,yt_planet_garrison_success_row}`, `src/qb.c::qb_val` | the F action now performs independent entry planet/player reads, foreground-six direct blank and exact dynamic no-terminator prompt, 036F filtering, INT_D-to-SINGLE desired conversion, and two separately rounded SINGLE operations for remaining forces. Rejection emits the misspelled attention row. Acceptance freshly reads the planet, installs desired plus dirty-zero owner, emits positive success before live ownership and selector-four sound, writes planet before a second player hydration, then overlays only the fresh player FIELD ground lane without changing its hydrated cache and writes it. Native fixtures pin the canonical 147/111-byte ANSI/plain positive body, dirty-zero/positive owner transitions and unrelated-byte preservation. The positive caller join composes the 77-byte planet prompt, 3-byte F editor, 147/111-byte body and 87/77-byte redraw into exact 314/268-byte cycles with eleven completed local rows, exact color tape, selector-four sound, terminal style/pager state and open following prompt; all 42 focused upstream body/cycle tests pass. The presentation family and all 20 body edges are candidate. | `candidate` | add one complete injected planet-menu/raw-FIELD/cache/input/effect/redraw transaction, exact cancel/rejection/zero caller joins, local-mode and endpoint variants and every dependency/physical failure prefix |
| Planet landing caller and permission helper | `src/yt_session.c::{command_land,planet_permission_update,read_planet_physical,write_planet_physical,planet_update_cached_physical,planet_assault,planet_inventory,planet_menu,reload_player,session_0317,session_031f,session_036f,session_a8d2,session_present_text,session_sound,session_wait}`, `src/yt_game.c::{yt_planet_permission_run,yt_planet_landing_record,yt_planet_landing_immediate_allow,yt_planet_landing_valid_owner,yt_planet_landing_vacant,yt_planet_landing_attrition,yt_planet_landing_vacancy_overlay,yt_planet_landing_traffic_row,yt_planet_landing_sensor_row,yt_planet_landing_amount_prompt,yt_planet_landing_commitment,yt_planet_landing_commitment_valid,yt_planet_landing_unrest_row}`, `src/qb.c::{qb_brun_random_record_number,qb_val}` | the landing caller owns its title and independent player hydration, numeric-zero-only creation split, foreground-six landing row, cached carried-force value and exact permission-helper join. The isolated helper reconstructs the updater's raw logical argument independently from the caller's retained physical expression, then performs the fresh planet read, immediate/self/fresh-team admission, fractional-owner BRUN reads, distinct owner-status vacancy read, exact direct rows, selector-one/two-second-wait/fresh-planet/two-draw attrition/raw two-lane PUT/five-second-wait order, and exact styled denial. Its injected tapes pin complete vacant and live-owner denial order, every fallible dependency prefix on both paths, immediate and teammate admission, binary names, malformed name failure, unrelated fresh-record preservation, dirty failed-PUT residue and unconditional second draw. Denial then uses the caller's fresh defender snapshot and pre-helper carried cache before the existing input/assault continuation; successful landing enters inventory/menu. The canonical 27-test landing oracle remains green. The presentation family, all 63 caller/helper edges, and the `369D -> 39B1` menu join are candidate. | `candidate` | add one complete injected caller/FIELD/cache/input/assault/menu transaction, exact ANSI/plain/local/mode/carrier endpoints, malformed-link continuation and remaining physical adapter/failure prefixes |
| Planet ground-assault body | `src/yt_session.c::{planet_assault,read_planet_physical,write_planet_physical,planet_update_cached_physical,reload_player,session_present_text,session_sound,append_news_bytes}`, `src/yt_game.c::{yt_player_stored_name,yt_planet_stored_name,yt_planet_assault_player_overlay,yt_planet_assault_victory_overlay,yt_planet_assault_failure_overlay,yt_planet_assault_round,yt_planet_assault_attack_news,yt_planet_assault_status_row,yt_planet_assault_capture_news,yt_planet_assault_failure_row}`, `src/qb_format.c::qb_str_single` | the separate body now emits its entry blank before saving foreground and initializing the result, runs the updater before an independent planet/name snapshot, independently hydrates and raw-overlays the player debit before exact binary-safe attack news, then emits the blink/bold engagement row, blank and selector two. Every entered combat round consumes side then amount, performs separate SINGLE multiply/subtract plus INT/clamp, emits the correct foreground-three/four force row, and calls selector two only after defender-damage rows. The common terminal blank follows foreground restoration. Victory emits defenses/capture screen, news and selector-one calls before the fresh raw two-lane planet PUT; the both-zero fallback omits capture and clears both lanes. Defeat fresh-reads and persists only integral ground forces before its prefixed news and unprefixed blink/bold direct row. Native reducers pin binary rows/news, round arithmetic and unrelated-byte preservation; presentation pins the canonical 117-byte plain victory and 97-byte plain defeat, all 14 focused upstream cases pass, and all 38 body edges are candidate. | `candidate` | add one complete injected raw database/FIELD/cache/provider/news/sound transaction, exact ANSI/local/mode endpoint fixtures, native both-zero/nonprogress/failure cuts and every dependency/physical failure prefix |
| Planet zero-link creation arm | `src/yt_session.c::{create_planet,planet_rename,mutate_player_credits,reload_player,read_planet_physical,write_planet_physical,session_0317,session_02fc,session_02db,session_a8d2,session_present_text,session_sound,append_news_bytes}`, `src/yt_game.c::{yt_credit_mutation_run,yt_player_stored_name,yt_planet_rename_protected,yt_planet_rename_prepare_name,yt_planet_rename_overlay,yt_planet_creation_credit_row,yt_planet_creation_overlay,yt_planet_creation_timestamp_overlay,yt_planet_creation_credit_overlay,yt_planet_creation_news,yt_planet_creation_success_row}`, `src/qb.c::qb_brun_random_record_number` | the zero-link arm now emits its direct blank and two offer rows before an independent second player hydration, formats the cached credit as DOUBLE, applies the strict price gate, and uses the exact raw A8D2 purchase prompt. It scans physical slots from offset plus two through the live marker with BRUN record conversion, composes the exact shared Rename as the first durable write, and exits rather than resuming after protected/empty/reserved cancellation. Success independently reads and raw-overlays the thirteen initializer lanes (including both dirty zeros), fresh-reads and writes only the sector link, samples DATE then TIMER and fresh-overlays only timestamps, fresh-reads and raw-debits only credits through the shared owner, appends binary news using the pre-second-hydration trader cache, then emits the quoted created row, selector four and advice. Native reducers pin dynamic rows, dirty-zero and unrelated-byte preservation; the canonical 336-byte plain created body is exact, all 23 focused upstream cases pass, and all 33 arm edges are candidate. | `candidate` | add one complete injected raw FIELD/cache/concurrent-snapshot/file/provider transaction, exact poor/decline/full/protected/ANSI/local/mode variants, copied/live-marker stale seam, malformed record aliases and every dependency/physical failure prefix |
| MBF32/MBF64 serialization | `src/qb.c::{qb_mbf32_*,qb_mbf64_*}` | exact boundary/ties-even vectors plus 16,384 deterministic raw/host oracle cases in `tests/test_qb.c`; dirty-zero, underflow and overflow preservation in `tests/test_data.c` | `verified` | operation-specific arithmetic overflow and runtime-error routing remain separate components |
| BRUN `INT`/`FIX`/`CINT` arithmetic | `src/qb.c::{qb_cint,qb_cint_mode}` | nearest/half-away and live-mode-`04h` floor boundary vectors in `tests/test_qb.c` | `candidate` | complete raw malformed-MBF source handling, special mode-byte residues, overflow state and runtime-error routing; audit all callers for the live conversion mode |
| BRUN random-record conversion | `src/qb.c::qb_brun_random_record_number`, `src/yt_game.c::yt_port_rename_record`, `src/yt_portname.c::yt_portname_record_number` | positive fractional truncation, negative floor/two's-complement, 24-bit saturation and compiled SINGLE port-offset boundaries in `tests/test_qb.c`, `tests/test_score.c`, and `tests/test_utilities.c` | `candidate` | complete raw malformed-MBF argument/scratch state, zero/negative physical GET error routing, corrupt offset arithmetic and physical random-file adapter boundaries |
| BRUN `VAL` semantic grammar/result | `src/qb_format.c::qb_val` | recovered decimal/radix/underflow/overflow vectors and exact raw-MBF64 results in `tests/test_qb.c` | `verified` | allocator/heap/CPU scratch and ERR-6 routing remain part of the separate public runtime transition |
| General BRUN `STR$`/numeric core | `src/qb_format.c::{qb_str_integer,qb_str_single,qb_str_double,qb_str_mbf32,qb_str_mbf64,qb_print_*}` | recovered boundary/double-rounding vectors and 8,192 deterministic raw-MBF oracle vectors in `tests/test_qb.c` | `verified` | caller BASIC-type mapping, comma/newline destination routing and complete PRINT statements remain separate components |
| Scoreboard `PRINT USING` | `src/yt_score_format.c` | final adjacent boundary, sign-order and AA2D guard-stream vectors plus 40,960 deterministic raw-MBF/field oracle cases in `tests/test_score.c` | `verified` | row/file write failure prefixes remain part of scoreboard generation rather than the numeric formatter |
| BRUN `DATE$`/`TIME$` value formatting | `src/yt_config.c::{yt_format_date,yt_format_time}` | all 43,830 valid DOS dates and all 8,640,000 DOS time tuples in `tests/test_score.c` | `verified` | allocator/heap/CPU residue and caller-specific observation ordering remain separate runtime components |
| Shared game-day serial helper | `src/yt_config.c::{date_serial_epoch,yt_date_serial,yt_current_date_serial}` | all twelve month lanes, same-year/leap, adjacent/stale epoch, year-100 rollover, fractional-epoch, injected-clock and compiled one-prior-year early-exit fixtures in `tests/test_score.c::check_date_serial`, including epoch 26 plus 01/01/30 = 366; YTCONFIG root `1DF1`, initializer roots `YT-INIT:00B5`/`RMT-INIT:2339`, and YTMAINT root `79E3` are candidate with all 188 helper-body transfers mapped. `test_rmt_clock_boundaries` additionally pins separate `08D6`, `09CD`, and `17E6` observations and all 17 RMT clock calls. | `candidate` | physical clock failure and BRUN DATE$/substring/VAL allocation, scratch, by-reference, and caller-copy residues remain separate seams; non-RMT caller joins and larger YTCONFIG/initializer/maintenance compositions remain open |
| Shared `YT-SUB:94FD` timed wait | `src/yt_input_model.c::{yt_timed_wait_begin,yt_timed_wait_timer,yt_timed_wait_input}`, `src/yt_session.c::session_timed_wait`, `src/yt_input.c::yt_input_poll_legacy` | exact SINGLE-rounded deadline overwrite, TIMER-before-input priority, one/two-byte local return, exact-mode-zero LOC/serial selection, persistent serial scratch, nonzero-mode serial skip, loop counters, and unrepaired-midnight vectors in `tests/test_input_model.c::test_timed_wait`; origin-aware local-before-serial arbitration in `test_arbitration`; the distinct `YT:07F5` 99-second post-login owner and its direct-blank continuation are connected in `src/yt_session.c::post_login` and bounded by `tests/test_presentation.c::test_post_login_press_presentation` | `candidate` | raw caller-cell/heap/CPU residue, stack/INKEY/INPUT/device fault and retry prefixes, physical OpenDoors queue/result proof, corrupt duration/TIMER domains, and remaining caller-specific duration/continuation fixtures |
| Current/adjacent-sector private pause | `src/yt_session.c::{display_sector,display_sector_one}`, `src/yt_pager.c::{yt_sector_pager_begin,yt_sector_pager_add,yt_sector_pager_finish_sector}` | exact initial count 3, specified row increments, carry across physical adjacent slots, strict `>15` gate, reset to zero, 15-second `YT-SUB:94FD` owner, and no duplicate current-sector render after the adjacent trailer in `tests/test_presentation.c::test_sector_private_pager` plus shared wait fixtures | `candidate` | exact full scanner byte/event/state/effect fixture, all candidate/cache/RNG and persistence variants, physical failures, raw wait cell and retry/fault paths |
| Connected gameplay wait callers | `src/yt_session.c::{salvage_player,xannor_victory,projectile_opening,plasma_hop_report,launch_xannor_retaliation,launch_player_counterattack,emergency_warp}` | all 13 static salvage sites are assigned to initial/simple/post-cargo/cargo-row/NOTHING/final native phases; victory 99, plasma prelude 1+1, hop 0.5, retaliation/counterattack 4, and emergency heat 0.330000013 durations call the shared successful wait and propagate failure instead of continuing | `candidate` | exact joined caller component/effect/failure fixtures, raw duration cells, wait active-fault/retry state and physical adapter results |
| Ship-salvage cargo sampler | `src/yt_game.c::yt_salvage_cargo_sample`, `src/yt_random.c::yt_random_one_based_single`, `src/yt_session.c::{salvage_player,session_random_one_based}` | the cargo loop now follows the BASIC positive-step fractional terminal, draws even when corrupt remaining holds are nonpositive, passes that live SINGLE bound to the shared one-based helper, subtracts one before the three ordered cumulative comparisons, and retains the unconditional empty-slot else. Native fixtures pin a four-class without-replacement sequence and descending bounds, fractional stock/terminal behavior, negative remaining state, corrupt out-of-total selection and a second-draw failure prefix. The formerly missing salvage-to-`1AD9` call edge is candidate. | `candidate` | add the complete six-opening-draw/cap/fresh-GET/two-PUT/rows/news/waits transaction, nonfinite/raw BRUN arithmetic, physical provider/database/output failures and all caller joins |
| Shared current-player A41C hydration | `src/yt_game.c::yt_current_player_hydrate_run`, `src/yt_session.c::{reload_player,session_hydration_read_player,computer_prompt_hydrate}` | the ordinary integral-record transaction performs one independent player GET, installs the complete fresh raw FIELD image, hydrates only the documented gameplay scalars, computes the SINGLE current-sector-record scratch, refreshes sector and conditionally cloak cache, and writes shields last. It no longer overwrites the separate dynamic name or the unlisted last-active, killed-by, lottery and name-length caches. The native fixture pins all scalar results, raw unrelated FIELD bytes, preserved dynamic/unlisted caches, unrelated cache slots, anti-cloak suppression, ordinary record bounds, and zero-effect GET failure. The `A54F` refresh/skip split and shared `A579` return remain output- and RNG-free. | `candidate` | add byte-exact MBF cache/temp lanes, dirty-zero copy behavior, sector-add/CINT partial faults, corrupt record aliasing, physical BRUN GET prefixes and caller-specific joins |
| Shared paginated text-file viewer | `src/yt_session.c::{display_game_file,session_file_viewer_entry_present,session_file_viewer_present,session_file_viewer_missing_present,session_file_viewer_missing_news}`, `src/yt_text.c::{yt_file_viewer_entry,yt_file_viewer_stream_run,yt_file_viewer_next,yt_file_viewer_missing,yt_text_input_eof,yt_text_input_read_line}` | production now holds one case-resolved sequential stream from the pre-OPEN close through every pager callback to normal cleanup. The injected stream tape pins initial close, the post-close count reset, open, mandatory EOF-before-exact-Q, dynamic LINE INPUT, all five marker classes, repeated loop, Q and natural/immediate EOF exits, close/restoration/final blank and all 11 callback cuts; the physical join pins mixed-case open, read/unread EOF behavior, DOS EOF, row bytes and final close. Exact success joins now cover startup, both instruction callers, both retained-news callers, all computer-scoreboard selector paths, and the normal-exit scoreboard caller with its count-two/nonstop-one inherited state. Missing recovery is selected only for `YT_NOT_FOUND`, while other physical failures remain errors. All 22 qualified internal transfers are candidate. | `candidate` | join the live stream to full threshold editor/Ctrl-X/source/queue behavior; add complete caller framebuffers, raw BRUN file-block/refill/string/frame state, OS partial read/close/output faults and handler retry/resume composition |
| Shared fresh-turn gate and action finalizer | `src/yt_session.c::{fresh_no_turn_gate,finalize_action,spy_sweep,reload_player,launch_xannor_retaliation,session_031f,session_02fc,session_02db,session_attention,session_present_text}`, `src/yt_game.c::yt_no_turn_gate_denied` | the gate performs a fresh hydration, admits only strictly positive turns, and emits the exact foreground-inherited denial body. The finalizer stages turn offset 49 and optional cloak offset 125 in the fresh raw player FIELD, retains the shipped dirty-zero cloak clamp, uses foreground-seven `031F` cloak output followed by the exact positive/expired blank path, persists before the strict-`<51` warning-styled `02FC` turn row, consumes the unconditional trigger draw, and preserves the Xannor/destruction/post-GET routing. Native fixtures pin the positive-cloak plus turn framing and existing no-turn bodies. | `candidate` | add one deterministic full raw cache/store/RNG/spy/Xannor fixture, expired-cloak ANSI/sound, non-short-circuit conversion faults, pager/carrier cuts and every partial-I/O prefix |
| Active-spy scan/report/pause/movement engine | `src/yt_game.c::{yt_spy_sweep_run,spy_first_finding,yt_sector_mine_warning_row,yt_sector_planet_row,yt_sector_player_row,yt_sector_fighter_row}`, `src/yt_session.c::{spy_sweep,spy_read_sector,spy_update_planet,spy_read_planet,spy_read_player,spy_read_team,spy_present,spy_pause}` | the live finalizer child now follows the nonzero BASIC FOR count, foreground-seven per-spy entry, duplicate-sector suppression with inherited `found` leakage, first-finding marker commit before selector nine, disruption/mines/planet/cached-player/deployed-fighter order, per-candidate cloak draw and cache-only reveal, two-snapshot deployed-fighter owner/count split, one shared drain/press pause, six ordered warp conversions and retry draws, and the shipped foreground-zero exit. The representative provider tape pins all 30 outer operations and every failure prefix; its complete serial transcript is 363 bytes plain and 553 bytes ANSI. Focused branches pin exact zero, negative nonzero, no-finding, hidden cloak, stale-found suppression, duplicate disruption equality and the two-snapshot Xannor owner path. Roots `YT-SUB2:1D3A` and `24A5`, all 88 body/helper transfers, and the 30-site presentation family are candidate. | `candidate` | add raw BRUN dynamic-array allocation/erase/string/CPU/frame residue, all-zero-warp nontermination and corrupt adjacent-memory domains, persisted updater raw-FIELD/error joins, physical drain/wait/local/serial/output failure prefixes, and a complete finalizer composition |
| Main projectile command parent | `src/yt_session.c::{command_projectile,reload_player,fresh_no_turn_gate,finalize_action,session_projectile_resolver,launch_projectile,launch_player_counterattack,launch_xannor_retaliation,session_031f,session_036f,session_02db,session_02fc,session_present_text}`, `src/yt_game.c::{yt_projectile_target_prompt,yt_projectile_target_response,yt_projectile_quantity_response,yt_projectile_debit_overlay,yt_projectile_commit}` | command entry preserves the displayed missile/plasma snapshot; every target pass emits the direct blank, performs the explicit player hydrate and the gate's independent hydrate, tests no-turn before fresh ammunition, and uses the exact singular noun and sector bound. Empty target cancels, invalid zero/out-of-range targets emit the exact `02DB` row and repeat without refreshing the displayed snapshot, fractional in-range sectors are retained, and quantity uses E-clear/`VAL`/floor with exact cancel and excessive-ammunition behavior. Acceptance emits its direct blank before the shared finalizer, debit/write and typed child chain. The extracted transaction applies only the selected raw ammunition overlay, writes and flushes the post-finalizer player, clears the destruction result only after successful persistence, and then calls the selected resolver with the live origin by reference. Its raw-disk fixture observes the durable record from inside both callbacks, pins exact offset-97/113 changes and unrelated-byte preservation, callback mutations, resolver-failure durability, and PUT-failure suppression. Model fixtures pin both exact prompts and numeric boundaries; presentation pins the canonical no-ammunition, invalid-target/retry, excessive-quantity, accepted-blank and finalizer byte partitions. All 60 qualified parent transfers through the resolver bridge and postlaunch tail are candidate. | `candidate` | add a complete dual-hydration/finalizer/raw-child-return state/effect fixture, active-main entry/ordinary/fatal return joins, dirty plasma attribution and raw selector/editor/runtime state, carrier/pager/local endpoints and every dependency/physical partial-failure prefix |
| Cruise-missile full-player scan, damage and persistence continuation | `src/yt_session.c::{missile_sector,friendship_read_player,projectile_damage_draw,random_value,deploy_victim_mines,kill_player,salvage_player,append_news_bytes,append_news,session_present_text,session_sound}`, `src/yt_game.c::{yt_projectile_candidate_route,yt_projectile_candidate_admitted,yt_friendship_resolve,yt_projectile_player_damage,yt_projectile_damage_iteration,yt_projectile_attack_first_rows,yt_projectile_destroyed_rows,yt_projectile_player_survives,yt_projectile_salvage_admitted,yt_projectile_death_continuation,yt_projectile_survivor_sets_counterattack,yt_projectile_survivor_overlay,yt_projectile_victim_mines_overlay,yt_projectile_sector_mines_overlay}` | the full scan preserves the compiled sector-mismatch/empty-stock/shooter-exclusion route order, invokes the shared friendship helper for its exact GET side effects, then performs the caller's unconditional target GET before cached-cloak and provoker admission. Rejected candidates consume no RNG. An admitted candidate emits selector two, saves the original SINGLE missile count as its positive-step FOR terminal, decrements the live by-reference count first, consumes scanner/fighter/shield-test draws unconditionally and the shield-damage draw conditionally, promotes the shield-test draw for the original DOUBLE fighter operand, and applies the scanner predicate, joint lethal exit and separate clamps. It then sets foreground five, performs the distinct presentation GET, appends/displays both exact rows, and takes the fresh death or survivor GET. Death clears only victim mines, emits the blinking rows, adds carried mines through a fresh sector overlay, composes the shared death and conditional salvage children, and applies the positive-mine re-entry and sub-one return gates. Survival overwrites only shields/fighters/scanner on the fresh record, including the exact dirty scanner zero, suppresses the counterlaunch latch only for Xannor and returns directly without later planet/footer work. Native fixtures pin the fractional `2.5` two-iteration case, three/four-draw cadence, scanner/CINT paths, all row bytes, death/salvage routes, and unrelated-byte preservation for every overlay. All 62 transfers at `7505..79EA` are candidate. The planet, defense, mine, victim and survivor writes persist their explicit FIELD overlays directly, avoiding unrelated-field normalization. | `candidate` | add one injected whole-body raw FIELD/cache/database/output transaction with ordered failures; finish planet, route and corrupt-state slices before promoting the root |
| Cruise-missile linked-planet front | `src/yt_session.c::{missile_sector,missile_planet_impact,planet_update_cached_physical,read_planet_physical,friendship_read_player,projectile_planet_read,projectile_planet_write,projectile_sector_read,projectile_sector_write,projectile_planet_present,projectile_planet_news,projectile_planet_sound,session_present_text,append_news_bytes,session_sound}`, `src/yt_game.c::{yt_projectile_physical_record,yt_friendship_resolve,yt_projectile_friendly_planet_row,yt_projectile_planet_attack_rows,yt_projectile_planet_ground_overlay,yt_projectile_planet_productivity_overlay,yt_projectile_planet_destroy_overlay,yt_projectile_sector_unlink_overlay,yt_projectile_planet_ground_damage,yt_projectile_planet_productivity_damage,yt_projectile_planet_ground_row,yt_projectile_planet_productivity_row,yt_projectile_planet_impact_run}`, `src/qb.c::{qb_cint,qb_brun_random_record_number}` | after full-player scan completion, only `CINT(raw link)` gates numeric zero; the updater and every planet GET/PUT use `BRUN(SINGLE(planet offset + raw link))`, retaining fractional-link and 24-bit behavior. The updater runs before friendship or friendliness. Owner self-match is immediate; eligible other owners use the canonical friendship child and unconditional planet reload, including floor record conversion rather than CINT rounding. Friendly output returns to the route, while hostile output appends news, invokes selector two and dispatches on numeric-nonzero ground forces. Patterned-record fixtures pin all ground/owner, six productivity/stock, active-marker and sector-link overlays plus both dirty-zero exponents. All 24 transfers at `7A05..7C1C` are candidate. Extracted RNG models additionally pin negative-ground zero-draw owner clearing, stale-updater-ore admission, exact one/three-draw cadence and partial failures, physical-order normalization, strict stock caps, destruction predicates and early returns; 32 arithmetic/control transfers at `7C29..8096` are candidate. The live continuation now delegates to one provider-driven transaction which pins the exact ground/productivity/destruction payloads, direct-before-news order, fresh physical-record GET/overlay/PUT order, selector-three placement, ground-exhaustion early return, unrelated-byte preservation and every one of its 19 injected callback failure prefixes. The six formerly missing direct/news calls at `7CEC`, `7CF5`, `7EEE`, `7EF7`, `8068` and `8083`, plus the exact `807A` caller transcript, are candidate. | `candidate` | add dependency-internal physical partials, dynamic strings and arrays, raw updater/FIELD/cache state, remaining route-builder/black-hole/defense/mine joins and corrupt-state domains |
| Cruise-missile opening transaction | `src/yt_game.c::yt_projectile_cruise_opening_run`, `src/yt_session.c::{projectile_opening,cruise_opening_sound,cruise_opening_present,session_present_text}` | the live opening now owns selector four followed by an empty direct-line call, the exact 46-byte raw `Loading course into misile targeting computer.` payload, a second empty direct-line call that supplies its newline, the mine-news scratch zero, and the direct `*** Tracking Report ***` row. The ordered provider tape distinguishes raw from direct-line calls even though their composed bytes match the old line shortcut, observes the scratch before every callback, and pins all five failure prefixes: it retains its incoming value through the loading terminator and is zero before a successful or failed tracking call. The natural command-cycle fixture composes the same raw/direct split while retaining its exact 331-byte plain and 351-byte ANSI streams. The selector call at `6CA5` and four formerly missing output calls at `6CB7`, `6CC9`, `6CDB`, and `6CF6` are candidate. | `candidate` | add raw BRUN descriptor/frame/scratch residue, inherited pager/presentation state variants, physical remote/local output and sound adapters, and their dependency-internal failures |
| Cruise-missile route-entry shooter/team side effect | `src/yt_game.c::yt_projectile_route_entry_run`, `src/yt_session.c::{launch_projectile,cruise_route_entry_read_player}` | after every successful initial or black-hole-rebuilt route, the live resolver copies the start into current-hop scratch, initializes shooter-team to zero, and performs exactly one player GET only when shooter is strictly above two and no greater than the configured player-record boundary. It copies the fresh team value and replaces values below one with exact internal `-99999`; skipped GET lanes therefore also receive that sentinel. The provider fixture pins lower/upper/equality gates, Xannor and out-of-range no-GET lanes, positive, zero, fractional, negative and unordered team values, fractional configured-bound behavior, retained fractional start in the extracted reducer, and the failed-GET prefix where current-hop and zero team initialization survive but sentinel replacement does not. All six formerly missing branches and joins at `6DA0..6DDD` are candidate. | `candidate` | add raw MBF scratch bytes, CVS/FIELD replacement and FLAGS/frame residue, physical BRUN GET adapter cuts, corrupt validated-entry domains and a repeated black-hole rebuild composition |
| Cruise-missile black-hole detection and reroute | `src/yt_game.c::{yt_projectile_is_black_hole,yt_projectile_cruise_reroute_run}`, `src/yt_session.c::{launch_projectile,cruise_reroute_line,cruise_reroute_attention,cruise_reroute_random,session_attention_bytes}` | either exact hop equality enters one live ordered transaction; all other and unordered values bypass it. The transaction emits the empty direct row and exact `The missiles are deflected by a black hole in sector<STR$(hop)>!` attention event before touching route arguments or RNG, then writes the old hop to the caller origin, consumes exactly one draw, computes destination through SINGLE offset subtraction, SINGLE multiplication, `INT`, and SINGLE addition of one, and re-enters the route builder without replaying the opening. Its provider tape pins exact payload/order, argument values observed at every callback, the retained-origin/unchanged-destination RNG-failure prefix, two output-failure zero-state prefixes, configured span arithmetic, and first/second/nonmatch/unordered detection. All seven formerly missing branches, output calls and back-edge at `6E21..6EA3` are candidate. | `candidate` | add raw MBF arguments/config/numeric-temporary/FLAGS/frame bytes, A406 dependency-internal partials, physical output/sound/RNG/route failures, inherited presentation variants and repeated reroute composition |
| Cruise-missile Union Police terminal | `src/yt_game.c::{yt_projectile_union_police_admitted,yt_projectile_union_police_run}`, `src/yt_session.c::{launch_projectile,cruise_union_police_present,session_present_text}` | the live cruise-only gate evaluates raw SINGLE hop below eight, raw SINGLE destination below eight, numeric-zero counterattack and numeric-zero Xannor-provoker latches. The resolver now retains the incoming fractional destination instead of eagerly replacing it with `CINT(destination)`, while route construction still performs its own documented conversion. Admission emits exactly one 45-byte `The Union Police have destroyed the Missiles!` direct line and returns without footer, sector GET or RNG. The provider fixture pins negative/fractional/exact-eight/unordered numeric boundaries, fractional destination `7.5`, both nonzero latch polarities, exact payload, silent rejection and the failed-output prefix with terminal admission already selected. All eight formerly missing predicate, rejection, output and return transfers at `6EB2..6EF8` are candidate. | `candidate` | add raw MBF argument/latch/boolean-temp/FLAGS/frame bytes, physical remote/local output failure details, full route-array predecessor state and caller-specific return composition |
| Cruise-missile cheap sector-presence probe | `src/yt_game.c::yt_projectile_sector_probe_run`, `src/yt_session.c::{launch_projectile,missile_sector}` | the live cruise hop performs one initial sector GET, then builds presence from the four independent strict-positive mine, fighter, port and planet predicates. Regardless of that object result, it scans the cached player-sector array with a SINGLE counter starting at two through the raw configured player terminal, uses `CINT(counter)` for both cache indices, and admits the first exact-sector candidate whose cached cloak is numeric zero or whose raw counter equals the Xannor-provoker latch. A match records presence and exits early; otherwise the final counter is the first value above the raw terminal. No presence returns directly to the next route hop, avoiding the old extra sector GET and full-player scan. The extracted fixture pins each object predicate, non-short-circuit cache scanning, negative/unordered object values, fractional/low/unordered terminals, cloak rejection, exact Xannor override, first-match state, CINT cache bounds and the range-failure prefix. All 17 formerly missing branches and joins at `6F36..701F` are candidate. | `candidate` | add raw MBF object/cache/config/boolean-temporary/FLAGS/frame bytes, physical first-GET failure state, corrupt cache descriptors and a complete caller-specific multi-hop transaction |
| Cruise-missile fighter-defense front | `src/yt_game.c::{yt_projectile_defense_front_run,yt_projectile_defense_row,yt_friendship_resolve}`, `src/yt_session.c::{missile_sector,cruise_defense_owner,cruise_defense_friendship,friendship_read_player,cruise_defense_present,cruise_defense_sound}` | only a strictly positive deployed-fighter count enters the front and is promoted to DOUBLE for its report. The owner label starts as `The Xannor`, exact owner `-2` selects `Mercenaries`, and every owner above one first performs the independent raw random-record owner GET/name extraction and then invokes the shared friendship transaction with its own fresh current/candidate GET sequence. Exact shooter ownership overrides the resulting label with `THEM` for shooter `-1` or `YOU` otherwise and forces friendship. The exact binary-safe `Sector:<STR$ sector> defended by <owner> with<STR$ promoted fighters> fighters.` bold row precedes the friendship branch. Friendly defense bypasses sound and combat to the mine layer; hostile defense sets bold and invokes selector two before combat. The provider fixture pins nonpositive/unordered admission, binary owner names, fractional owner preservation, Xannor/Mercenary/self labels, friendly/hostile routing, exact output, selector value/order and all four owner/friendship/output/sound failure prefixes. Seventeen formerly missing transfers at `702B..714F` are candidate, and the existing `7168` sound call now has its caller transcript. | `candidate` | add raw MBF/FIELD/dynamic-string/FLAGS/frame residue, exact physical owner/friendship GET prefixes, inherited pager/local/sound endpoint failures, corrupt random-record domains and joined mine/combat continuations |
| Cruise-missile fighter-defense damage and persistence | `src/yt_game.c::yt_projectile_defense_combat_run`, `src/yt_session.c::{missile_sector,projectile_damage_draw,cruise_defense_damage_present,cruise_defense_news,cruise_defense_read_sector,cruise_defense_write_sector,cruise_defense_victory,xannor_victory}` | a raw positive missile argument snapshots the SINGLE `FOR` terminal before damage: `2.5` permits exactly counters one and two, while `0.5` performs no RNG iteration but still emits the destroyed-zero row and executes the fresh persistence transaction. Nonpositive and unordered arguments bypass the damage body to the mine layer. Each iteration consumes one draw, evaluates `INT(SINGLE(RND*5000+destroyed))`, decrements live missiles in SINGLE and clamps to the original DOUBLE defense. The exact binary-safe direct row precedes DOUBLE remainder calculation; destruction above nine then emits the exact binary-safe shooter news row. A fresh sector GET preserves unrelated bytes, first overlays the CSNG remainder, writes dirty MBF zero to both defense fields when exhausted, or records every surviving Xannor defense shooter—including `-1`—as the provoker before PUT. PUT precedes the Headquarters victory call, and the post-PUT live missile `<1` gate returns instead of entering mines. The provider fixture pins fractional terminals, zero-iteration positive input, nonpositive/unordered bypass, arithmetic, binary actor names, direct/news/GET/PUT/victory order, byte overlays, provoker timing, Headquarters admission and every callback failure prefix. Twenty-four formerly missing transfers at `717C..7331` are candidate, and the existing `731B` victory call now has its complete caller transaction. | `candidate` | add raw MBF32/MBF64/FIELD/name/config/FLAGS/frame state, physical RNG/output/news/GET/PUT/victory partials and joined mine/caller return composition |
| Cruise-missile sector-mine transaction | `src/yt_game.c::yt_projectile_sector_mine_run`, `src/yt_session.c::{missile_sector,cruise_mine_read_sector,cruise_mine_present,cruise_mine_sound,cruise_mine_news,cruise_mine_write_sector}` | every mine pass starts with a fresh sector GET and promotes the raw SINGLE mine count to DOUBLE. Only strict positive counts enter; zero, negative and unordered counts proceed to the player scan. An admitted pass emits the exact binary-safe hit row, selector five, and—only when the raw SINGLE last-news sector differs—the binary-safe shooter news row before updating that latch. The destroyed quantity is the lesser of the live missile SINGLE promoted to DOUBLE and the mine DOUBLE narrowed with `CSNG`; exact singular/plural text uses strict `destroyed>1`. A second fresh GET preserves unrelated bytes while offset 129 receives `CSNG(DOUBLE observed mines−SINGLE destroyed)`, then PUT precedes the live SINGLE missile subtraction. Missiles below one return; otherwise the back-edge performs another fresh GET, allowing exhausted mines to continue into players. The opening scratch is now retained as its executable SINGLE type. The provider fixture pins both lesser-value branches, exact binary payloads, selector and news suppression, fresh-record overlay, singular/plural rows, nonpositive/unordered gates, return/re-entry order and every callback failure prefix. Seventeen formerly missing transfers at `7378..74F3` are candidate, and the existing `73C2` sound call now has its complete caller transaction. | `candidate` | add raw MBF32/MBF64/FIELD/name/config/FLAGS/frame state, corrupt missile arithmetic and physical GET/output/sound/news/PUT partials plus joined player-scan/route return composition |
| Cruise-missile route prefix, failure suffix and footer continuation | `src/yt_session.c::{launch_projectile,build_route,route_sector_reader,route_failure_report,missile_route_failure_suffix,missile_sector,missile_planet_impact,missile_footer}`, `src/yt_game.c::{yt_projectile_route_avoid_enabled,yt_projectile_route_failure_row,yt_projectile_route_has_next,yt_projectile_post_impact_route,yt_projectile_footer_row}`, `src/yt_route.c::yt_route_build` | the caller now retains the route builder's distinct outcome and returned raw-numeric status. Ordinary player fire with no pending counterlaunch initializes status one; Xannor and counterlaunch fire initialize numeric zero. This preserves the compiled same-sector split: status one takes only the caller's blank plus blinking/bold `Missles self destructed!` suffix, whereas status zero consumes the zero route and reaches the footer. Actual not-found first emits the route helper's two blanks and blinking/bold diagnostic, then the same caller suffix. Exact plain and ANSI streams pin both emphasis transitions. The live cruise loop carries an explicit phase-return versus post-impact continuation result instead of inferring both from missile quantity. Route-array zero and nonzero branches are separately modeled; post-impact positive values continue to the next hop, while zero, negative and unordered values select the footer. Ground/productivity exhaustion and every other phase-owned early return bypass the footer. The exact 21-byte `*** End of Report ***` payload is owned by the live footer adapter. Eleven prefix/failure transfers at `6D04..6D7F` and all eight terminal joins at `6E0D..6E12` and `809F..80B9` are candidate. | `candidate` | add a deterministic raw multi-hop route-array/builder transaction, fractional/raw latch/start/status/workspace domains, caller-specific return state and dependency-internal GET/allocation/output failure prefixes |
| Main nonfatal missile cycle | `src/yt_session.c::{command_shell,command_projectile,launch_projectile,projectile_opening,display_sector,display_sector_one,reload_player,session_031f,session_036f,session_present_text}`, `src/yt_game.c::{yt_projectile_target_prompt,yt_projectile_target_response,yt_projectile_quantity_response}` | ordinary missile/plasma parent outcomes now take the shipped scanner-only continuation rather than the gameplay hazard router. The natural no-impact missile witness composes active `)` dispatch, target/quantity editing, turn row, launch/tracking/end rows, current-sector scanner, fresh prompt and empty-poll boundary. `tests/test_presentation.c::test_projectile_ordinary_cycle_presentation` pins the complete 331-byte plain and 351-byte ANSI streams, including both color transitions and final prompt space. | `candidate` | add authoritative raw dual-hydration/FIELD/store/cache/RNG/by-reference state/effect fixture, exact scanner terminal residue and AB36 arbitration, nonempty impact/wrapper successors, physical local framebuffer and every dependency/failure prefix |
| Missile counterlaunch wrapper and postlaunch fatal bridge | `src/yt_game.c::{yt_counterlaunch_run,yt_counterlaunch_score_count,yt_counterlaunch_debit_overlay,yt_counterlaunch_rows,yt_player_stored_name}`, `src/yt_session.c::{launch_player_counterattack,session_counterlaunch_random,session_counterlaunch_write_player,session_counterlaunch_present,session_counterlaunch_news,session_counterlaunch_projectile,session_counterlaunch_wait,launch_xannor_retaliation,command_projectile,common_fatal_self,kill_player}`, `src/qb.c::qb_mbf32_truth`, `src/yt_maint.c::yt_news_append_bytes` | root `YT-SUB:9D3D` and all 25 qualified transfers are candidate. The provider-driven wrapper pins all three silent/stuck-latch entry gates; raw killed-by and sub-one-ammunition rejection with canonical latch clear; original cache preservation with record/name/cloak swap; the retained MBF64 score count, nonpositive stale count and exact fallback draw; two ordered target GETs and stale-ammunition overlay on the fresh raw image; PUT-before-output; exact direct blank/bold row and binary news; and the cruise child handoff with original destination, first-GET origin, retained count and both latches by reference. Child failure preserves the swapped context, zero cloak, latch/count mutations and durable prefix. Normal return clears the child-mutated counterattack latch, restores record/name/cloak without hydrating unrelated cached player fields, performs the final GET for raw killed-by truth, updates destruction/sector cache and waits four seconds. The outer projectile caller alone consumes the shared Xannor latch afterward. Ordered native fixtures cover every entry/rejection branch and all ten first-GET-through-wait failure prefixes, plus stale negative retained count with no draw. Existing presentation/fatal fixtures pin the joined output families; upstream `1248add9` makes 1,875 plain/2,057 ANSI fatal bytes and hashes `ae296b...`/`1e3eb9...` authoritative. | `candidate` | add exact binary dynamic actor identity inside the cruise child, raw BRUN RNG/MBF/FIELD/cache/frame identity, corrupt fractional latch and stale-count domains, physical provider partials, and the complete fatal joined state/output/effect/normal-exit witness |
| Xannor retaliation wrapper | `src/yt_game.c::yt_xannor_retaliation_run`, `src/yt_session.c::{launch_xannor_retaliation,session_xannor_read_sector,session_xannor_random,session_xannor_present,session_projectile_resolver,session_xannor_read_player,session_xannor_wait}` | root `YT-SUB:9956` and all 23 qualified body/call/return transfers are candidate. The provider-driven transaction pins the score/provoker conjunction, exact Headquarters fighter/owner gate including admitted negative counts, nested three-draw count followed by the unconditional destination draw, direct blank, provoker-only cloak zero, `-1` / `The Xannor` actor swap without changing the cached player sector, provoked destination override, exact bold row, live Headquarters-origin alias, child nonreturn state, record/name/cloak restoration, final player GET without unrelated cache hydration, raw killed-by truth, destruction/sector-cache mutation, four-second wait and successful-return provoker clear. Ordered fixtures cover nested-draw, blank, destination, bold-row, child, final-GET and wait failure prefixes; presentation pins the representative 55-byte plain and 67-byte ANSI streams. | `candidate` | add raw BRUN reseed/arithmetic/MBF/cache/FIELD identity, provider-operation partials, physical output/database/wait adapters, corrupt record/config domains and complete caller joins |
| Common-fatal wait bridge | `src/yt_session.c::{common_fatal_self,session_02db,kill_player,yt_session_run,session_wait}` | deployed-combat and mine destruction paths now share the exact successful foreground-three fatal row, current-player reload, selector-three sound, self-killer death call, one qualified five-second wait, and unprompted normal-exit disposition; `tests/test_presentation.c::test_common_fatal_notice` pins plain/ANSI bytes, bold/blink residue, pager count and local reset, while `tests/test_input_model.c::test_timed_wait` pins duration/deadline behavior | `candidate` | add a deterministic full death/FIELD/news/exit composition, carrier/input/pager and partial-failure prefixes, physical endpoints, and the other six shipped fatal caller joins |
| Game-level admission/registration/exit waits | `src/yt_session.c::{registration,admit_player,lockout,quit_session,session_wait}` | evaluation/registered registration waits 10/2, returning rebuild 5 after persistence, matching lockout denial 10 before termination, and evaluation normal-exit reminder 10 plus direct blank are connected to their qualified caller edges with deadline vectors in `tests/test_input_model.c::test_timed_wait`; `tests/test_presentation.c::test_full_normal_exit_presentation` additionally joins the evaluation-only 10-second timer path between the exact reminder and following blank | `candidate` | exact joined title/registration, returning-login and pre-admission/lockout state/effect/failure fixtures; add normal-exit key/serial/failure variants and physical wait proof, while the platform OpenDoors startup/exit transaction remains deferred separately |
| Earth lottery wait sequence | `src/yt_session.c::{lottery,earth_store,session_wait}` | six pre-roll `0.400000006` waits, every inner `0.00499999989` animation wait, post-digits `0.400000006`, positive-award `3`, and unconditional caller `3` are connected to their five qualified `94FD` edges with SINGLE-rounded deadline vectors | `candidate` | exact dummy-byte/backspace/local cursor animation, complete branch-specific settlement/output/RNG/persistence/failure fixture, raw wait cells and physical adapter proof |
| Earth Sensors/Info display waits | `src/yt_session.c::{earth_store,display_sector,show_ship,session_wait}` | exact full-token `S` and `I` gates invoke the existing adjacent-scanner and Info owners, respectively, then the distinct qualified nine-second waits before redraw; the duration is pinned in `tests/test_input_model.c::test_timed_wait` | `candidate` | complete exact Earth report/table/menu/editor/redraw composition, joined scanner/Info state/effect/failure fixtures, raw wait cells and physical adapter proof |
| Vacant-planet governor wait sequence | `src/yt_session.c::{command_land,session_wait}` | exact governor row/sound, two-second wait, fresh planet read, two unconditional draws over the first cached ground count, two shipped unrest rows, fresh-record ground/owner overlay and write, then five-second wait; both durations have SINGLE deadline vectors in `tests/test_input_model.c::test_timed_wait` | `candidate` | complete linked-landing permission/output/FIELD and vacancy state/effect/failure fixture, malformed record conversions, raw wait cells and physical adapter proof |
| Radio-message reader/private pager | `src/yt_session.c::{radio_read,radio_name_bytes}`, `src/yt_file.c::{yt_radio_file_open,yt_radio_file_size,yt_radio_file_get,yt_radio_file_put,yt_radio_file_close}`, `src/yt_data.c::{yt_radio_reader_decide,yt_radio_reader_header,yt_radio_reader_mutate}`, `src/yt_pager.c::{yt_radio_pager_begin,yt_radio_pager_add_pair,yt_radio_pager_add_body}`, `src/yt_presentation.c::{yt_present_line,yt_present_character}` | numeric-zero/nonzero headings, exact signed-word `CINT(mode)` visibility including corrupt nonzero `.49`, inclusive zero-filled EOF probe, physical short-record prefix plus zero suffix, both fixed-field name resolutions before pair comparison, binary-safe changed-pair header, verbatim 74-byte body, strict private `>22` pause/reset, raw `[Pause]`, 99-second wait/direct blank, and post-pause numeric-zero-only decrement/dirty-zero mutation are represented. The random adapter fixture pins close-before-open, update-or-create, exact 4/4/4/74 binding, partial and past-EOF GETs, full PUT and close. | `candidate` | add raw BRUN file block/descriptors/frames, every shared OPEN/GET/player-name-GET/PUT/CLOSE and output failure prefix, raw wait fault/retry, asynchronous function keys and physical endpoint results |
| Direct-Attack casualty-radio alias helper | `src/yt_session.c::{attack_player,radio_append_bytes}`, `src/yt_game.c::{yt_direct_attack_radio_text,yt_player_stored_name}`, `src/qb.c::{qb_mbf64_encode,qb_str_mbf32}` | all three qualified `YT-SUB:[250B,255B)` transfers are mapped. The helper serializes the DOUBLE defender loss to MBF64, deliberately interprets its low four bytes as MBF32 for `STR$`, preserves the cached raw name, and hands sender `-2` plus the personal recipient to the binary-safe generic writer. Native fixtures pin ordinary integral loss as `0` and the `16777217` raw-alias boundary as `.5`; all 29 focused oracle tests pass. | `candidate` | add complete raw DS name/pointer/frame state, caller-before-casualty-write chronology, exact physical writer/error prefixes and joined result persistence/output |
| Shared low-time warning helper | `src/yt_presentation.c::yt_present_low_time`, `src/yt_session.c::{session_low_time,session_031f}` | root `YT-SUB:87CF`, its sole incoming call, and all 12 internal transfers are candidate. The native transducer compares VAL's DOUBLE against the remembered SINGLE before storing the changed value as SINGLE, then applies the strict stored-value-below-six warning gate. Focused fixtures pin unchanged and changed-six silent returns, round-to-six suppression, repeated fractional warnings caused by DOUBLE/SINGLE inequality, exact blank/BEL-or-BEEP/bold-row/blank ordering, local/remote state, VAL-overflow pre-effect failure, and an oversized construction failure after the remembered-time, first-blank, BEL and style prefix. | `candidate` | add raw MBF56 VAL/MBF32 remembered and dirty mode/FLAGS/descriptor/frame state, BRUN BEEP wait/reset, unrestricted dynamic strings, physical output faults and caller/error-router composition |
| Shared centered-line helper | `src/yt_presentation.c::yt_present_centered_line`, `src/yt_session.c::{session_centered_line,session_centered_line_bytes}` | root `YT-SUB:8887` and all ten internal transfers are candidate. The native transducer preserves a genuinely blank row, passes lengths above 78 without truncation, and applies left-only padding for lengths 1..78 using the compiler's positive-half-up result, equivalently `floor((79−LEN)/2)`. Focused fixtures pin lengths 0, 1, 3, 78 and 79, embedded NUL bytes, exact remote framing and the bounded-output failure. The registration transaction now maps the beta-only `A6B1` caller and pins its centered row before two local BEEPs and END. | `candidate` | add raw descriptor/heap/FLAGS/frame state, unrestricted dynamic strings, child/physical output failures and complete remaining caller compositions |
| `YT:02BF`/`02FC`/`0317`, `YT:B05D` and `YT:B1F3` presentation spine | `src/yt_session.c::{session_present_text,session_02fc,session_0317,session_02db,session_b05d,read_keyboard_line,session_command_notice}`, `src/yt_pager.c::yt_paged_row_run`, `src/yt_input.c::yt_input_poll_legacy`, `src/yt_input_model.c`, `src/yt_presentation.c::{yt_present_line,yt_present_paged_text,yt_present_paged_finish,yt_present_editor_echo}` | direct blank, forced-newline-zero B05D, blank-plus-row notice, raw-text then `LF/CR` remote split, local newline/color reset, newline suppression, mode-specific local/remote arbitration, source FIFO, extended-key rejection, Ctrl-X/Ctrl-R/printable/CR queue behavior, pager threshold/gates, compatibility uppercase, complete successful empty/E/NS plain/ANSI/mode-2 prompt transactions with nested counts and logical/local color residue, exact bounded `/R`/semicolon post-submit transforms, exact ANSI save/repeat plus terminal/fatal notice vectors, and a live file-viewer join pinning row-23 E/NS, nonboundary and boundary Ctrl-X, EOF-before-Q, source/queue residue, ANSI/mode-2 endpoint state, and every nested threshold callback cut after 22 committed rows in `tests/test_input_model.c` and `tests/test_presentation.c` | `candidate` | physical queue overflow and partial writes, active-fault/runtime-error routing, asynchronous SysOp interleaving, 94FD raw/fault/retry and physical-adapter fixtures, full AB36 internals, and complete caller framebuffers |
| Shared name/uppercase byte transforms | `src/qb.c::{qb_title_case_n,qb_ascii_upper_n,qb_compat_upper_n}` | exact punctuation/apostrophe examples, embedded-NUL and empty-loop vectors, and exhaustive 256-byte ASCII/compatibility transform checks in `tests/test_qb.c`; callers split between YT-SUB compatibility uppercase and LOCAL/YTCONFIG/RMT ASCII-only transforms. Roots `YT-SUB:1D99`, `YTCONFIG:2073`, `YTCONFIG:228F`, and `RMT-INIT:2B42` are candidate with all five, 33, nine, and 34 respective qualified successful body/return edges mapped; all 17 YTCONFIG, 33 initializer and 13 YT-SUB upstream tests pass. Together with the separately tested float-epoch date owner, this closes the site-free four-result `ytconfig-date-name-case-helpers` presentation family as candidate. | `verified` | movable-string heap allocation/faults, procedure-static MBF32 length/index/temp residue, active ERR-7/14 routing, caller joins and caller-specific raw frames remain separate components |
| Raw 137/86-byte records | `src/yt_data.c`, `src/yt_game.c`, `src/yt_file.c::{yt_database_open,yt_database_open_observed,yt_database_random_close,yt_database_read,yt_database_random_get,yt_database_write,yt_database_random_put,yt_database_set_seek_provider,yt_database_set_read_provider,yt_database_set_write_provider,yt_database_set_close_provider,yt_database_set_flush_provider}`, `src/yt_file.h::{yt_database_open_result,yt_database_close_result,yt_database_get_result,yt_database_put_result,yt_database_open_observation,yt_database_close_observation,yt_database_seek_observation,yt_database_read_observation,yt_database_write_observation}` | selected preservation plus injected disk-random OPEN/CLOSE/GET/PUT tests in `tests/test_data.c`. Update-or-create now begins with access 2, retries DOS 5 through 1 and 0, maps exhausted DOS 5 through exact ERR 70/75 extended results, distinguishes initial DOS 3 ERR 76, creates only for initial DOS 2, closes the temporary handle and reopens, classifies every create and reopen DOS byte, and retries a failed temporary close exactly once before ERR 70. Successful OPEN stores the handle before its IOCTL query, ignores query carry while using its device result, and retains a registered/open device handle on configure ERR 57. The typed tape pins both extended results, every initial/create/reopen/configure DOS byte, both ignored temporary-close retry results, malformed-provider ownership, exact operation/access order, and ordinary versus character-device returns. Random CLOSE returns without provider I/O for a missing registry entry, closes and unregisters on clear carry, and for each of the 255 first DOS errors performs exactly one retry whose result is ignored before retaining the first byte and reporting ERR 70 for disk or ERR 57 for device. Retry success, retry carry, consumed-stream, rejected-provider and malformed-observation fixtures pin registry and surviving physical-handle ownership; the native adapter consumes its `FILE *` exactly once. The host OPEN adapter uses descriptor metadata rather than synthetic cursor seeks and physically pins both existing and missing create/reopen paths. Ordinary YTDATA reads use BRUN random semantics: full, short, and zero-byte carry-clear reads return successfully after pre-read zero-fill, with exact prefix/suffix and cursor/full-record observations. Read carry retains only its accepted prefix over the zero fill. GET record zero and values above the 24-bit domain stop at typed ERR 63 before SEEK or buffer mutation; the maximum record retains its exact index and 2,298,478,318-byte offset. The default host adapter clears stale stdio error and `errno` state before each DOS-equivalent operation, so one call cannot invent carry or DOS-5 status for the next. File-one writes accept the shipped flag-`0x02` one-byte-short result, preserve the old final byte at the real short cursor, and retain an accepted prefix on carry without unregistering. Carry-clear rejected shorts retain their prefix, attempt CLOSE, remove the native registry entry before returning ERR 61, and distinguish successful CLOSE from a failed CLOSE with an orphaned physical handle. For both GET and PUT, all 255 DOS seek bytes retain their supplied terminal cursor and map to ERR 52 without reaching the data operation; all 255 read/write carry bytes retain accepted count/cursor, map non-5 to ERR 57, and map DOS 5 through both extended results ERR 70/75. Flush failure leaves the entry registered. | `candidate` | every layout-qualified alias and byte-preservation mutation; add raw BRUN allocation/path/file-number/FIELD/status/descriptor residue, buffered sequential CLOSE and public CLOSE_NO_ARGS, remaining unrestricted partial-operation observations, and connection from typed BASIC errors into caller-specific shared routers |
| Successful BRUN sequential APPEND image | `src/yt_text.c::yt_text_append_line` | final-128-byte boundary/earliest-marker, ignored `length-129`, missing-marker, embedded-NUL, embedded-EOF and repeat-append fixtures in `tests/test_data.c` | `verified` | 128-byte intermediate PRINT commits and physical failure prefixes remain a separate adapter component |
| YTNAME four-string `INPUT #` value grammar | `src/yt_names.c::{input_token,yt_names_load}` | cross-row groups, quoted comma/CRLF, doubled quote, NUL, LF-CR, EOF-tail, incomplete-group and exact-comparison fixtures in `tests/test_utilities.c` | `candidate` | dynamic strings above the native row capacity and allocator/store/failure prefixes are not yet represented |
| DOS-style file/text operations | `src/yt_file.c`, `src/yt_text.c` | exact-spelling preference and ambiguous case-collision checks plus selected read/write/delete cases in `tests/test_data.c`. Random update-or-create has a typed BRUN mode-4 state machine covering access fallback, create/close/reopen, DOS-byte-to-BASIC-error classification, ignored IOCTL-query carry, device configuration and open-handle ownership; native existing/missing paths prove no OPEN-induced cursor movement. Typed regular-random CLOSE covers missing entries, ordinary/device success, all first DOS error bytes, the one mandatory ignored-result retry, cleanup-before-error and unregistered surviving handles. | `candidate` | nested path components, buffering, sequential CLOSE writes, public CLOSE_NO_ARGS, LOF and remaining partial-operation retry/error mapping |
| Replacement CSPRNG provider and shared one-based draw | `src/yt_random.c::{yt_random_next,yt_random_one_based_single,yt_random_integer,yt_random_market_bases}`, `src/yt_session.c::{session_random_one_based,session_random_integer}` | injected-provider tests in `tests/test_data.c::test_random` pin raw 24-bit lower/maximum and adjacent `0.1` boundary samples, one-based SINGLE multiply/floor/add behavior, positive-fractional, negative and zero raw bounds, exact draw count, range-one result, integer-wrapper invalid-range no-draw and provider-failure output preservation. Salvage now calls the SINGLE helper instead of duplicating and narrowing this operation; the exact six-draw launch-base calculation remains pinned. | `candidate` | the authorized replacement source does not reproduce BRUN's seed stream; add nonfinite/raw BRUN arithmetic domains, caller-specific joins and remaining provider failure prefixes |
| Distributed immutable assets | `src/yt_assets.inc`, `data/` | exact sizes/checksums in `tests/test_assets.c` | `verified` | none for the declared eight-file byte scope |
| General OpenDoors output | `src/yt_output.c::{yt_out_bytes,yt_out_remote_bytes,yt_out_present_result}` | ordered replay-sink and PC-attribute vectors in `tests/test_presentation.c`; optimized/debug OpenDoors link | `candidate` | presentation replay now uses remote-only `od_disp` plus local-only counted `ODScrn` operations; add physical-result and Win32 framebuffer tests and migrate remaining provenance classes |
| Shared color/direct-output/attention transducers | `src/yt_presentation.c`, `src/yt_session.c::{session_attention,session_attention_bytes}` | exact ANSI/plain color cache, equal-color normalization, live-CINT-mode conversion, snoop/mode endpoint routing, CRLF framing, ordered attention/toggle endpoint timelines, right/fixed/centered width and ANSI-disabled bold-residue vectors in `tests/test_presentation.c`. Root `YT-SUB:A406` and all four internal transfers are candidate; its fixtures additionally pin mode-one local-only ordering and the complete post-output selector-six conversion-failure prefix. | `candidate` | replace fixed native result capacities with exact dynamic-string/failure behavior; prove physical OpenDoors writes and partial failures; connect all callers; local BASIC `PLAY` is the authorized ordered-event/no-host-audio departure |
| Sound dispatcher, 59-site caller inventory, and main `X` toggle | `src/yt_sound.c::{yt_sound_dispatch,yt_sound_toggle}`, `src/yt_presentation.c::yt_present_sound_toggle`, `src/yt_session.c::{session_sound,session_attention,command_shell}` | exact nine-cue, live-CINT-mode gates, stale-scratch and conversion-failure-prefix vectors in `tests/test_sound.c`; the complete nine-transfer `YT-SUB:9563..95EB` toggle helper is mapped, with nearest/floor conversion, bitwise-NOT ON/OFF selection, mode-zero local-sound preservation, nonzero-mode mirroring, exact 53-byte ANSI ON, 11-byte plain ON/OFF, local-only mode 1 and corrupt-mode-2 endpoint order pinned in `tests/test_presentation.c::test_sound_toggle`; all qualified-live static callsites are mapped to session operations; remote ANSI/BEL uses `yt_out_remote_bytes` | `candidate` | add raw MBF dirty-zero/scratch/frame state, direct-line and sound dependency/physical-failure prefixes, OpenDoors remote-write/Win32 framebuffer tests, and joined scanner/re-entry ordering; `YT:4F2D` selector 1 is qualified dead absent asynchronous corruption, while local BASIC `PLAY` is retained logically and intentionally silent |
| Local SysOp F4/F5/F9 logical handlers, F8 time body/wrapper, and F10 chat | `src/yt_sound.c::{yt_sound_sysop_toggle,yt_sound_sysop_snoop_toggle}`, `src/yt_input_model.c::{yt_sysop_chat_begin,yt_sysop_chat_step,yt_sysop_chat_finish,yt_sysop_f5_compose}`, `src/yt_presentation.c::{yt_present_sysop_sound_toggle,yt_present_sysop_snoop_toggle,yt_present_sysop_time_handler,yt_present_sysop_time_prompt,yt_present_sysop_time_replace,yt_present_sysop_chat_header,yt_present_status_row,yt_present_line,yt_present_character}`, `src/yt_session.c::{session_b05d,session_02bf,session_02fc}`, `src/yt_pager.c`, `src/yt_output.c` | roots `YT:A9E1`, `YT:BA00`, `YT:B3FB`, `YT:B66A`, `YT:B6D7`, and `YT-SUB:20A7` are candidate with their logical transfers mapped. Native fixtures distinguish F4 from player command X, pin its local-speaker toggle and local-only three-print notice, and pin F5's BA00 checkpoint, BA01 no-return END, fixed BRUN cleanup/DOS-exit phases, local END_CLEANUP and same-F5 pending residue. They pin F9's exact-mode-one skip and ON/OFF status/cursor/clear event order and F8's independent TIMER samples, SINGLE/VAL/clamp arithmetic and body-before-stale-B05D order. The resumable F10 reducer pins raw header ownership, SINGLE remaining-time capture, local-before-serial override, per-poll carrier checks, compiled lexicographic key acceptance, CR, raw backspace, ordinary destination asymmetry, wrap thresholds, delayed ESC exit, two independent exit TIMER samples, accumulator clear and exact CR queue replacement. The 90 focused upstream process-exit/root-event cases pass. | `candidate` | connect physical OpenDoors KEY registration; add BRUN event stack, raw INPUT/redo/editor, complete mutable greeting/stale-B05D/recursive-pager and carrier-END joins, inherited framebuffer/file registry, physical cleanup observation, Ctrl-C, raw frames/scratch, partial physical output and every runtime-failure prefix |
| Main-module runtime-error dispatcher | `src/yt_main_error.c::{yt_main_error_compose,yt_main_error_commit_fatal,yt_main_error_commit_fatal_to}`, `src/yt_presentation.c::yt_present_forced_local_line`, `src/yt_session.c::{session_b05d,session_0317,append_news}`, `src/yt_text.c::yt_text_append_line`, `src/yt_output.c` | root `YT:B2DA` and all 16 internal dispatch/call/jump transfers are candidate. The native owner pins ERR-24/57 retry precedence before all output, ERL-40000 precedence over resumable errors, the 5/6/13/15 gameplay route, exact SINGLE/INTEGER forced-local debug text, binary-safe missing-path construction and exact fatal DATE$/TIME$ record grammar. The fatal transaction finishes the supplied session presenter before opening the fixed `ERRORS.DOR` sink, then uses the canonical final-128-byte DOS-EOF scan and successful close/truncation transform. Native fixtures cover positive and negative formatter vectors, path capacity, route-local unused-input behavior, forced-local event ownership, a fresh 76-byte log image, an 81-byte stale-tail replacement, presentation-before-file ordering, presentation failure with no mutation, and post-presentation open failure; all 80 focused upstream main-error/router cases pass. | `candidate` | add saved-IP statement retry execution, A6B4 recovery and gameplay re-entry joins, inherited B05D/pager/carrier/framebuffer state, physical YTNEWS recovery, PRINT/final-close partial commits, active-handler second faults, KEY suppression/FIFO state, and BA00/normal-END cleanup |
| Shared `YT-SUB` runtime-error dispatcher | `src/yt_main_error.c::yt_shared_error_compose`, `src/yt_presentation.c::{yt_present_local_line,yt_present_line}` | root `YT-SUB:45F7` and all 49 internal dispatch/call/resume/terminal transfers are candidate. The native owner pins ERR-24 retry precedence, the ordered ERL routes and 64003/64004/64005/64006 boundaries, exact SINGLE/INTEGER debug text, all direct diagnostics and ERR-53/64 refinements, the autopilot session/news row, and all three generic news rows with explicit logical destination classes. Native fixtures cover every route, positive/negative numeric spacing, snoop gating, ordinary session presentation and the generic-news sequence; all 90 focused upstream shared-handler/router cases pass. | `candidate` | add retry execution and normal-END transitions, inherited ANSI/cache/local/remote state, physical YTNEWS transactions including stale tails, active-handler second faults and every construction/output failure prefix |
| Cruise-missile and plasma-bolt resolvers | `src/yt_session.c::{launch_projectile,projectile_opening,route_failure_report,plasma_hop_report,plasma_footer,missile_sector,missile_planet_impact,plasma_sector,plasma_planet_impact,projectile_fighter_owner,deploy_victim_mines,session_present_text}`, `src/yt_game.c::{yt_projectile_plasma_opening_run,yt_projectile_plasma_route_run,yt_projectile_sector_probe_run,yt_projectile_plasma_fighter_run,yt_projectile_plasma_mine_run,yt_projectile_plasma_dispatch_run,yt_projectile_plasma_player_run,yt_projectile_plasma_killed_run,yt_projectile_plasma_planet_run,yt_player_stored_name,yt_planet_stored_name,yt_projectile_defense_row,yt_projectile_attack_first_rows,yt_projectile_destroyed_rows,yt_projectile_friendly_planet_row,yt_projectile_planet_attack_rows}` | the prelude now has an injected exact native owner for attribution, SINGLE-rounded energy, SINGLE hop loss, raw loading termination, both waits, fractional firing and every callback prefix. An injected route controller now also owns equal-endpoint origin/route-array mutation, shared route calls, first-versus-later hop loss, the OR footer gate, hop row/wait, black-hole mutation/RNG/output order and every callback prefix. The joined AD17 probe uses the first physical sector GET, four strict-positive field tests and ascending sector/cloak cache scan; zero presence returns immediately, while positive presence passes that already-loaded record into impact. Completed analysis contracts are mapped to native launch, routing/rerouting, defense, mine, player, planet, news, sound, wait, and terminal paths; release/debug builds and broad tests compile the owners. Opening/tracking/end rows, plasma hop rows, and the early same-sector self-destruct, unreachable-route, Union Police, black-hole blank, and plasma-footer terminals cross the presentation transducer. Same-sector plasma preserves the by-reference numeric-zero origin effect; cruise latch mutations update caller storage immediately across failure prefixes; defense owner GET conversion/bounds distinguish cruise from plasma. All defense-owner, first player-hit, destroyed/warning, friendly-planet and hostile-planet identity rows use explicit fixed-field byte lengths and binary-safe news/presentation paths; native fixtures pin embedded-NUL forms. The missile defense/mine/player core uses fresh persistence GET overlays, repeated mine probe/persistence GETs, post-PUT missile decrement, ignored friendship GET before candidate GET, post-GET cloak gates, eager scanner CINT, DOUBLE fighter damage, dirty defense/scanner zeroes, fresh survivor/death overlays and positive carried-mine re-entry. Missile planet impact uses CINT link admission, updater plus independent planet GET, physical friendship reads and planet FIELD restoration, numeric-zero ground skipping, the updater's stale ore predicate, fresh ground/productivity overlays, and independent destruction planet/sector writes with dirty `00 00 20 00` zeroes. Plasma defense/mine/player impact uses post-output fresh persistence overlays, dirty defense zeroes, PUT-before-HQ order, the mandatory B099 reload, news-before-direct mines, distinct entry/presentation/persistence player GETs, DOUBLE fighter damage, canonical killed-player clears and positive carried-mine B099 re-entry. The injected plasma planet transaction uses the updater-return cache rather than the stored post-update ore field, retains the binary launch attacker rather than reconstructing mutable session identity, consumes one continuous damage draw per iteration, reports before the fresh six-field/ground/owner raw overlay, uses independent canonical-zero destruction/unlink writes, suppresses ground reporting after destruction, and pins every callback failure prefix. | `candidate` | add raw REDIM/far-array/string/FIELD/runtime state, dependency-internal physical prefixes, complete caller compositions, corrupt numeric domains, endpoint/presentation variants and remaining shared-child joins |
| Info panel and captain resolver | `src/yt_game.c::{yt_info_panel_run,yt_info_team_resolver_run}`, `src/yt_session.c::{show_ship,info_refresh_time,info_team_lines,info_team_read_player,info_team_load_team,info_team_read_overlay,info_team_write_overlay,info_team_present,info_panel_refresh,info_panel_team,info_panel_read_player,info_panel_present}`, `src/yt_presentation.c::yt_present_refresh_time` | roots `YT-SUB2:0FD8` and `YT-SUB:856E`, all 49 outer-panel transfers, and all 33 resolver transfers are candidate. The outer transaction snapshots the counted cached name at admission, refreshes time first, preserves the separate resolver and fresh box-player reads, owns all 35 outer helper calls, formats the eight typed rows and three commodity branches, restores only foreground, and feeds ANSI-consumed bold state back between calls. Native fixtures pin its exact 38-event order, every outer provider-failure prefix, binary names, scanner/cloak alternatives, zero/nonzero commodity routes, 50-byte CP437 geometry, and the canonical 602-byte plain and 678-byte ANSI streams. The resolver fixture separately pins raw fractional team/captain operands, inclusive boundaries, zero/self/other/promotion routes, binary names, validation re-GET, fresh promotion overlay GET, `F77` mutation, PUT-before-output ordering, and every provider failure prefix. Complete planet joins now pin 718/642-byte self-captain panels with no captain GET, 715/639-byte other-captain panels with distinct validation/display GETs, and 808/732-byte due-refresh/promotion panels with four TIMER reads, fresh overlay, exact `F77`-only PUT, promotion rows and final player GET. | `candidate` | add raw MBF/FIELD/cache/string-descriptor/FLAGS/frame state, physical GET/PUT/output partials, loader-internal faults, other caller-specific joins, final framebuffer and physical adapter proof |
| Planet-production updater | `src/yt_planet_updater.c::{yt_planet_updater_run,yt_planet_updater_stage_name,yt_planet_updater_stage_site}`, `src/yt_session.c::{planet_update,planet_update_cached_physical,planet_updater_date,planet_updater_get,planet_updater_timer,planet_updater_put}`, `src/qb_format.c::{qb_mbf32_from_mbf64_raw,qb_mbf64_int_positive_raw}`, `src/yt_file.c::{yt_database_read,yt_database_random_get,yt_database_write,yt_database_random_put,yt_database_set_seek_provider,yt_database_set_read_provider,yt_database_set_write_provider,yt_database_set_close_provider,yt_database_set_flush_provider}`, `src/yt_file.h::{yt_database_get_result,yt_database_put_result}` | the live updater now owns the exact 20-effect DATE/opening-record-expression/GET/TIMER/fourteen-LSET/closing-record-expression/PUT sequence and feeds its raw result and caches directly into every production caller. The native fixture pins the representative 137-byte record, physical record 2001, unchanged non-FIELD bytes and tail, rounded CSNG, residue-preserving positive INT, constructor and repeated dirty zeroes, base-cancellation residue, strict commodity catchup, weapon-rate timing, elapsed clamps, logical missing GET, corrupt dirty-zero rejection, exact sites/order, and every failed dependency cut with its precise partial FIELD image and unchanged logical store. The production database seam additionally pins full/short/zero-fill GET, accepted-prefix read carry, the shipped flag-`0x02` 136-byte successful PUT, carry/error PUT prefix persistence, all-byte seek/read/write DOS classifications with exact ERR 52/57/70/75 and terminal cursors, flush failure, and rejected-short ERR-61 unregister/CLOSE state for both a successful close and a failed close retaining an orphaned handle. All eleven callers plus fifteen internal transfers are mapped, including joined pre- and post-transfer updater calls. | `candidate` | add raw BRUN heap/FIELD/status/descriptor-free projection, connect typed physical errors to the shared router, inject platform DATE/TIMER failures below the production adapters, and cover unreachable/corrupt arithmetic-runtime domains |
| Nearest-port layer scanner | `src/yt_session.c::{computer_nearest_ports,nearest_more,nearest_market_cell,project_port_market}` | ordinary BFS discovery order, descending ties, filters, DATE/GET/TIMER ordering, projected rows, private pager, final blank, one incoming call, and all 143 internal transfers are mapped. The formerly mislabeled `YT-SUB:0D23` report-helper root and all 18 of its qualified edges are candidate; all 34 focused command-14 body/cycle oracle tests pass. | `candidate` | add bounded deterministic native raw row/filter/price/pager/state/effect and failure-prefix fixtures; retain separate input-adapter proof for local/remote arbitration, corrupt raw-record conversions and physical endpoints |
| Ordinary-port updater and commerce report | `src/yt_session.c::{port_update,port_report,port_owner_row,port_report_length,port_report_right}` | all three updater/report caller pairs and 29 internal transfers are mapped; updater preserves DOUBLE projected stocks separately from SINGLE persistence, and report composes cached owner/treasury, fresh player, fresh port name, DATE/TIME, mixed line framing, typed numeric fields, and color rows | `candidate` | add bounded deterministic raw-record/arithmetic/report/effect and failure-prefix fixtures; exact input arbitration and physical adapter prefixes remain separate; fixed-price trade is its own child component |
| Shared port-owner/treasury row | `src/yt_session.c::{port_owner_row,port_owner_row_capture,session_present_text}`, `src/yt_game.c::{yt_port_owner_classify,yt_port_owner_compose}`, `src/qb.c::qb_brun_random_record_number` | all ten `YT:[A949,A9E1)` body/return transfers are mapped. The native owner preserves the `owner <= 1` silent precedence, current-owner cached DOUBLE treasury row, other-owner GET-before-output path, BRUN positive-fraction truncation and 24-bit record conversion, fixed-field binary name bytes, direct leading blank and direct line. Focused tests pin silent zero/one/negative/fractional values, self, ordinary/fractional/wrapped other-owner selection, embedded NUL composition and bounded failures. | `candidate` | add raw owner-name CINT mode/FIELD replacement, caller-specific cached/fresh joins, physical GET/output failure prefixes, endpoint modes and final local framebuffer |
| Port docking, commerce controller, and fixed-price commodity child | `src/yt_session.c::{command_trade,fresh_no_turn_gate,finalize_action,port_update,port_report,trade_commodity,earth_store,mutate_player_credits,reload_player,session_present_text,session_02fc,session_031f,session_0357,session_a8d2,session_02db,session_0317}`, `src/yt_game.c::{yt_credit_mutation_run,yt_port_link_missing,yt_port_trade_schedule,yt_trade_treasury_overlay,yt_trade_credit_overlay,yt_trade_holds_overlay,yt_trade_stock_overlay}` | the controller now emits `<Port>` before its foreground-three fresh-turn gate, calculates the selected port expression before the exact-zero-only no-port branch, composes the direct blank and no-newline `Docking, ` prelude into the shared finalizer, performs the redundant selected-port GET, branches on the post-finalizer sector, rereads the ordinary updater sector, schedules negative factors 1..3 before positive factors 1..3, tracks whether a nonzero-maximum prompt was reached, and emits the exact refusal and fresh DOUBLE cargo-status rows before main-command gameplay reentry. The commodity child independently reloads the player, computes the recovered direction/maximum and validation sequence, and persists accepted trades as optional treasury, shared fresh-credit transaction, three holds, then selected stock. Native presentation pins canonical plain/ANSI no-port, docking/turn, refusal/status partitions and a 249-byte accepted buy. The planet predecessor now joins its 77-byte prompt and 3-byte `P` editor directly to the exact-zero no-port controller and one friendly gameplay re-entry, pinning exact 223/179-byte ANSI/plain cycles with four partitions, 11 local rows, inherited-cache-sensitive ANSI, complete terminal state, no turn debit and no RNG; model tests pin negative links, order `1,3,2` for `(-60,74,-66)`, and unrelated-byte preservation in all four trade write reducers. | `candidate` | add authoritative raw-store/cache composition across finalizer, updater, report and every trade; pin sell/cancel/retry/zero-maximum branches, Earth and successful-commerce planet-menu caller returns, corrupt record coercions, terminal endpoints, physical database order and all partial-failure prefixes |
| Ship-computer activation/help/prompt/early-dispatch/filter front | `src/yt_session.c::{computer_menu,computer_activate,computer_help,computer_nearest_ports,reload_player,session_0317,session_02fc,session_031f,session_0357,session_02db,session_fixed_width}`, `src/yt_game.c::yt_computer_selector_position` | activation notice/sound, fresh-player prompt, blank/default/two-byte ordering, exact-comparison precedence, all inherited one/two-byte aliases, exact 17-row two-column help, invalid/reactivation/deactivation loops and command-14 visible filter front are represented. Native presentation pins the canonical 674-byte plain activation/help/reprompt and pager count 11; model tests pin selector positions 1 through 12 and zero misses. The planet position-five `C` route now transfers directly into this owner and pins the complete first-prompt join at 178 ANSI/music and 146 plain bytes. | `candidate` | add raw scanner scratch/argument residues, exact invalid/reactivation and all command-14 branch transcripts, Earth caller join, downstream return compositions, terminal/editor/carrier paths, final local framebuffer and every failure prefix |
| Shared ship-computer return/fresh-prompt seam and command-I join | `src/yt_session.c::{computer_menu,show_ship,computer_prompt_hydrate,reload_player,session_present_text,session_031f,session_0357}`, `src/yt_game.c::yt_info_panel_run` | every loop return performs the ordinary fresh player hydration, anti-cloak-gated cloak-cache refresh, shared scratch clear, inherited-color direct blank, foreground-one prompt and compatibility-uppercase/default/two-byte editor normalization in recovered order. Native presentation pins the isolated 52-byte ANSI and 42-byte plain prompt plus mode-one zero-serial and mode-two blank-only partitions. The exact active-prompt `I` caller now composes the shared 678/602-byte ANSI/plain Info panel and the same 52/42-byte fresh prompt into exact 785/689-byte cycles; `I;NEXT` produces exact 790/694-byte cycles while retaining `NEXT CR`. Both variants pin the zero-byte adapter, component boundaries, 20 completed local rows, open 40-byte prompt fragment, complete color tapes and final pager/style/source state. | `candidate` | add injected raw A41C/FIELD/cache/scratch and fault-prefix fixture, promotion/time-refresh joins, low-time/carrier/key/editor branches, complete framebuffer and corrupt endpoint/conversion states; reconcile the shared A41C owner across its other 52 callers separately |
| Ship-computer command-1 deactivation and friendly gameplay re-entry | `src/yt_session.c::{computer_menu,command_shell,sector_entry,display_sector,computer_prompt_hydrate,reload_player,session_0317,session_present_text}` | the exact one-byte selector emits the `0317` deactivation notice and returns with `enter_sector`, causing the main controller to jump into one sector-entry pass rather than reprompting inside the computer. Native presentation pins the canonical quiet-sector 165-byte ANSI and 145-byte plain cycles through the first main-editor poll; all 11 upstream command-1 cycle cases pass. | `candidate` | add database-backed raw FIELD/cache/scratch/RNG state, hostile joined stream, black-hole/mine typed handoffs, queue sampling, mode/local/carrier endpoints and every scanner/hazard/physical failure prefix |
| Shared current-sector scanner and ship-computer command-S joined cycle | `src/yt_session.c::{computer_menu,display_sector,display_sector_one,scanner_read_sector,scanner_read_port,scanner_write_port,scanner_read_planet,scanner_read_player,scanner_read_team_overlay,scanner_read_current_player,computer_prompt_hydrate,session_present_text,session_attention,session_sound,session_timed_wait,session_031f,session_0357}`, `src/yt_game.c::{yt_sector_candidate_eligible,yt_sector_cloak_revealed,yt_sector_sensor_targets,yt_sector_mine_warning_row,yt_sector_port_row,yt_sector_planet_row,yt_sector_player_row,yt_sector_fighter_row,yt_port_stored_name,yt_player_stored_name,yt_planet_stored_name}`, `src/yt_pager.c::{yt_sector_pager_begin,yt_sector_pager_add,yt_sector_pager_finish_sector}`, `src/qb.c::{qb_brun_random_record_number,qb_cint,qb_str_single,qb_str_double}` | The scanner now uses the shipped SINGLE record expressions through BRUN's 24-bit random-record conversion, emits exact binary-safe port/planet/player/deployed-fighter rows through the correct presentation helpers, performs the port row before its backlink-only PUT, reloads the sector at both documented boundaries, consumes one RNG draw only after self/cache admission, clears revealed cloak state in order, carries dynamic-string and last-target scratch, and performs a final player GET without overwriting that target. Mode one copies the six caller slots and preserves zero suppression, duplicates and a current-sector self target. Native fixtures pin every row and numeric type, all four fighter-owner forms, team overlay text, eligible/reveal thresholds, physical target order, strict private-pager threshold/reset, the 15-second wait model, the complete all-zero cycle at 211 ANSI and 135 plain bytes, and a complete nonzero planet caller composition whose 1,119/673-byte ANSI/plain scanner child includes duplicate/self targets, cloak reveal, port backlink, planet/player/fighter rows, disruption and mine attention, five selector-four sounds, two attention-owned selector-six sounds, the exact 15-second pause, terminal state and following prompt. All 105 body/return transfers are candidate. | `candidate` | add one injected whole-scanner raw FIELD/cache/RNG/effect fixture covering successful and staged failure prefixes, exact scan-mode/descriptor/frame residue, corrupt dynamic-array domains, remaining mode/local/carrier endpoints and physical partial-I/O proof |
| Ship-computer command-7 avoid-list body and joined cycle | `src/yt_session.c::{computer_menu,computer_avoid,computer_avoid_cell,reload_player,session_fixed_width,session_0317,session_031f,session_0357,session_036f,session_02fc,session_present_text}`, `src/yt_game.c::yt_computer_selector_position` | exact command-7 dispatch, both headings, all 30 slots in ten mixed-output rows, both numeric prompts, range-before-CINT selection, fractional sector mutation, locked/available ordering and fresh computer-prompt return are represented. Native presentation pins the canonical 744-byte all-zero blank-exit stream and final pager count one; shared QB tests cover numeric foundations. | `candidate` | add accepted fractional mutation/status combinations, authoritative raw MBF/scratch/FIELD joins, ANSI/mode/local results, every terminal/editor/carrier path and all failure prefixes |
| Ship-computer command-2/alias-23 port-report wrapper | `src/yt_session.c::{computer_menu,computer_port_report,computer_port_friendship,port_update,port_report,earth_store,reload_player,session_0345,session_031f,session_02db,session_0317,session_present_text}` | case-preserved prompt/editor, blank exit, VAL→INT→SINGLE retry, selected-sector GET, unconditional fresh friendship reads, entry-team deployed-fighter visibility, Earth dispatch and second-sector-read ordinary updater/report composition are represented. Native presentation pins the canonical 69-byte unavailable body; shared numeric and commerce fixtures cover foundations and children separately. | `candidate` | add complete ordinary/Earth joined native streams, authoritative FIELD/cache/raw friendship cells, physical persistence/order, terminal/endpoints and every failure prefix |
| Ship-computer command-10 path and command-3 autopilot | `src/yt_session.c::{computer_menu,computer_route,build_route,reload_player,queue_commands,session_0317,session_031f,session_036f,session_02db,session_02fc,session_a8d2,session_present_text}` | both entry modes, retained marker, destination validation, avoid-aware route/display, course and turn rows, raw confirmation, engagement notices, queue prefix and final warp refresh are represented. Native presentation pins the canonical 170-byte direct-path and 239-byte accepted-autopilot bodies; all 61 upstream path/autopilot/navigation-cycle oracle cases pass. | `candidate` | reproduce exact raw BFS FIFO/predecessor/avoid/FIELD behavior and remaining-warp processing, local `POS(0)` wrapping, raw marker/cache/scratch/queue joins, every alternate/endpoint/corrupt-workspace path and all partial-failure prefixes |
| Ship-computer command-4 scoreboard wrapper | `src/yt_session.c::{computer_menu,computer_scoreboard,computer_scoreboard_progress,display_game_file,session_031f,session_0357,session_present_text}`, `src/yt_score.c::{yt_score_generate_progress,yt_score_generate}` | exact whole-response selector, old/update split, blank/heading/four-dot/viewer ordering and generator phase callbacks are represented. Native presentation pins the canonical 131-byte updated-`NUL` body. A joined fixture feeds the exact 603-byte retained DOS-EOF scoreboard through a real `FILE *`, production dynamic `LINE INPUT` and the shared pager for old, default-update and non-O-update paths, pinning the complete 720/700, 753/733 and 754/734-byte ANSI/plain bodies, 26/27 local rows, colors, source state and cleanup. All 28 upstream scoreboard/file-cycle oracle cases pass. | `candidate` | add joined entry-command/generator-mutation/fresh-prompt state, raw cached-score/FIELD/queue joins, local-only/corrupt modes, pager/carrier endpoints, physical pathname/generator faults and every failure prefix |
| Radio target front and personal-recipient search | `src/yt_session.c::{radio_compose,radio_player_search,radio_name,reload_player,team_load,session_0317,session_031f,session_0345,session_a8d2,session_02db,session_02fc,session_present_text}`, `src/yt_game.c::{yt_fixed_text_contains,yt_radio_player_prompt,yt_player_stored_name}` | exact warm-up/target wrapper order, blank return, normalized whole-target branches, fresh team gate, ascending inclusive personal scan, raw exponent eligibility, case-sensitive binary-safe `INSTR` over all 41 fixed bytes, raw `LEFT$` confirmation including embedded NUL, explicit-N continuation, tuning/limit rows and body handoff are represented. Native model/presentation fixtures pin the fixed-string seam and canonical 94-byte blank-target body; all 60 upstream target/body/joined radio oracle cases pass. | `candidate` | add authoritative raw fixed-name/FIELD/team-slot/config-bound state and complete candidate/retry/not-found plus accepted ALL/TEAM/personal native streams, fractional/corrupt loop bounds and lengths, every mode/editor/carrier/failure prefix |
| Radio message body, menu, edit and send | `src/yt_session.c::{radio_compose,radio_line_prompt,radio_edit_draft,radio_append,append_news,session_0317,session_031f,session_0345,session_0357,session_036f,session_a8d2,session_02db,session_02fc,session_present_text}`, `src/yt_data.c::yt_radio_message_record` | numbered-line pager resets, menu/dispatch framing, Abort, numeric and quoted Edit flow, recipient-major ALL news/radio ordering, styled success and exact personal/broadcast radio-record construction are represented. Native presentation pins canonical 7-byte first-empty and 99-byte plain personal-send bodies; data tests pin the complete 86-byte personal record. | `candidate` | add exact custom-poll timing, wrap cleanup, backspace local `LOCATE`, raw draft/pager cells, complete Edit/List/Continue variants, physical writer FIELD/file prefixes, every endpoint/failure, and joined accepted-send cycles |
| Gameplay 86-byte radio-record writer | `src/yt_session.c::radio_append_bytes`, `src/yt_file.c::{yt_radio_file_open,yt_radio_file_next_record,yt_radio_file_get,yt_radio_file_put,yt_radio_file_close}`, `src/yt_data.c::{yt_radio_message_record,yt_radio_set_number,yt_radio_set_text}` | all six qualified `YT-SUB:[2561,260E)` transfers are mapped; exact close-before-open/update-or-create and 4/4/4/74 binding feed aligned `LOF/86+1`, the zero-filled destination GET, exact counter/header/text overlay, full PUT and close. Native fixtures pin partial-tail fractional-record rejection, case-insensitive shared-handle reopen, personal/broadcast images and the reachable 75-byte-to-74-byte boundary. | `candidate` | add raw CDBL/CSNG/file-block/descriptors/frames, every caller composition and staged seek/read/write/close/error-router failure prefix |
| Shared fresh-player credit mutation helper | `src/yt_session.c::{mutate_player_credits_observed,mutate_player_credits,apply_player_credit_mutation,create_planet,planet_bank,planet_productivity,trade_commodity,earth_receipt,lottery,xannor_victory,earth_anti_cloak}`, `src/yt_game.c::{yt_credit_mutation_run,yt_xannor_victory_run,yt_earth_anti_cloak_run}` | `YT-SUB:[4B67,4BAA)` is correctly identified as the shared credit helper. All eight shipped callers now enter one native owner. Its injected transaction pins the sole qualified return, fresh A41C player hydration and cache effects, exact SINGLE add then `INT_S`/floor, raw MBF32 argument/fresh/sum/result cells, canonical MBF zero, credits-only raw overlay, unrelated-byte preservation, player PUT, pre-GET argument failure, post-GET arithmetic failure, untouched read failure and dirty in-memory write-failure residue. Existing reducers retain the large/tie boundaries, while Anti-Cloak and Xannor retain their exact enclosing GET/PUT failure cuts. | `candidate` | add physical database read/write/flush and shared error-router prefix fixtures, remaining malformed MBF/runtime domains, raw BASIC frame residue and complete caller-level failure-prefix compositions |
| PORTNAME whole-program controller and generator | `src/main_portname.c::main`, `src/yt_portname.c`, `src/yt_file.c::{yt_database_open,yt_database_open_observed,yt_database_random_close,yt_database_read,yt_database_random_get}`, `src/yt_config.c::yt_config_load`, `src/yt_init.c::yt_generate_port_name` | entry root and roots `PORTNAME:02B5`, `03A0`, and `0442`, their 39 qualified transfers, and the 68-site presentation family are candidate. Native fixtures pin exact CR-only startup/missing/blank/abort/success bytes; BASIC one-string parsing and redo; first-byte destructive-uppercase confirmation; update-or-create, redundant close and zero-byte deletion paths; SINGLE loop/24-bit record conversion; output-before-GET prefixes; name/length-only raw overlays; exact 908-token draw behavior; close-before-logical-PLAY order; and unrelated-byte preservation. A newly reached missing final record now takes the shipped zero-byte successful GET, receives a zero-based name/length overlay, is PUT as a new full record, and reaches the ordinary success tail instead of aborting at host EOF. Its shared update-or-create OPEN pins exact access-denied fallback, create/temporary-close/reopen, error classification, IOCTL carry/device handling, handle ownership and native existing/missing paths. The shared regular-random CLOSE adapter pins no-I/O missing returns, clear-carry success, every first DOS error, mandatory ignored retry, cleanup-before-ERR 70/57 and orphaned-handle residue. Checked startup, accepted controller/helper and completion calls use it; the final failure fixture retains all completion bytes but does not reach logical PLAY. | `candidate` | add physical BRUN framebuffer/editor/Ctrl-C/default-fatal behavior, raw allocation/path/FIELD/file-number/status/descriptor/string state, public CLOSE_NO_ARGS registry/fixed-console state, every remaining console/END and generator-provider failure prefix, malformed numeric domains and remaining supported-platform adapter results |
| Shared YT/RMT initializer bounded-random and port-name helpers | `src/yt_init.c::{yt_initializer_bounded,yt_generate_port_name}` | roots `YT-INIT:004F`, `RMT-INIT:07F3`, `YT-INIT:23E4`, and `RMT-INIT:307E` plus all 34 qualified helper-body transfers are candidate. Injected native draws pin bounded lower/mid/maximum results, exact draw counts, invalid-bound no-draw behavior, the 908-token pool, two-to-four fragments and `Inging`/`Monkey Kangeroo Tractor Lightning` vectors under the authorized replacement-CSPRNG source. All 33 upstream initializer tests pass. | `candidate` | map every caller join and complete raw RND/DATA/READ/RESTORE/string/frame state, provider-error prefixes, repeated-call pool state and composed world/persistence effects |
| YT-INIT shared operator input helper | `src/yt_cli.c::yt_cli_line`, `src/yt_init.c::yt_initializer_confirm_response`, `src/main_yt_init.c::main` | root `YT-INIT:009C`, its sole return, both confirmation/scoreboard caller joins, and both `0A81/0A83` scoreboard-selection transfers are candidate. Native fixtures pin exact one-byte `Y/y` admission, empty/N/suffix rejection, decline before any `YTDATA.DAT` mutation, accepted blank `YTSCORE.ASC` default, nonempty pathname retention, and accepted-input EOF after the initializer truncation boundary. | `candidate` | add canonical BRUN B5 editor/echo/cursor/framebuffer, nonempty 79-byte cap, raw descriptor/argument state, EOF/Break/runtime cuts and physical console adapter |
| YT-INIT preparation and operator presentation | `src/main_yt_init.c::{main,console_present}`, `src/yt_init.c::{yt_initializer_layout_yt,yt_init_present_confirmation_prefix,yt_init_present_opening,yt_initialize_bind_yt,yt_initializer_prepare_yt,yt_init_present_prepared_configuration,yt_initialize_yt_prepared_bound,write_config_and_players,randomize_sector,build_graph,write_world_database,write_yt_auxiliary}` | native entry initializes the authorized replacement RNG provider and shipped 50-player/2,004-sector/1,000-port/100-planet layout before the exact confirmation prefix, mapping the three startup transitions at `0030`, `004C`, and `0099` without consuming entropy or prematurely applying later configuration values. The accepted native path then truncates and closes the database, reopens/binds it, performs both zero-filled direct record-1 GETs, retains that handle across distinct epoch and maintenance DATE samples and the one headquarters draw, displays the live defaults, and only then accepts the scoreboard pathname. A third fresh date at the port-table finalizer supplies production serial minus ten. A varying-date raw fixture pins all three values and the `087A`/`1A3F` caller joins. A recovered-LCG/fixed-clock fixture pins 31,297 draws/final `9F26F4`, 17 clock calls, the unchanged 432,235-byte image/FNV, 8,379 current presentation events/FNV `E72AA6DB06FEF26C`, 35 long-warp triples, 2,003 locate/verification triples, 1,000 port number/name pairs, comma-zone header dispositions, termination rows and logical `L64cgaL1p1p1p1`. Raw BRUN register flow pins `0B09` to FIELD descriptor `A546`, hence the four offset-53 bytes `00 00 4C 86` for MBF32 51. A `0ABB` cut leaves the bound database empty; a `0B1B` cut accepts `0ABB` plus that raw row, leaves exactly record 1 durable and consumes no post-headquarters draw. Upstream `1248add9` now agrees with the C presenter: the turns/day and lottery rows produce a 31-event accepted tape and 40-event combined tape with framebuffer hashes `a5ff53...` and `40123d...`. | `candidate` | add physical CRT/platform-loader and editor state, exact framebuffer/LOCATE adapter, remaining raw BRUN/FIELD/file-registry state, all output/file failure prefixes and physical RUN result |
| RMT-INIT handoff and standalone entry front | `src/main_rmt_init.c::{read_handoff,rmt_handoff_close,main}`, `src/yt_init.c::{yt_rmt_handoff_parse,yt_rmt_standalone_prompt_compose,yt_rmt_standalone_response_compose}`, `src/yt_file.c::{yt_database_open,yt_database_random_lof,yt_database_random_close}`, `src/yt_text.c::{yt_text_input_open,yt_text_input_read_line,yt_text_input_close}`, `src/yt_rmt_door.c` | all nine qualified entry/classification/confirmation/terminal transfers through `014D` plus both standalone/remote join transfers at `0165..0167` are candidate. The update-or-create random handle is measured live at `0066`: zero retains that same open handle through the standalone prompt and normal decline END cleanup, while nonzero performs the explicit `0123` close, sequential reopen, one `LINE INPUT`, and `014D` close before deletion. Pure and executable fixtures pin missing/empty random-open classification; CR/LF/DOS-EOF first-line extraction; the two leading blanks, banner, intervening blank, semicolon prompt and normal immediate-response paint; exact whole-string `Y`/`y` admission; blank/N/suffix rejection; accepted extra blank; normal decline END with retained empty handoff; deletion before accepted validation; standalone DORINFO/UART bypass with `The Sysop`; unchanged old database on decline; and nonempty COM1 handoffs through both missing-old and accepted reset/progress/completion supported POSIX/OpenDoors paths. | `candidate` | add canonical BRUN B5 raw key/editor/cursor/framebuffer, Break/EOF, embedded-NUL/string descriptors, CLOSE-all cleanup registry and every physical/failure prefix |
| RMT-INIT remote DORINFO input | `src/yt_init.c::{yt_rmt_dorinfo_parse,yt_rmt_dorinfo_field}`, `src/main_rmt_init.c::read_dorinfo_name` | the executable no longer uses host `fgets` semantics. A bounded eight-call parser discards NUL, retains bare LF, terminates on CR with optional LF consumption, leaves DOS EOF unread, accepts an unterminated final field, reports ERR 62 with the exact failed-field/cursor boundary, ignores line nine, and preserves the three discarded plus COM/framing/discarded/user-first/user-last assignment order. Pure fixtures cover all eight truncation cuts and capacity/argument failures; subprocess fixtures join embedded-NUL COM0 local status and a valid COM1 physical OpenDoors session through normalized alias lookup and the exact missing-old terminal bytes. | `candidate` | add raw BRUN file/device/string-descriptor/heap state, 256-plus-byte executable field domains, OPEN/CLOSE and per-read physical failure prefixes, plus non-missing-old remote composition |
| RMT-INIT DORINFO/UART reducer and supported-platform adapter | `src/yt_init.c::{yt_rmt_serial_state_compose,yt_rmt_serial_events_compose}`, `src/yt_startup_model.c::{yt_startup_divisor_from_observed_baud,yt_startup_framing_compose}`, `src/yt_platform.c::{yt_platform_rmt_serial_prepare,yt_platform_rmt_serial_restore,yt_platform_rmt_serial_close}`, `src/yt_rmt_door.c` | 37 mapped qualified transfers cover optional-colon/final-byte port selection; strict COM1..4 gating; COM1/3 versus COM2/4 BRUN aliases; COM3/4 BDA byte writes; exact `03F8-offset` register layout; pre-OPEN LCR/IER/DLM/DLL event order; `115200/divisor` sampling; destructive 7-bit framing selection; no-carry divisor reconstruction; local bypass; zero-divisor ERR 11 prefix; OPEN-error prefix; and all eight post-OPEN register events. Native POSIX pseudoterminal fixtures now execute on FreeBSD as well as Linux: they independently observe 38400, prepare 1200/E7 with byte-transparent output, attach OpenDoors using only published fields/calls, restore 38400/E7, and reapply that state after OpenDoors shutdown. The Win32 implementation owns and passes a preconfigured client handle through published `od_open_handle`. | `candidate` | add Win32 physical execution coverage, nonstandard/fractional divisor policy, raw DOS BDA/register effects, descriptor-corruption branches, shared runtime-error disposition and every staged OS/OpenDoors failure prefix |
| RMT-INIT remote status rows | `src/yt_init.c::{yt_rmt_serial_state_compose,yt_rmt_remote_status_compose}`, `src/main_rmt_init.c::main`, `src/yt_platform.c::yt_platform_rmt_serial_prepare` | the range predicate and two decoded-outcome jumps at `05B2`, `05B4`, and `05E8` are candidate. Pure fixtures join strict COM1..4 state to exact `Local Console Mode` or five-value `Opening COM port 2 at 2400 baud` CR rows. The executable now obtains remote baud from the physical terminal/handle observation, not the DORINFO framing line; the POSIX process fixture proves the local-only status does not enter the exact remote device stream. | `candidate` | add Win32 local framebuffer and physical failure prefixes, raw numeric/frame state, and nonstandard divisor outcomes |
| RMT-INIT remote identity and alias scan | `src/main_rmt_init.c::{read_dorinfo_name,credited_remote_name}`, `src/yt_init.c::yt_rmt_credited_name`, `src/yt_names.c::yt_names_load` | all six qualified normalizer/EOF/compare/back-edge transfers at `071F..07E2` are candidate. Injected fixtures pin trim/collapse/title normalization, complete ordered scanning, case-sensitive exact normalized-real-name comparison, last duplicate match wins, and empty credit with no real-name fallback. The executable status row precedes this scan. | `candidate` | add joined DORINFO/YTNAME process fixtures, raw descriptor/frame and per-row INPUT/EOF state, embedded NUL/CP437 domains, physical file failures/prefixes and the preceding UART composition |
| RMT-INIT reset/progress presenter, auxiliary files, dual output helpers and completion tail | `src/yt_init.c::{write_rmt_auxiliary,rmt_present,yt_rmt_output_compose_state,yt_rmt_output_apply,yt_rmt_completion_compose,yt_rmt_completion_delay}`, `src/yt_file.c::{yt_radio_file_open,yt_radio_file_next_record,yt_radio_file_put,yt_radio_file_close}`, `src/main_rmt_init.c::{write_rmt_output,write_rmt_presentation,write_rmt_completion}`, `src/yt_rmt_door.c` | roots `RMT-INIT:264D`, `268F`, and `269E`, all eight helper-body transfers and 75 reset/progress/completion transfers are candidate. A stateful width-80 reducer pins pre-value wrap, comma zones, explicit serial wrap, terminal-control handling and independent endpoint columns. The Genesis auxiliary transaction truncates, random-opens once, performs five live-handle LOF/86+1 selections and PUTs, then closes before the final blank; its executable fixture pins five identical 86-byte counter-50/all-sentinel records, 72-byte padded prophecy fields and two zero tail bytes. Recovered-LCG fixtures pin exact complete local/remote reset tapes, dynamic long links and repairs, 50-sector/15-port dot thresholds, four destructive presentation cuts, and the byte-pinned `FOR 1 TO 2222` completion delay with final value 2223. Physical POSIX fixtures join the exact reset prefix and completion tail through unmodified public OpenDoors. | `candidate` | add raw descriptor/float-stack/PRINT/FIELD/framebuffer state, typed radio PUT/CLOSE failures, a deterministic joined physical whole tape, DOS/Win32 reduction, carrier/termination results and remaining staged failures |
| RMT-INIT old-data validation and normalization | `src/main_rmt_init.c::{main,write_missing_old_data,write_rmt_completion}`, `src/yt_file.c::{yt_database_open,yt_database_random_lof,yt_database_random_close}`, `src/yt_init.c::{yt_rmt_preprocess_old_database,yt_rmt_normalize_config,yt_initialize_rmt_presented,rmt_present,yt_rmt_completion_delay}`, `src/yt_rmt_door.c` | root `RMT-INIT:27BF`, all 32 qualified body transfers, and its three entry binder/validator/rebuild joins are candidate. The `26D4` update-or-create open now owns one live file-1 handle across `27C2` LOF, the nonzero config GET and old-data preprocessing, followed by checked close; zero emits the missing-old row before closing and deleting. Native fixtures pin the LOF-zero decision before GET/config/RNG and the complete nonzero reset: old mutations, normalization, dynamic layout, fixed date, one HQ draw, database, ordered reset/progress tapes, four destructive presentation cuts and the bounded completion delay. Physical POSIX fixtures pin the missing-old terminal plus reset prefix and ordinary completion/cleanup tail with final terminal speed/framing. | `candidate` | add raw FIELD/file-number/signed-sector-cache and public CLOSE-all registry state, every remaining GET/PUT/output/delete/cleanup/runtime partial-failure prefix, deterministic physical middle and Win32 framebuffer/transport |
| YT/RMT initializer deterministic graph and database images | `src/yt_init.c::{yt_initialize_bind_yt,yt_initialize_yt_prepared_bound,reachable,build_graph,randomize_sector,sector_nonempty,pair_already_linked,assign_ports,make_config_record,yt_init_sector_prepass,allocate_world,write_world_database,yt_generate_port_name,yt_initialize_world}`, `src/yt_file.c::{yt_database_read,yt_database_random_get,yt_database_set_seek_provider,yt_database_set_read_provider}`, `src/yt_config.c::{yt_config_decode,yt_config_load}` | fixed-clock/recovered-24-bit-LCG fixtures reproduce the complete YT 31,297-draw/final-`9F26F4` 432,235-byte image and RMT images/tapes across three seeds. After accepted-path truncation, the YT binding reopens the 137-byte random file, performs the helper's direct record-1 zero-filled short-success GET plus the distinct following direct GET, and retains the handle across configuration presentation and scoreboard input. Its fixture pins empty 0/0-byte reads, a three-byte prefix with zero suffix on both reads, decoded all-zero helper state, open lifetime and missing-file failure. Before allocation, the YT sector prepass reproduces the distinct record-1 GET, SINGLE sector-base-plus-count calculation, field-57 MKS$/LSET, and PUT. Its fixture now pins both a patterned full record and an empty physical file: the latter takes the shipped zero-byte successful GET and creates the exact zero-based record containing only MBF32 `00 70 00 8C` at field 57. The shared adapter additionally pins all-byte seek/read ERR 52/57/70/75 results, accepted-prefix zero-fill and terminal cursors. The zeroed allocation then represents the inclusive six-lane clear for sectors 1 through 2,004. A scripted seven-sector YT fixture additionally pins local threshold/slot/direction gates, occupied-source and occupied-target preservation, successful and duplicate-pair local results, the long-warp self-target retry, occupied/empty long targets and whole-pass empty-sector retry. The RMT vectors include dynamic long links, explicit serial wrapping, repairs and bounded progress dots; the YT comparison exposed and corrected the prior port-offset-101 residue. Roots `YT-INIT:03C9`/`13D3`/`15EA`, `RMT-INIT:26D4`/`2DBE`/`3031`, all 54 helper-body transfers, the `0712` binding call, three YT sector-prepass/clear transfers, twelve YT main graph-controller transfers, eighteen local-warp transfers, twenty long-warp/pass-retry transfers, five shortcut-controller transfers, ten final-sector/port-assignment transfers, and thirteen port-record/planet-initialization transfers are candidate. The YT tape explicitly pins six fixed reciprocal slots, 2,003 verification triples, 35 long-warp triples, 21 no-path rows, 21 linked-result rows, eleven cumulative shortcut candidates, all 2,004 final sector records, collision-sensitive assignment and record generation for all 1,000 ports, the distinct Earth/generated-name paths, all 2,000 template-derived planet records, and the final persistence joins. | `candidate` | add exact raw FIELD/file-registry/CVS/loop/status/cursor cells, isolated BFS arrays/counters/base indices, allocation/provider/partial-write failure cuts, phase-specific native digests and raw shared-router joins |
| YTMAINT bounded and nested random helpers | `src/yt_maint.c::{yt_maintenance_random_integer,yt_maintenance_nested_integer}`, `src/yt_random.c::{yt_random_nested_single,yt_random_nested_integer}` | roots `YTMAINT:78F3` and `7939` plus all eight qualified helper-body transfers are candidate. Injected native draws pin one-based lower/mid/maximum results, exact one-draw and count-draw cardinality, repeated per-draw mutable result/range assignments, either-zero normal no-draw return, and every replacement-CSPRNG provider failure prefix. All 71 focused maintenance-AI/BRUN-RNG upstream tests pass. | `candidate` | connect both callers and add the pre-gate physical TIMER reseed/result boundary, raw argument/frame/loop/result cells and composed faction effects |
| YTMAINT maintenance point-to-point route builder | `src/yt_maint.c::{yt_maintenance_route_next_hop,yt_maintenance_route_cache_free}` | root `YTMAINT:34EB`, both Xannor/Mercenary caller joins and all 30 qualified body/helper/return transfers are candidate. A real 2,004-sector YTDATA fixture pins same-sector zero before cache allocation, first-use caching of all six physical warps for every sector, reuse after an on-disk graph mutation, physical-slot/FIFO equal-hop selection, zero-neighbor rejection, successful first hop, the live group-20 zero-target not-found lane, and exact failed-route YTNEWS bytes with CRLF and one DOS EOF. All nine native CTests and all 61 focused path/maintenance-AI oracle tests pass. | `candidate` | add raw 3,001-cell MBF32 predecessor plus aliased FIFO/route workspaces, unrelated same-sector residue, corrupt/fractional indices, dynamic-array/FIELD/cache/config/frame state, every staged physical failure prefix, and complete Xannor/Mercenary caller compositions |
| YTMAINT maintenance-route neighbor enqueue helper | `src/yt_maint.c::{yt_maintenance_route_next_hop,yt_maintenance_route_enqueue}` | root `YTMAINT:3953`, all six physical-slot caller joins and all six qualified body/return transfers are candidate. Native fixtures pin zero-neighbor and already-visited no-mutation returns, predecessor writes, FIFO append/order, distinct skip/add results and safe range/capacity rejection. The joined parent fixture now pins physical-slot order, the full cached BFS, success and failure composition. All 61 focused path/maintenance oracle tests pass. | `candidate` | add raw 0..3000 SINGLE/CINT workspace/base/index cells, corrupt indices/warps, allocation/runtime failures and per-record database GET prefixes |
| YTMAINT configuration defaults and headquarters persistence | `src/yt_maint.c::{yt_maintenance_default_headquarters,yt_maintenance_run,store_config_field}`, `src/yt_config.c::yt_config_normalize_maintenance` | all fourteen qualified `024B..02F2` branches are source-bound: numeric-zero headquarters becomes 85 and is written immediately before the later process-local defaults; empty scoreboard becomes `NUL`; local screen is constrained to inclusive `[-1,0]`; lottery values strictly below one become one without an upper bound; and maximum holds outside inclusive `[10,250]` becomes 250. Native boundary fixtures pin just-inside/exact/just-outside values and preservation of unrelated configuration state; an isolated record-1 fixture pins durable headquarters 85 while proving that the later process-local defaults have not leaked into the file at the next failure cut. | `candidate` | add raw MBF comparison/FLAGS and descriptor residue, the exact record-1 random-PUT physical accepted prefixes and failures, and composition with startup OPEN/FIELD/GET and the later maintenance phases |
| YTMAINT protected-sector mine pass | `src/yt_maint.c::{yt_maintenance_clear_protected_mines,yt_maintenance_run}` | both qualified `0421`/`0488` loop-control transfers are candidate. A native physical database fixture pins ascending logical sectors 1 through 7, the computed-record mapping, numeric zero at offset 129 only, preservation of every unrelated and unbound-tail byte, and no mutation of sector 8. The native owner retains one read/write pair per visit, and the upstream daily-maintenance oracle independently pins the same bounded state transform. | `candidate` | add raw loop/record-argument/FIELD/file registry and runtime state, exact per-iteration GET/PUT accepted prefixes and failures, and the joined transition into message/news compaction |
| YTMAINT unconditional maintenance-news header | `src/yt_maint.c::{yt_maintenance_write_header,yt_news_append,yt_maintenance_run}` | the qualified `04D8 -> 57D1` caller join is candidate. A scripted-clock native fixture proves two fresh samples in shipped TIME$-then-DATE$ order and pins the complete `22:47:29 08-23-2031: Maintenance Program Ran (Revision 03/14/94)` line, CRLF framing, single DOS EOF, and creation of a missing current paper. | `candidate` | add raw movable-string descriptors/temporaries, file-number and caller-frame residue, the physical append accepted prefixes and every OPEN/PRINT/CLOSE/runtime failure cut |
| YTMAINT entry gate, wrapper and compaction prefixes | `src/yt_maint.c::{yt_maintenance_same_day,yt_maintenance_compose_entry,yt_maintenance_compose_wrapper,yt_maintenance_compose_message_compaction,yt_maintenance_run}` | the bounded `YTMAINT:0030..0517` entry slice pins ordinary and same-day control flow, including the informational same-day fallthrough, the exact title, immutable empty `DS:7E60` operands, fixed revision, warning, player prefix, completion wrapper, and fixed compaction blank/heading. The production driver joins those rows around the protected-sector writes, compaction file work, and TIME$-then-DATE$ news header without a synthetic date or extra clock sample; a clean-install process fixture pins the resulting physical prefix. All 53 focused maintenance-entry/daily/output oracle tests pass. | `candidate` | add raw RANDOM/FIELD/file/frame state, complete physical screen reduction, raw shipped END cleanup and every partial-output/runtime failure endpoint; whole-driver deterministic shipped-scale evidence remains open within the candidate root |
| YTMAINT player aging and expiry/deletion presentation | `src/yt_maint.c::{yt_maintenance_player_name,yt_maintenance_age_player,yt_maintenance_compose_player_aging,yt_maintenance_maintain_players,maintain_players_impl,yt_maintenance_expire_player,yt_maintenance_remove_alias}` | all 23 qualified `052A..0798` player-loop transfers are candidate, including the already-owned cleanup and alias calls. Native reducers pin raw zero versus raw nonzero/CINT-zero name admission, negative-cloak normalization, exact SINGLE charge/clamp/write decisions, inclusive inactivity cutoff, nonzero killer gate, and expiry priority over deletion. A four-record physical fixture pins empty, ordinary, expiry and normalized-negative visits in ascending order; exact cache slots and raw cloak-only writes; the one news/direct row; fresh TIME$-then-DATE$ radio payload; and final-record exit. This corrected two native divergences: the stale fixed radio text and erroneous same-iteration deletion after cloak expiry. All 53 focused upstream entry/daily/output tests pass. | `candidate` | add authoritative raw FIELD/record-argument/frame state, exact stdout physical reduction, and every GET/PUT/news/radio/output/cleanup failure prefix |
| YTMAINT port maintenance and plague aggregate | `src/yt_maint.c::{yt_maintenance_compose_port_phase,yt_maintenance_update_port,yt_maintenance_maintain_ports,maintenance_write_port}` | all 34 qualified `07E5..0FA6` transfers are candidate. Native reducers pin the SINGLE elapsed-day expression and 10-day anomaly clamp, DOUBLE stock accumulation, strict production/plague/cap/maximum predicates, conditional RNG draw order, classification and factor signs, plus zero/nonzero aggregate output using `STR$_SINGLE`. A two-record physical fixture pins the unconditional heading, fresh date/TIMER sampling per record, ascending PUT order, RNG continuity, exact direct-screen-then-news aggregate, and raw mutation only at PORT offsets 41–84 and 101–104; an embedded NUL proves unrelated name bytes remain unchanged. | `candidate` | add dynamic-array descriptors, raw temporary/loop/frame/file-registry state, the shipped 1,000-record scale fixture, exact stdout physical reduction, and every staged clock/RNG/GET/PUT/output/news/runtime failure prefix |
| YTMAINT planet maintenance and disaster rows | `src/yt_maint.c::{yt_maintenance_compose_planet_phase,yt_maintenance_update_planet,yt_maintenance_maintain_planets,maintenance_write_planet}` | all 52 qualified `1007..1CE0` transfers are candidate. Native reducers pin the strict positive active marker, SINGLE production/inventory arithmetic with DOUBLE bank/plasma seams, unconditional three-draw event gate, plague-before-civil-war override, three reduction draws, optional two ground draws and civil-war expense draw, stock caps and optional row gates. A three-record physical fixture pins inactive preservation, fresh date/TIMER samples for active records only, shared RNG continuity, ascending PUTs, exact phase/event screen-before-news rows with SINGLE totals and floored ground values, binary-safe stored-length name output, and writes only to the documented PLANET maintenance lanes. | `candidate` | add dynamic-array descriptors, raw temporary/loop/frame/file-registry state, the shipped 100-record scale fixture, exact stdout physical reduction, and every staged clock/RNG/GET/PUT/output/news/runtime failure prefix |
| YTMAINT Wanderer relocation and rebuild | `src/yt_maint.c::{yt_maintenance_compose_wanderer_phase,yt_maintenance_maintain_wanderer,maintenance_write_wanderer_sector,maintenance_write_wanderer_rebuild,maintenance_write_wanderer_planet}` | all 14 qualified `1D1D..203A` transfers are candidate. Native existing/missing fixtures pin the ascending first-link scan, first-link-only clear, exact-zero candidate admission (including negative-link rejection), per-retry RNG order, success output before the first candidate GET, fresh missing-branch date sample, exact direct-screen-before-news rows, zero-bank-only refill, and the shipped scan/optional rebuild/candidate/final update physical ordering. Raw comparisons prove sector writes touch only offset 93, the rebuild preserves every documented unwritten planet field and tail, and the final planet update touches only owner and bank. | `candidate` | add raw temporary/frame/file-registry state, the shipped 2,004-sector scale fixture, exact stdout physical reduction, and every staged date/RNG/GET/PUT/output/news/runtime failure prefix |
| YTMAINT Xannoron home creation and daily update | `src/yt_maint.c::{yt_maintenance_compose_xannor_home,yt_maintenance_maintain_xannor_home,maintenance_write_xannor_rebuild,maintenance_write_xannor_sector,maintenance_write_xannor_daily}` | all nine qualified `20CE..23AC` transfers are candidate. The corrected native transaction probes the configured headquarters SECTOR first and rebuilds only for an exact zero planet link; a different positive link bypasses reconstruction. Raw fixtures pin the conditional date/TIMER and ground-then-bank draws, screen-before-news rows, PLANET rebuild PUT before the fresh headquarters GET/link PUT, unconditional fresh PLANET GET and daily draw, zero-bank-only refill, and mutation only of the documented rebuild, link, and daily-update lanes. | `candidate` | add raw temporary/frame/file-registry state, shipped-scale composition, exact stdout physical reduction, fresh DATE$/TIMER rollover cases, and every staged date/TIMER/RNG/GET/PUT/output/news/runtime failure prefix |
| YTMAINT Xannor top-player scan and hunt gate | `src/yt_maint.c::{yt_maintenance_compose_xannor_hunt,yt_maintenance_xannor_hunt}` | all 17 qualified `2451..261D` transfers are candidate. Native fixtures pin the unconditional processing/blank/locator rows, ascending records 2 through the configured player maximum, raw-nonzero active admission, strict-greater score replacement and lower-record tie retention, report-news placement after the scan, the no-player lane, unconditional first hunt draw for a selected top record, inclusive score/cloak eligibility, stored-length binary-safe name output, strict `RND > .25` cache selection, and no persistent player mutation. | `candidate` | add raw top-score/player/cache temporaries and frame/file-registry state, fractional/corrupt record/cache indices, exact stdout physical reduction, and every staged GET/RNG/output/news/runtime failure prefix |
| YTMAINT Xannor ordinary-target fallback | `src/yt_maint.c::{yt_maintenance_xannor_target,yt_maintenance_random_integer,maintain_xannor}` | all seven qualified `2648..26D1` transfers are candidate. The live pass validates the hunt target before its first group-slot GET. Inclusive sectors `8..sector_count` with a nonzero hunt-player token consume no draw; an out-of-range target or exact-zero token consumes one bounded draw over `sector_count-7`, adds seven, and clears the token. Native fixtures pin both valid endpoints, low/high draw endpoints, exact consumption and range failures. | `candidate` | add raw hunt/cache/config/frame state, fractional/corrupt SINGLE operands, injected provider failure, and complete group caller composition |
| YTMAINT Xannor group extraction and merge | `src/yt_maint.c::{yt_maintenance_xannor_groups_extract,maintain_xannor}` | all 25 qualified `276D..2A0A` transfers are candidate. The physical owner now follows the shipped three separate ascending passes: all 20 reserved location slots are loaded and raw-cleared first, Xannor-owned host defenses are then extracted with only fighter/owner lanes cleared, and later co-located groups finally fold into the earliest slot. A disk-backed raw fixture pins other-owner preservation, duplicate folding, unrelated-byte preservation, and the invalid-host failure prefix after all metadata PUTs. | `candidate` | add raw BASIC array descriptors/procedure/FIELD/file-registry state, noncanonical and fractional/corrupt locations, and every staged GET/PUT/CINT/runtime failure prefix |
| YTMAINT Xannor regeneration output | `src/yt_maint.c::{yt_maintenance_xannor_regeneration,yt_maintenance_compose_xannor_regeneration,maintain_xannor}` | all three qualified `2A2F..2A8C` transfers are candidate. The reducer preserves SINGLE division/`INT` for regeneration and ceiling, strict aggregate suppression, and DOUBLE addition back to group 1. The live transaction emits date, DOUBLE-formatted regeneration row, news, then date before applying group/location state. | `candidate` | add raw MBF temporaries/array descriptors, large/corrupt numeric domains, physical stdout reduction, and every staged output/news/runtime failure prefix |
| YTMAINT Xannor headquarters reclaim | `src/yt_maint.c::{yt_maintenance_xannor_headquarters_reclaim,yt_maintenance_compose_xannor_reclaim_attempt,yt_maintenance_compose_xannor_reclaim_result,xannor_reclaim_and_relocate}` | all 26 qualified `2B2D..2D4D` transfers are candidate. The physical transaction loads positive-owner raw player names or the nonpositive Mercenary label, emits attempt news before screen, preserves DOUBLE defenders and SINGLE group arithmetic with the strict 250 quantum and inclusive `.5` polarity, freshly reloads headquarters before raw overlay/PUT, then emits result screen before news. Success and failure disk fixtures pin binary-safe names, exact draw counts, persistence lanes and retained group location. The positive-owner player record now lives for the whole transaction, so the counted opponent view remains valid through attempt-row composition; the binary-name physical fixture passes under AddressSanitizer and UndefinedBehaviorSanitizer. | `candidate` | add no-attempt and large-quantum physical cases, raw temporaries/FIELD/file registry, malformed owners/names, and every staged GET/PUT/RNG/output/news/runtime failure prefix |
| YTMAINT Xannor headquarters relocation | `src/yt_maint.c::{yt_maintenance_xannor_headquarters_relocate,yt_maintenance_compose_xannor_relocation,xannor_reclaim_and_relocate}` | all 15 qualified `2D60..2F7F` transfers are candidate. The transaction pins both shipped trigger lanes and the no-op exit, bounded sector retries, the exact fighter/owner/planet rejection predicate, process-local headquarters/location update before persistence, fresh raw record-1 overlay/PUT before the old-sector GET, conditional old-link clear, fresh accepted-sector relink, relocation news before direct screen, and the trailing date row. A disk-backed fixture proves exact draw counts and bytes, unrelated-field preservation, and the non-rollback failure prefix where record 1 remains committed after the later old-sector GET fails. | `candidate` | add raw BASIC temporaries/FIELD/file-registry state, fractional/corrupt configuration operands, same-sector relocation, and every staged GET/PUT/RNG/output/news/runtime failure prefix |
| YTMAINT Xannor revenge slot | `src/yt_maint.c::{yt_maintenance_xannor_revenge_slot,yt_maintenance_compose_xannor_revenge,consume_revenge_slot}` | all five qualified `2FFA..307B` transfers are candidate. The transaction reads reserved sector-metadata slot 21, loads a positive selected player, admits only a live sector strictly above seven, keeps that live mode flag separate from the cached target, emits date then news/direct revenge then date, freshly reloads slot 21, and unconditionally clears offset 105 with the pinned dirty-zero bytes `00 00 28 00`. Raw success, low-sector and numeric-zero fixtures pin both branch arms, exact output/news and unrelated-byte preservation. | `candidate` | add raw BASIC temporaries/FIELD/file-registry state, fractional/corrupt slot/player/cache values, and every staged GET/PUT/output/news/runtime failure prefix |
| YTMAINT Xannor roaming split, discovery, target override and group row | `src/yt_maint.c::{yt_maintenance_compose_xannor_roaming,yt_maintenance_xannor_roaming_split,yt_maintenance_xannor_candidate_discovery,yt_maintenance_xannor_target_override,yt_maintenance_compose_xannor_group,maintain_xannor}` | 45 qualified split/discovery/override transfers at `313E..3471` and the distinct constant-two loop-entry edge are candidate. The corrected split emits the exact prowl/date rows, admits either size or location strictly below one, bypasses exact-one pairs, preserves the unrounded threshold, overwrites positive size through a zero location, and performs the four nested draws. The physical discovery owner pins rejected-current initial redraws, one ordinary versus 25 revenge attempts, candidate GETs, the fighter/owner/planet interest gate, cached-revenge replacement/early exit, unconditional ascending player cloak draws, strict cloak equality, live-revenge bypass, and the final `>7` replacement. The separate override pins groups 16..20/top-target precedence, unconditional group-20 override and final weak-group headquarters redirection. The live pre-route presentation uses shared SINGLE formatting for both numeric operands, preserves the group-number cursor transition from column 11 to 14, applies the QuickBASIC comma zone, and emits the exact group-2/size-1 row as 36 payload bytes followed by direct-screen CR. | `candidate` | add the full 2..20 caller-loop transaction, raw array/frame/MBF state, corrupt ranges/caches, every RNG/GET/output/runtime failure prefix, and joined route/arrival composition |
| YTMAINT Xannor route traversal, first-pass hunt-origin bypass, completion and protected-sector retarget | `src/yt_maint.c::{yt_maintenance_xannor_route_arrivals,yt_maintenance_xannor_bypass_initial_arrival,yt_maintenance_xannor_route_complete,yt_maintenance_xannor_should_retarget,yt_maintenance_compose_xannor_path_error,maintain_xannor}` | nineteen qualified route-return/exhaustion/bypass/back-edge transfers at `34E8`, `39FE..3A94` and `3CAA..3E0B` are candidate. The live controller keeps the selected target across successive one-hop route/sector/planet/player arrivals until exact SINGLE equality, then selects a fresh target only for a surviving group at a location strictly inside `(0,8)`, without repeating the split gate. The distinct first-pass gate compares exact group 20 and raw group location against the retained top-player target, sending that case through completion before the already-built next hop; this differs from the selected route target after the weak-group headquarters override. A disk-backed `1 -> 2 -> 3` fixture pins two ordered empty arrivals with unchanged records, cached graph reuse, a disconnected path-news continuation with preserved group state, and the after-route size/location-below-one clear including CINT-rounded location `0.6`. The disconnected lane now also composes and emits the exact address-owned `38A8` direct row after the identical `3898` news append, using `STR$_SINGLE` for both endpoints. Pure boundaries pin the hunt-origin conjunction, unequal/equal target values, and negative, zero, fractional-below-eight, exact-eight and nonpositive-size retarget inputs. | `candidate` | add a weak-group redirected-headquarters physical ordering fixture, nonempty combat and joined second-hunt-player paths, raw route/array/FLAGS/frame state, allocation failures, exact stdout reduction, and every staged route/arrival/output/runtime failure prefix |
| YTMAINT complete Xannor presentation family | `src/yt_maint.c::{yt_maintenance_compose_xannor_home,yt_maintenance_compose_xannor_hunt,yt_maintenance_compose_xannor_regeneration,yt_maintenance_compose_xannor_reclaim_attempt,yt_maintenance_compose_xannor_reclaim_result,yt_maintenance_compose_xannor_relocation,yt_maintenance_compose_xannor_revenge,yt_maintenance_compose_xannor_roaming,yt_maintenance_compose_xannor_group,yt_maintenance_compose_xannor_path_error,yt_maintenance_xannor_player_line,yt_maintenance_xannor_planet_arrival,yt_maintenance_xannor_defense,maintain_xannor}` | All 152 catalogued sites across `2069..3F73` and the `5811` player, `5EFD` planet and `71AF` sector arrivals have native address-owned composition and connected phase owners. Component and disk fixtures pin literal/dynamic rows, numeric formatting and cursor state, news/radio/death order, persistence and RNG boundaries; the last missing direct copy at `38A8` is now exact. All 79 focused upstream AI/Xannor/output tests and all nine native suites pass. | `candidate` | add a complete groups-2..20 joined transaction, authoritative raw runtime/FIELD/file-registry state, exact stdout/local framebuffer reduction and every staged physical/runtime failure prefix |
| YTMAINT Xannor post-planet gate and ordinary player scan | `src/yt_maint.c::{yt_maintenance_xannor_post_planet_exhausted,yt_maintenance_xannor_player_scan_admit,yt_maintenance_xannor_player_scan_continue,xannor_attack_players,yt_maintenance_xannor_route_arrivals}` | all thirteen qualified `3B5B..3C8D` transfers are candidate. The live route owner always invokes planet arrival after sector arrival, then clears and bypasses players only when size is below one or location is exact zero. Every configured player record consumes one cloak draw in ascending order, including different-sector records and group 20; the physical player-arrival call admits only same-sector ordinary groups when `cached_cloak - .330000013 <= RND`, with equality admitted. A two-hop/two-player fixture pins four draws with no player GETs, while pure boundaries pin negative-location survival and both sides of the cloak threshold. | `candidate` | add joined admitted-player and nonempty planet-to-player transactions, raw loop/cache/MBF/FLAGS/frame state, provider failures, and every staged arrival/RNG/runtime failure prefix |
| YTMAINT Xannor retained hunt-player and group-loop suffix | `src/yt_maint.c::{yt_maintenance_xannor_should_attack_hunt_player,yt_maintenance_xannor_advance_group,maintain_xannor}` | all four `3CBA..3CD6` second-attack gates and all three `3E2B..3E38` group-loop/persistence transitions are candidate. Boundary fixtures prove that any nonzero hunt-player token, including negative, combines only with exact group 20; zero tokens and group 19 bypass the second call. The group owner advances 2 to 3 and 19 to 20, while 20 becomes terminal 21 and enters final persistence. | `candidate` | add joined second-player-arrival and no-call physical transactions, a complete 2..20 controller-order fixture, raw MBF loop/token/FLAGS/frame state, and every partial failure prefix |
| YTMAINT Xannor group-20 terminal increment | `src/yt_maint.c::{yt_maintenance_xannor_group_twenty_finish,maintain_xannor}` | all five qualified group-20/positive-size/positive-location gates and bypass at `3D2B..3D6A` are candidate. The disk-backed owner proves that group 19, zero size, and zero location perform no physical access; a surviving group 20 freshly reads its CINT-selected sector, adds exactly one to fighters, writes it before final group persistence, and preserves owner, planet link and unrelated bytes. | `candidate` | add fractional/corrupt location and staged GET/PUT failures, raw FIELD/FLAGS/frame state, and a joined final-target fixture including the optional retained-hunt-player attack |
| YTMAINT Xannor final group persistence | `src/yt_maint.c::yt_maintenance_xannor_groups_persist` | all seven qualified persistence gates/back-edges at `3EA1..3F70` are candidate. A 20-slot disk-backed fixture pins metadata PUT before each optional host access, metadata-only zero locations and sizes, CINT-rounded fractional location, cumulative same-sector fighter addition, forced owner `-1`, planet-link and unrelated-byte preservation, ascending loop order, and the non-rollback prefix in which the current metadata PUT survives a failing host range check. | `candidate` | add raw BASIC array/loop/FLAGS/frame/FIELD/file-registry state, malformed MBF operands, every staged GET/PUT/runtime failure prefix, and the joined roaming-controller transition into persistence |
| YTMAINT Mercenary tax, opening, base/funding order and report rows | `src/yt_maint.c::{yt_maintenance_collect_mercenary_tax,yt_maintenance_compose_mercenary_phase,yt_maintenance_maintain_mercenary_base,yt_maintenance_place_mercenary_fleets,maintain_factions}` | all 36 qualified tax/base/funding controller transfers at `3FBA..4643` are candidate. The runtime emits date/blank before the ascending port scan; numeric-zero treasury skips PUT; every nonzero treasury performs two SINGLE `INT_S(treasury / 10)` evaluations; and the pool remains SINGLE. The misspelled tax row is screen-first then news, followed by date/phase/blank, report news and base-check. A missing base consumes one fresh date sample, writes the rebuilt PLANET before candidate RNG/GET, admits a nonpositive planet link, writes that SECTOR, then performs the unconditional fresh PLANET GET and owner/ground/bank floor PUT; an existing link skips date/rebuild/RNG. Funding selects sectors 2..maximum, retries positive fighter counts, makes exactly ten successful fighter/owner overlays, then emits the hired row screen-first/news. Raw tax, base and funding fixtures pin dirty-zero canonicalization/preservation, unrelated bytes, one occupied retry in each random loop, exact draw counts, zero-strength no-draw, and the ten-row source-addressed output stream. | `candidate` | add physical operation/write-count instrumentation, negative/subnormal/corrupt MBF domains, shipped-scale loops, raw temporaries/FIELD/file-registry state, exact stdout reduction and every staged clock/GET/PUT/RNG/output/news/runtime failure prefix |
| YTMAINT Mercenary player-defense defection | `src/yt_maint.c::yt_maintenance_mercenary_defections` | upstream `55f47117` reconciles the oracle with `46A1..46D2`: every positive defense consumes one draw before owner `>1` is ANDed with the strict damage predicate. The native raw transaction pins five positive-defense draws, including owners `-2` and `1`, plus sector PUT → owner-player GET → binary-safe direct row → identical news order and SINGLE formatting. All ten qualified first-entry, positive/skip, conjunction, news and ascending loop transfers at `4661..47C6` are candidate. | `candidate` | add raw temporaries/FIELD/file-registry state and staged RNG/GET/PUT/output/news failure prefixes |
| YTMAINT Mercenary movement and route controller | `src/yt_maint.c::{move_mercenaries,move_mercenaries_impl,yt_maintenance_move_mercenaries,yt_maintenance_mercenary_stays,yt_maintenance_compose_mercenary_movement,yt_maintenance_route_next_hop}` | upstream `0fe41956` resolves the former one-hop conflict: the retained path workspace maps each predecessor to its successor, and `4932` repeatedly replaces the live cursor until a zero successor. Empty arrivals, friendly merges, and surviving attacks fetch another successor; depletion, planet absorption, and player joins terminate; a positive force at zero successor is installed in the originally selected target. Native raw-disk fixtures pin a hostile linked intermediate, silent friendly merge, surviving target attack and terminal install, plus a copied lower intermediate that re-enters the descending sweep and later depletes against Xannor. They retain exact eager hold/target/selector/combat draw order, output/news bytes, unrelated record tails, and every newly unblocked `495A..49D1`, `4EE7/4F5F/5315`, and loop-suffix transfer is candidate. | `candidate` | add authoritative raw 3,001-element MBF workspace/CINT/FOR/FIELD/frame state, disconnected-route physical install, fractional/corrupt domains, operation-count instrumentation and every staged GET/PUT/RNG/output failure prefix |
| YTMAINT Mercenary destination attack selector and transaction | `src/yt_maint.c::{mercenary_arrival,mercenary_destination_impl,yt_maintenance_mercenary_destination,yt_maintenance_mercenary_attacks}` | upstream `1248add9` reconciles `4FB1..4FD6`: every non-merge destination consumes one selector draw before negative-owner is ORed with strict `RND > stored .95`. Raw-disk fixtures pin silent Mercenary merge, binary-safe player join, exact screen/news/radio order, fresh overlays, Xannor Won and Lost exchanges, strict combat draw arm, the 201-on-201 150-unit quantum followed by one-unit iterations, and the corrected repeated-route joins after merge and surviving attack. Exact selector-before-combat ordering, carried survivor state, terminal player join/depletion and all destination result bytes are represented. | `candidate` | add initial raw gate/frame state, malformed owners/names, fractional strengths, exact physical operation counts and every staged GET/PUT/RNG/output/news/radio failure prefix |
| YTMAINT Mercenary routed-arrival mines | `src/yt_maint.c::{yt_maintenance_mercenary_mines,mercenary_mine_line,mercenary_arrival}` | all 20 qualified `4A43..4C65` transfers are candidate. A raw disk fixture pins no-mine bypass, the one-iteration/two-nested-draw mine pass, fresh arrival-sector reload before a mine-only overlay/PUT, unrelated-byte preservation, surviving and killed fleets, and the fractional clamp-to-zero path with no mutation or rows. Hit and killed/loss reports append to news before direct screen output, preserve the shipped `mercenariers` spelling, and produce the exact CRLF/DOS-EOF image. | `candidate` | add a complete movement/route/arrival transaction, raw loop/MBF/FIELD/file-registry state, corrupt numeric domains, and every staged GET/PUT/RNG/output/news/runtime failure prefix |
| YTMAINT Mercenary linked-planet absorption | `src/yt_maint.c::{yt_maintenance_mercenary_planet_absorption,mercenary_planet_line,mercenary_arrival}` | all 13 qualified `4C8D..4EC0` transfers are candidate. A raw disk fixture pins the owner-`-2`/zero plus `CINT(link)` gate, PLANET GET and pinned `00 00 80 00` fighter-reserve PUT before a fresh SECTOR GET and fighter/owner PUT, one DOUBLE sum converted to SINGLE, unchanged planet owner, binary-safe stored-length names, and unrelated-byte preservation. It covers intermediate-hop capture and same-target suppression plus positive/zero reserve branches; capture is screen-before-news while the taking row is news-before-screen, with exact DOUBLE/SINGLE formatting. | `candidate` | add a complete movement/route/arrival transaction, raw MBF/FIELD/file-registry state, fractional/corrupt link/name/fighter domains, and every staged GET/PUT/output/news/runtime failure prefix |
| YTMAINT Super Lottery controller and common marker suffix | `src/yt_maint.c::{yt_maintenance_super_lottery,yt_maintenance_store_final_marker,super_lottery,store_final_marker,yt_maintenance_run}` | all 15 qualified gate/selector/writer/common-suffix transfers at `538B..5780` and the `57C6` body return are candidate; the already-mapped scoreboard call remains separately owned. One raw physical fixture covers success and the four no-winner exits with exact 12/2/3/4/1 draw prefixes, three one-based selections, raw-nonzero/CINT player-name length, binary-safe winner name, a second fresh PLANET GET, exact eight-draw SINGLE construction, dirty `00 00 3B 00` stock/missile zeroes versus canonical mine zero, preservation of plasma/fighters/unrelated bytes, PLANET PUT before fresh SECTOR GET/link PUT, exact screen/news/personal-radio bytes, and recipient/sender headers. A separate stale-cache fixture proves the common suffix freshly reads configuration record 1 and overlays only offset 81 before PUT. After the scoreboard returns, the controller closes the database before returning to the exact retained-date and completion rows. The authorized platform-CSPRNG departure removes physical `RANDOMIZE TIMER` effects while retaining each reached selection/construction draw. | `candidate` | add raw CLOSE-all/END, reseed/string/FIELD/file-registry/process state, corrupt numeric domains and every staged RNG/GET/PUT/output/news/radio/clock/scoreboard/stdout failure prefix |
| YTMAINT message compaction and newspaper rotation | `src/yt_maint.c::{yt_maintenance_run,yt_radio_compact,yt_news_rotate}`, `src/yt_file.c::{yt_radio_file_open,yt_radio_file_size,yt_radio_file_get,yt_radio_file_close}` | root `YTMAINT:672D`, its caller join and all seven qualified helper/body/return transfers are candidate. Production now takes the `67D3` live-handle typed LOF once, derives the complete-record bound, and performs numbered GETs on that same handle. A native binary fixture pins decoded numeric-zero omission, ordered nonzero retention, raw 84-byte record copying with two zero tail bytes, incomplete-tail omission, successful TEMP replacement, and whole-current-file news rotation over the old file. The clean-install process test physically pins the immutable blank and `Compressing Message Base's` row before file work; exhaustive shared LOF tests pin cursor and failure behavior. | `candidate` | add raw RANDOM/FIELD/file-number state, missing-file variants, and every remaining OPEN/CLOSE/GET/PUT/KILL/NAME/runtime accepted-prefix and failure endpoint |
| YTMAINT immediate combat-death cleanup | `src/yt_maint.c::{xannor_attack_players,yt_maintenance_immediate_death,remove_from_teams}` | root `YTMAINT:6924`, its Xannor-combat caller join and all 20 qualified body/return transfers are candidate. A real native YTDATA fixture pins cache clear, killer `-1`, sector/ground/team clear, owned-port owner/treasury release, deployed-defense transfer to owner `-2` with count retained, duplicate roster removal, unrelated port/defense preservation, and deliberate retention of the active marker, name, ship resources, credits, cloak, score, and owned planet. All 77 focused maintenance-daily/AI oracle tests pass. | `candidate` | add raw FIELD/cache/config/loop/MBF/frame state, zero/fractional/corrupt team/record/killer inputs, every staged GET/PUT/runtime failure prefix, and the full Xannor combat/news/radio/output caller composition |
| YTMAINT final expired-player cleanup | `src/yt_maint.c::{maintain_players,yt_maintenance_expire_player,remove_from_teams,invalidate_radio}`, `src/yt_file.c::{yt_radio_file_open,yt_radio_file_size,yt_radio_file_get,yt_radio_file_put,yt_radio_file_close}` | root `YTMAINT:6C0D`, its caller join and all 32 qualified body/helper/return transfers are candidate. Radio invalidation now retains the preliminary record-1 GET, observes the `6EF3` typed live-handle LOF once, derives the complete-record bound, and performs all numbered GET/conditional PUT operations on that handle. A real native YTDATA/YTRMSG/YTNAME fixture pins cache and lottery clears, all matching roster slots, owned-planet release, name/team clear with stale player bytes retained, owned-defense count/owner clear, exact recipient-or-sender radio invalidation with unrelated records unchanged, dangling killer repair to `-98`, port nonmutation, and the separate alias successor. It also exposed and corrected an `a+b` append-mode loop that had appended each invalidated radio record instead of updating it in place. | `candidate` | add raw FIELD/cache/config/loop/MBF/frame state, zero/fractional/corrupt team and record bounds, remaining staged GET/PUT/CLOSE/OPEN/runtime failure prefixes, and the caller's deletion-news plus physical alias composition |
| YTMAINT expired-player alias compactor | `src/yt_maint.c::{maintain_players,yt_maintenance_remove_alias}` | root `YTMAINT:7039`, its expired-player caller join and all six qualified body/return transfers are candidate. The native byte fixtures pin the stable pre-clear player-name handoff, exact case-sensitive comparison of YTNAME fields three/four, removal of every duplicate match, retention and order of real-name-only/case-mismatched rows, one DOS EOF, and successful TEMPWORK consumption by delete/rename. All 45 focused YTNAME/maintenance-daily oracle tests pass. | `candidate` | add raw INPUT#/EOF/parser/movable-string/file-number state, malformed/incomplete/quoted-row execution, and every physical CLOSE/OPEN/PRINT#/KILL/NAME accepted-prefix, failure and error-routing endpoint |
| YTMAINT Xannor player arrival | `src/yt_maint.c::{xannor_attack_players,yt_maintenance_xannor_player_arrival,yt_maintenance_xannor_player_combat,yt_maintenance_xannor_player_line}` | root `YTMAINT:5811`, both incoming caller joins and all 70 qualified body/helper/return transfers are candidate. The live player scan delegates each co-located active ship to a directly testable transaction. Injected reducers pin fighter-phase `RND > .5` versus shield-phase `RND >= .5`, quantum and draw counts, killed-player and Xannor-lost result rows. A real YTDATA fixture pins fighter radio before fighter PUT, shield radio before shield PUT, cache hydration, Xannor death cleanup, exact newspaper/direct result row, final kill radio, three ordered raw YTRMSG records, and persisted/cached killed state. All nine native suites and all 77 focused maintenance AI/output/physical-owner tests pass. | `candidate` | add raw MBF56 accumulators, procedure-static/frame/FIELD/file-registry state, survivor and large-quantum full transactions, every staged physical/runtime failure prefix, and complete groups-2..20 caller composition |
| YTMAINT Xannor planet arrival | `src/yt_maint.c::{maintain_xannor,xannor_attack_planet,yt_maintenance_xannor_planet_arrival,yt_news_append}` | root `YTMAINT:5EFD`, its caller join and all 57 qualified body/helper/return transfers are candidate. The live arrival now delegates to a directly testable disk-backed transaction. Native fixtures pin the Xannor-owner zero-draw skip, SINGLE group-size prefix, ground-before-economic draw order, shipped group-location large-quantum anomaly, owner/group/location/link/active-marker clears, production stock caps, both terminal rows, and news-before-direct-screen ordering. All nine native suites and all 77 focused maintenance AI/output/physical-owner tests pass. | `candidate` | add raw FIELD/frame/file-registry state, inactive and malformed/corrupt numeric domains, every staged physical/runtime failure prefix, and complete groups-2..20 caller composition |
| YTMAINT Xannor sector arrival | `src/yt_maint.c::{maintain_xannor,xannor_mines_and_defense,yt_maintenance_xannor_defense,xannor_attack_planet,xannor_attack_players}` | root `YTMAINT:71AF`, its caller join and all 63 qualified body/helper/return transfers are candidate. The live path now delegates deployed-defense combat to a directly testable reducer. Injected native fixtures pin unchanged owner `-1` and owner zero with no RNG draw, nonpositive defender/group gates, provider failure before mutation, one-draw defense destruction clearing both count and owner, and one-draw Xannor destruction preserving the surviving defense. The corrected upstream owner-zero gate from commit `ab8e6588` is authoritative; all 61 focused path/maintenance-AI oracle tests and the focused native maintenance suites pass. | `candidate` | add exact sector-mine/news composition, planet and player combat, authoritative raw FIELD/cache/RNG state, every staged physical/runtime failure prefix, and the complete Xannor caller output/persistence composition |
| YTMAINT news and maintenance-radio writers | `src/yt_maint.c::{yt_news_append,yt_radio_append_maintenance}` | roots `YTMAINT:57D1`, `7155`, and `7D0B` plus all seven qualified helper-body transfers are candidate. Native file fixtures pin repeated CRLF news appends with one DOS EOF and ordered personal/broadcast 86-byte radio images with counters 1/20, exact MBF32 headers, 72-byte LSET clipping/padding and zero tails. All 21 focused YTNEWS/YTRMSG oracle tests pass. | `candidate` | connect every caller and reproduce raw PRINT#/CLOSE-5/RANDOM/FIELD/LOF/file-number state plus every staged OPEN/PRINT/PUT/CLOSE failure prefix |
| YTMAINT scoreboard regenerator and readback | `src/yt_score.c::{yt_score_generate,yt_score_generate_progress}`, `src/yt_score_format.c`, `src/yt_maint.c::yt_maintenance_scoreboard` | root `YTMAINT:7DB8`, its Super Lottery caller join and all 73 qualified body/return transfers are candidate. Deterministic native fixtures pin empty/Xannor and all-zero failure prefixes, exact `NUL` to retained `yttemp`, complete CRLF/EOF bulletin grammar, alive and dead player scoring, deployed-defense addition, durable score-cache writes, player/team reorder and names, and exact 19/23-row close–reopen logical readback. All 45 focused scoreboard/lottery/output oracle tests pass. | `candidate` | add raw static-array descriptors/FIELD/file registry/process residue, corrupt numeric/team/owner inputs, every staged GET/PUT/OPEN/PRINT/CLOSE/LINE INPUT failure prefix, and joined Super Lottery suffix/CLOSE-all/END composition |
| Ship-computer command-5/alias-59 joined radio-composer cycle | `src/yt_session.c::{computer_menu,radio_compose,radio_player_search,radio_line_prompt,radio_edit_draft,radio_append,append_news,reload_player,team_load,session_0317,session_031f,session_0345,session_0357,session_036f,session_a8d2,session_02db,session_02fc,session_present_text}`, `src/yt_data.c::yt_radio_message_record` | the canonical blank target now composes the active computer prompt/echo, command dispatch, exact target front, ordinary return hydration and fresh prompt into one native 181-byte stream. Separate native fixtures retain the 7/99-byte body and exact 86-byte record seams; all 60 upstream joined/target/body oracle cases pass. | `candidate` | add joined personal/ALL/TEAM sends, authoritative FIELD/team/draft/queue/pager state, custom-poll/wrap/backspace/Edit variants, physical writer prefixes, every terminal/editor/carrier endpoint and all partial-failure prefixes |
| Ship-computer command-6 radio-log cycle | `src/yt_session.c::{computer_menu,radio_read,radio_name_bytes,reload_player,session_031f}`, `src/yt_data.c::{yt_radio_reader_decide,yt_radio_reader_header,yt_radio_reader_mutate}`, `src/yt_pager.c::{yt_radio_pager_begin,yt_radio_pager_add_pair,yt_radio_pager_add_body}` | canonical mode-one call/return and the complete 134-byte plain empty-log command/fresh-prompt cycle are pinned natively; reader tests separately cover visible bodies, binary headers, nonmutation decisions and private pagination. | `candidate` | add authoritative physical radio/name GET/FIELD/CLOSE state, message-bearing joined cycle, queue/typeahead, all endpoints/wait faults/shared-handler prefixes and final local framebuffer |
| Ship-computer command-8 newspaper wrapper | `src/yt_session.c::{computer_menu,computer_newspaper,display_game_file,reload_player,session_031f,session_0357,session_0317,session_present_text,append_news}` | one leading blank, exact whole-response T/Y retry, lowercase current/uppercase yesterday paths and shared viewer/controller handoff are represented. Native presentation pins the canonical 90-byte successfully empty current-news body. The joined retained-file fixture feeds the exact 434-byte current and 309-byte yesterday DOS-EOF images through a real `FILE *`, the production dynamic `LINE INPUT` parser and shared pager, pinning the complete 635/523-byte current and 418/398-byte yesterday ANSI/plain bodies, 15/13 local rows, logical colors, source state and cleanup. All 31 upstream newspaper/file-cycle cases pass. | `candidate` | add joined entry-command and fresh-prompt native streams, raw FIELD/cache state, pagination/Ctrl-X, corrupt/local-only modes, physical pathname/recovery faults and every endpoint/failure prefix |
| Ship-computer command-9 planet report | `src/yt_session.c::{computer_menu,computer_planet_report,computer_port_friendship,planet_inventory,planet_update_cached,fresh_no_turn_gate,finalize_action,reload_player,session_0317,session_031f,session_036f,session_02db,session_02fc,session_present_text,session_right_aligned}` | fresh strict-positive turn gate, exact no-newline prompt and numeric editor, floor-to-SINGLE selection, excessive-value styled retry, valid-link friendship/limited/unavailable/full decisions, action-finalizer ordering, updater live P/Q/A cache and exact typed 49-byte inventory rows are represented. Native presentation pins the ANSI no-turn/range/limited paths and canonical 625-byte full body; all 82 upstream body/cycle/input/selection cases pass. | `candidate` | add authoritative raw world/FIELD/cache and invalid-link stale-current-planet composition, complete native 710-byte command/fresh-prompt cycle, all endpoint/editor/carrier/runtime variants, physical updater/finalizer order and every failure prefix |
| Ship-computer command-11/13 owned-asset finders and joined cycles | `src/yt_game.c::yt_owned_planets_run`, `src/yt_session.c::{computer_menu,computer_owned_fighters,computer_owned_planets,owned_planets_read_sector,owned_planets_read_planet,owned_planets_present,reload_player,session_set_color,session_031f,session_02fc,session_fixed_width,session_present_text}` | command 11 uses the exact opening blank, unterminated `Searching;`, first-match direct blanks, fixed-width heading/rows, ascending scan, post-row exact-Q gate and trailing blank. Command 13 now has one directly injected transaction pinning its foreground-2 three-line prelude, foreground-3 ascending scan, raw-nonzero link gate, SINGLE base addition, BRUN 24-bit conversion including fractional/wrapped-negative/record-zero cases, exact owner match, duplicate raw 41-byte rows, no-match blink row, and every sector/planet GET and output failure prefix. Native presentation also pins the canonical plain 170-byte command-11 and 227-byte command-13 cycles through the fresh prompt; all 15 focused finder oracle tests pass. | `candidate` | add ANSI/NONE/pager-Q command-11 cycles, authoritative raw FIELD/scanner scratch and command-13 ES/direction/file/frame state, all endpoint/carrier/editor paths and every physical accepted-prefix/runtime failure |
| Owned-port treasury body and command cycles | `src/yt_session.c::{command_collect,computer_menu,command_shell,sector_entry,session_fixed_width_bytes,session_present_text}`, `src/yt_data.c::yt_record_set_raw_number` | the shared body now uses direct presentation throughout, tests the raw treasury exponent, binds the raw 41-byte name field through `CINT` and fixed width, clears collected treasuries with shipped dirty zero `00 00 20 00`, and preserves the fresh-player settlement tail. Main `$` now takes the mandatory mode-zero sector-entry join. Native presentation pins the canonical 348-byte command-12 report, 339-byte hidden-`!` collection, 362-byte main-`$` scan/fresh-prompt cycle and ANSI no-owned branch; all 64 upstream body/computer/main-cycle cases pass. | `candidate` | add injected raw database/cache and exact MBF56 accumulation/settlement/scanner fixtures, ANSI joined cycles, corrupt numeric/name boundaries, all endpoint/local results and every physical partial-failure prefix |
| Ship-computer command-15 active-spy body and joined cycle | `src/yt_session.c::{computer_menu,computer_spies,session_02db,session_02fc,session_present_text}` | zero spies now takes the styled `02DB` notice; nonzero counts take one direct blank and ascending bold `02FC` rows with SINGLE counter and signed-INTEGER target formatting. Native presentation pins the canonical 199-byte ANSI and 155-byte plain two-spy cycles plus the 152-byte ANSI zero-spy cycle through the fresh prompt; all 28 upstream body/cycle cases pass. | `candidate` | add count-one/three, ANSI-disabled zero-style residue, queue/key/carrier/mode/local endpoint and post-hire joined fixtures, authoritative cache/scratch/FIELD state and every failure prefix |
| Ship-computer commands 16/17 profit-report bodies and joined cycles | `src/yt_session.c::{computer_menu,computer_profit,profit_project_port_market,project_port_market,profit_emit_row,profit_more,computer_prompt_hydrate,reload_player,session_present_text,session_031f,session_0357}` | both bodies now execute the date helper immediately before each port's `TIMER` projection; command 17 retains the fractional cached source-sector value for its SINGLE row field while using the truncated positive record for the sector read. The ordinary adjacent cycle pins its exact title, two 36-byte rows and colors at 312 ANSI/226 plain bytes. The global cycle pins three encounter-order rows, raw CP437 two-column separators, end banner and fresh prompt at 340/228 bytes. All 44 upstream body/cycle cases pass. | `candidate` | add authoritative raw entry/return A41C FIELD/cache and physical record expressions, complete date/TIMER tapes, zero/no-port/no-result/corrupt-class paths, 44-result pager, mode/local/carrier endpoints and every physical/error prefix |
| Main command-F sector fighter controller and joined cycle | `src/yt_session.c::{command_shell,command_fighters,reload_player,session_02db,session_02fc,session_031f,session_036f,session_sound}` | the title precedes fresh player hydration; Union/foreign-force gates, DOUBLE available row, exact desired-count editor and VAL→INT→SINGLE path, rounded remaining gate, fresh sector-before-player overlays and success/sound suffix now follow the recovered body. Native presentation pins canonical 246-byte ANSI and 214-byte plain lowercase/trailing-command cycles through the fresh prompt; all 29 upstream body/cycle cases pass. | `candidate` | add exact refusal/cancel/zero/insufficient native streams, injected independent raw record/cache/FIELD and partial-write state, all mode/editor/carrier/local endpoints and every failure prefix |
| Input/session runtime | `src/yt_input.c`, `src/yt_input_model.c`, `src/yt_pager.c`, helpers in `src/yt_session.c` | bounded origin-aware B05D/AB36/wait arbitration with retained source tags, per-source FIFO, chronological merged FIFO, exact paired-local/serial pending-input drain and residue replacement, distinct case-preserving `0345`, compatibility-uppercase `0357`, and contains-uppercase-E-clearing `036F` owners, exact `/R` VAL/INT/cap/nonpositive/504-byte-overshoot transformations, semicolon/notice-sampled queue ordering, and exact successful 94FD observation order/state in `tests/test_input_model.c`; AB36 resets editor state before sampling its inactivity deadline, pins both strict-greater-than deadline boundaries and exact-mode-one-only inactivity suppression, samples carrier in every mode while terminating only for exact mode-zero loss, and consumes an inherited queued byte while bypassing all live local/serial polling and replacement. Exact one-byte live Ctrl-R commits the typed prefix/paged-text and newline flag before its B05D child, retains those effects on child failure, and only after success restores the saved command and forces CR. Exact CR clears newline state before the direct blank-line child and returns the accumulator only after that child succeeds. Only `08h` erases a nonempty accumulator; `7Fh` remains in the accepted printable range. Erase mutates the accumulator before the snoop-gated local `1D 20 1D` and exact-mode-zero remote `08 20 08` effects, retaining the truncation on output failure. The inclusive `20h..7Fh` printable lane emits local then remote echo before appending, copies the selected byte to paged-text, sets newline one, and then takes the post-printable carrier sample; echo failure preserves old editor state while carrier failure retains all new state. Its bounded terminal transaction emits the exact inactivity or session-limit notice, closes the live game database, then marks the session ended; direct carrier loss likewise closes before termination, and callback-cut tests preserve pre-END state. Exact editor endpoint, complete successful pager transaction, gate/nested-count/color, post-login main-prompt entry, save/repeat auxiliary notice, press-any-key presentation, carrier row and plain/ANSI terminal vectors are in `tests/test_presentation.c` | `candidate` | cover wrapper raw frames/string faults and all callers, notice-sampled Ctrl-X/printable plus joined carrier-in-notice termination state, raw MSR/helper-frame state, complete native file registry/CLOSE/END and OpenDoors cleanup, physical queue overflow and partial-I/O results, input/wait fault/retry/raw-runtime effects, interruption and SysOp consequences; the pinned OpenDoors one-row personality is an available local `ON KEY` adapter on supported console personalities but is not yet connected |
| Main YT startup registration and immutable prefix | `src/yt_startup_model.c::{yt_startup_main_prefix_begin,yt_startup_main_prefix_finish}`, `src/yt_session.c::yt_session_run`, `src/yt_door.c`, `src/yt_game.c::yt_startup_configuration_run` | The retained prefix pins the ordered enabled F4/F5/F8/F9/F10 selector-to-handler bindings, main `B2DA` error-handler installation before serial setup, CR/LF and both three-byte erase cells, all ten display labels, the eight-byte registration signature, two separately ordered real-name component canonicalizations, and the exact `0409` continuation. `main_yt.c` composes the pre-serial phase before `yt_door_start`, finishes it after serial setup, and the live session retains the completed state before configuration. The configuration transaction independently records `45F7` before its first provider operation and every injected provider-failure prefix retains it. | `candidate` | physical local function-key delivery is explicitly deferred; add raw BRUN loader/event-slot/handler-stack/string-heap/frame residue, joined OpenDoors startup failures, and exact CRT/process boundary state |
| Shared title and registration presentation | `src/yt_startup_model.c::yt_registration_run`, `src/qb_format.c::{qb_mbf64_add_raw,qb_mbf64_mul_raw,qb_mbf64_div_raw,qb_mbf64_sqrt_raw,qb_mbf64_floor_positive_raw}`, `src/yt_text.c::yt_text_stream_line_input_next`, `src/yt_session.c::{registration,session_centered_line_bytes,session_wait}`, `src/yt_presentation.c::{yt_present_centered_line,yt_present_forced_local_line,yt_present_local_beep}` | root `YT-SUB:A46C`, all 32 previously missing internal transfers, its caller and return are candidate. The injected transaction pins create/LOF/close, empty delete or sequential reopen and exactly three byte-oriented lines; NUL discard, CR/optional-LF, retained bare LF, pending DOS EOF, canonical binary names and VAL overflow timing; every shipped raw MBF sum/product/root/key; registered/evaluation construction; invalid four-BEEP/forced-local/CLOSE ALL/END order; beta centered-row/two-BEEP/END; anti-tamper CLOSE ALL/nonreturn; and every fallible callback prefix. The physical stream fixture additionally pins capacity failure after record consumption and repeated EOF without exposing bytes beyond `1Ah`. Portable limb arithmetic matches 500 randomized exact upstream add/multiply/divide/two-pass-SQR samples. The caller retains its eleven title rows, common four-row suffix and 2/10-second waits at startup and main-V. | `candidate` | add unrestricted BRUN dynamic-string heap/CPU/frame/file-block residue, arbitrary inherited framebuffer, DOS device variants and physical partial-fault joins |
| Startup opening spine and shared row-25 status | `src/yt_session.c::{opening_and_date,opening_poll_local,opening_poll_remote,opening_wait,post_login,build_route,display_game_file}`, `src/yt_output.c::yt_out_opening_file`, `src/yt_text.c::{yt_opening_stream_run,yt_file_viewer_stream_run}`, `src/yt_pager.c::yt_paged_row_run`, `src/yt_presentation.c::yt_present_status_row` | `YT:03A2` performs the sector-1-to-2 route prime/diagnostic, optional ANSI path, exact snoop-gated empty-alias local row-25 sequence, and mandatory `YTOPEN.ASC` viewer in recovered order; `YT:0739` reuses the same local-only status owner after both repairs with the cached nonempty alias. The ANSI path owns a live 67-row physical stream with EOF-before-read, local-key priority, post-serial pending test, true-EOF-only wait, endpoint-specific reset and ordered close. The physical ASCII startup component now consumes the exact 505-byte generated asset as 17 DOS-EOF-terminated rows, pins 18 B05D samples, all 21 local rows and color events, exact 544-byte ANSI/524-byte plain streams, mode-one/local-only and corrupt-mode-two endpoints, early body Ctrl-X state, cleanup and retained caches. | `candidate` | add complete joined ANSI-plus-ASCII startup and post-login framebuffer compositions, missing-handler behavior, raw runtime frames and physical OpenDoors/OS partial faults |
| BBS/serial startup logical state and direct-screen output | `src/yt_startup_model.c`, `src/yt_presentation.c::{yt_present_serial_startup_missing_command,yt_present_serial_startup_status,yt_present_carrier_drop}`, `src/yt_session.c::session_carrier` | root `YT-SUB:52F2`, 56 logical internal transfers and its presentation family are candidate. Native fixtures pin shared-handler installation before command inspection; the first-space-retaining command split and exact empty-command rows/END intent; exact twelve-read BRUN grammar with discarded NUL, retained bare LF, CR/optional-LF, unread DOS EOF, all twelve ERR-62 truncation boundaries, 256-byte fields and ignored line 13; optional-colon/final-byte port selection; COM1–4 BRUN/UART/BDA layout; all nonzero divisor arithmetic including `FFFFh`; destructive in-place compatibility uppercase and zero-divisor nonmutation; OPEN specification; post-OPEN divisor recovery; two-sample deadline cap; canonical name; final clear mask and pre-ANSI zero-divisor state; all direct local rows; and an observation-driven ordered tape for BDA writes, UART sampling/restoration, serial OPEN success/error, local-sound store, wait result, one carrier sample, ordinary return, or snoop-gated carrier row/CLOSE/END with no late divisor restoration. All 73 focused upstream startup/uppercase/handler tests pass. | `candidate` | add complete raw heap/file-registry/framebuffer effects, physical UART/COM/CLOSE/END accepted prefixes and every runtime failure composition; the post-OPEN wait remains explicitly deferred |
| Startup pre-admission and lockout | `src/yt_session.c::{startup_pre_admission,lockout,session_0317,session_02fc,session_wait}`, `src/yt_file.c::{yt_database_open,yt_database_random_lof,yt_database_random_close}`, `src/yt_startup_model.c::{yt_startup_lockout_run,yt_startup_lockout_scan}`, `src/yt_text.c::{yt_text_input_open,yt_text_input_read_line,yt_text_input_close}`, `src/yt_config.c::yt_current_date_serial` | root `YT-SUB:9B4D` and all 15 qualified transfers are candidate. Foreground-five `Initializing...`, silent date calculation, lockout, then non-denied `Welcome <real first>!` and unframed `Searching my records for your name.` execute in recovered order. Production now performs the shipped case-resolved lowercase random update-or-create, typed live-handle current/end/restore LOF, empty close or mixed-case sequential reopen, dynamic exact `LINE INPUT`, first canonical identity match, denial rows and ten-second wait with the stream live, CLOSE-all and END. The injected transaction pins exact paths, empty/no-match/first-match tapes, row payloads, wait duration, state and every fallible callback prefix; exhaustive LOF fixtures pin every DOS byte/cursor cut, and native stream fixtures pin NUL/CR/optional-LF/bare-LF/DOS-EOF/unterminated grammar. Existing presentation fixtures pin the 83-byte ANSI continuation, 140-byte ANSI denial, 106-byte plain denial and pager/nonstop state. | `candidate` | add modes 1/2, carrier/input cuts, inherited framebuffer, raw BRUN random/sequential file-block/read-ahead/string-heap/frame state, operating-system faults and physical CLOSE-all/END cleanup |
| Startup new-alias gate | `src/yt_session.c::{resolve_alias,session_0317,session_031f,session_0345,session_0357,session_02db}`, `src/yt_names.c::{yt_names_prepare_alias,yt_names_find_real_last,yt_names_alias_exists,yt_names_append}`, `src/qb.c::qb_title_case_n` | silent last-match existing mapping remains intact; a new alias uses the exact three framed notices, low-time raw prompts, case-preserving editor, transform/predicate ordering, bold identity row, whole-response exact-Y gate, append-before-success ordering and bold/blink success framing; `test_new_alias_success_presentation` pins the complete 308-byte ANSI stream/final state and `test_alias_key_preparation` pins default, comma/quote, normalized-empty, reserved-before-clip, one-word trailing-space and 40-byte one-word behavior; all internal alias predicate edges are mapped | `candidate` | add exact rejection/retry and first-poll terminal fixtures, all seven carrier cuts, raw accumulator/file/state/effect and endpoint/framebuffer proof, and physical append failures |
| Returning-player admission and prior-death handling | `src/yt_session.c::{admit_player,construct_player_visible,session_wait}`, `src/yt_game.c::{yt_game_construct_player,yt_player_construct,yt_player_name_matches,yt_player_stored_name,yt_player_killer_row}`, `src/yt_maint.c::yt_news_append_login` | returning-name selection is separate from vacancy selection and uses the fixed 41-byte field with positive clamp and negative-`CINT` error behavior; the foreground-two blank precedes same-day notice, daily PUT, canonical login-news append and death classification. Alive returns directly, ordinary deaths rebuild after visible constructor rows and wait five seconds, and same-day self-death terminates after denial. The resolved referenced-killer rule tests raw length first: raw zero skips, while raw nonzero `.4` reaches `CINT(0)` and emits the bold+blink suffix-only ` destroyed your ship!` row. Presentation tests pin the 37/104/152-byte streams and exact suffix-only ANSI row; score tests prove padded/oversized/fractional extraction, the raw-zero distinction, fresh constructor defaults, preserved fields/team clear, and exact login-news DOS-EOF image. | `candidate` | add remaining death rows, joined player-PUT/news state and ordered failures, terminal close-all, all modes and inherited framebuffer |
| Vacant/full new-player admission | `src/yt_session.c::{admit_player,construct_player_visible,session_0317,session_02db,session_02fc,session_0345,display_game_file}`, `src/yt_game.c::{yt_game_construct_player,yt_player_construct,yt_game_set_player_identity}`, `src/yt_maint.c::{yt_news_append_new_player,yt_news_append_game_full}`, `src/yt_input_model.c::yt_input_yes_no_candidate`, `src/qb_format.c::qb_str_single` | a returning miss begins a fresh ascending raw-`name_length < 1` scan; exhaustion emits rejection before canonical full-news/terminal disposition, while a vacancy emits retention/constructor rows, performs fresh-config construction then identity GET/PUT, appends canonical new-player news and offers instructions. Score tests pin both mutations, ordinary failure stages and exact 63/54-byte news files; input tests pin original accumulator versus derived output source; presentation pins exact 217-byte default, 279-byte invalid-X/N and 97-byte full streams. Accepted Y now consumes the physical 24,736-byte `YTINSTR.DOC` through the production viewer at saved foreground five: all 542 rows, 23 prompts, 566 B05D samples, 569 local rows, exact 26,733-byte ANSI and 25,977-byte plain streams, cleanup and restored caller colors are pinned. | `candidate` | add fractional scan/malformed records, joined two-PUT/news failure state and physical append prefixes, preceding admission/question-to-viewer composition, pager/carrier/terminal cuts, corrupt endpoint mode and inherited framebuffer |
| Normal post-login and gameplay re-entry | `src/yt_game.c::{yt_game_post_login_repairs,yt_sector_is_black_hole,yt_sector_mines_admitted,yt_sector_force_route,yt_sector_force_same_team}`, `src/yt_session.c::{post_login,show_ship,radio_read,display_sector,sector_entry,sector_force_friendly,same_team,command_shell,read_keyboard_line}`, `src/yt_input_model.c::yt_input_ab36_remote_replace` | the native path now performs two fresh repair GETs with independently flushed optional PUTs, then local nonempty-alias status, Info, caller blank, exact 17-byte press B05D, 99-second wait, post-wait blank, exact automatic mode-zero radio reader, scanner and router. Radio fixtures pin its compiled visibility, inclusive EOF probe ownership, direct streams and counter transforms. Pure route fixtures pin the ordered two-cell black-hole predicate, strict-positive mine/exact-zero-suppression gate, numeric-zero-only fighter emptiness, negative self-owned/nonself distinctions, positive integral owner conversion, and matching-nonzero-team rule. All thirteen qualified friendship-gate/warning transfers at `095E..09C4` are candidate, including the compiled OR at `0971` and its hostile fallthrough. The caller emits the exact 65-byte hostile warning before the hostile menu, while friendly entry uses fresh hydration, foreground-two blank, cached time, low-time B05D, editor reset/deadline, and queued-byte/serial replacement. | `candidate` | add one deterministic full Info/radio/scanner remote/local/RNG/persistence join, physical radio transaction/failure prefixes, typed black-hole/mine continuations, database-backed owner GET failures, carrier cuts and raw frames |
| Black-hole entry and shared emergency-warp child | `src/yt_session.c::{sector_entry,emergency_warp,reload_player,session_timed_wait,session_present_text,session_attention,session_sound}`, `src/yt_game.c::{yt_emergency_warp_duration,yt_emergency_warp_destination,yt_emergency_warp_cost,yt_emergency_warp_player_overlay,yt_emergency_warp_result_row,yt_emergency_warp_stranded_row}` | the black-hole caller now owns its direct blank, attention and synthetic queue clear before the shared child. The child emits the exact wormhole/temperature gauge, consumes duration draws after the gauge open, starts the SINGLE counter at one, applies the strict heat threshold and one-through-31 tick bound, waits after every tick, then emits two blanks before its fresh player GET and destination/HQ/jitter draws. Normal and meltdown rows, selector topology and foreground/blink transitions follow the shipped order. Only sector and turns are overlaid on the fresh raw player image; the process sector cache changes only after the PUT and flush succeed, and the helper itself does not clear caller typeahead. Native presentation pins the 377/517-byte normal and 390/992-byte meltdown streams; reducers pin arithmetic and unrelated-byte preservation; all 20 focused upstream tests pass. Root `YT-SUB:9FC4`, all 53 body transfers and four black-hole caller/join edges are candidate. | `candidate` | add one complete injected provider/FIELD/cache/wait/input/local/sound transaction, exact HQ and partial-failure cuts, corrupt numeric domains, TIMER/raw runtime state and every physical endpoint/failure prefix |
| Bounded gameplay hazard continuation | `src/yt_session.c::{sector_entry,display_sector,reload_player,mine_encounter,emergency_warp,common_fatal_self}`, `src/yt_game.c::{yt_sector_is_black_hole,yt_sector_mines_admitted,yt_emergency_warp_player_overlay,yt_sector_mine_player_overlay}` | one native router loop owns the scanner and admission exactly once, invokes either the black-hole or anonymous-mine child, composes the mine's optional emergency warp, and then loops into one following scanner without replaying an admitted handoff. The returned destroyed flag alone selects common death after a mine or mine warp. Child pager, queue, cache, RNG and raw storage state remain in the session object across each zero-effect return edge. The exact black-hole and mine fixtures plus all 16 focused upstream bounded-composition tests cover the three canonical 489/649, 234/324 and 583/799-byte plain/ANSI lanes; the zero-site presentation family is candidate. | `candidate` | add one deterministic full joined raw FIELD/cache/RNG/news/wait/local transcript, lazy independent following-sector snapshots, recursive second hazards, terminal/error adapters and every dependency/physical failure prefix |
| Ordinary movement parent and active-main M cycle | `src/yt_session.c::{command_shell,command_move,sector_entry,display_sector,fresh_no_turn_gate,dangerous_destination,finalize_action,reload_player,session_0317,session_031f,session_036f,session_02db,session_a8d2,session_present_text}`, `src/yt_game.c::{yt_movement_warp_row,yt_movement_confirmation_prompt,yt_movement_player_overlay}` | the movement parent uses the six cached warp cells in physical order, emits the exact `Warps lead to` row and destination prompt, repeats only on whole transformed response `M`, applies the exact bound/same-sector/adjacency gates, and joins the danger scanner and A8D2 confirmation before finalization. A successful hop freshly reloads the player, overlays only raw sector bytes 57..60, persists before changing the process cache, and reports movement to each caller. The main dispatch enters sector reentry only for an actual hop; direct refusal/cancellation results return to the single loop-top prompt hydration. Native fixtures pin warp-row spelling, prompts/refusals, confirmation, raw overlay preservation and the canonical 89-byte safe parent. The planet predecessor now joins its 77-byte prompt and 3-byte `M` editor directly to that accepted parent and a 95/75-byte gameplay re-entry, pinning exact 264/244-byte ANSI/plain cycles with four partitions, 14 local rows, complete color/terminal state and the oracle's one finalizer draw. All 24 isolated-parent and 15 active-cycle upstream cases pass; all 44 qualified parent transfers and both presentation families are candidate. | `candidate` | add an exact native 205/235-byte active cycle, complete injected independent shell/gate/danger/finalizer/router/FIELD/cache/RNG/queue state, every typed hazard/hostile/terminal join, Earth/computer predecessor cycles, local framebuffer and every failure prefix |
| Direct player-Attack parent and active-main A cycle | `src/yt_session.c::{command_shell,command_attack_player,attack_player,combat_attrition,fighter_shield_spill,sector_entry,kill_player,salvage_player,mine_encounter,emergency_warp,common_fatal_self,reload_player,session_0317,session_031f,session_036f,session_02db,session_02fc,session_a8d2,session_sound,radio_append_bytes}`, `src/yt_game.c::{yt_direct_attack_team_row,yt_direct_attack_candidate_prompt,yt_direct_attack_commitment_prompt,yt_direct_attack_too_many_row,yt_direct_attack_result_rows,yt_direct_attack_radio_text,yt_direct_attack_fighter_overlay,yt_direct_attack_shield_overlay,yt_fighter_shield_spill_step,yt_fighter_shield_spill_rows}` | the native parent now emits the title before fresh hydration, scans cached candidates in ascending order, preserves teammate/decline exhaustion semantics, uses exact A8D2 and DOUBLE commitment prompts, and performs the fresh target/current validation reads. Admission durably overlays the raw reserve before sound/RNG; casualty radio retains the MBF64-low-half-as-MBF32 alias; the post-radio current read supplies the intentionally stale displayed reserve; current and target casualty writes precede the two exact result rows; shield spill and its fresh target/current overlays lead into the existing fighter-kill child. Main dispatch enters sector reentry only for candidate exhaustion; direct combat/refusal/kill-tail returns go to the next prompt. Binary-safe row fixtures, raw unrelated-byte checks and the canonical bounded body stream are native. All 29 isolated-body and 34 joined cycle/hazard upstream cases pass; all 74 parent transfers and the active-cycle presentation family are candidate. | `candidate` | add exact native 327/370-byte active and admitted-hazard joined streams, complete independent selector/combat/post-radio/casualty/shield/router FIELD/cache/RNG/radio state, all alternate and terminal paths, local framebuffer and every staged failure prefix |
| Anonymous sector-mine body and ordinary/direct caller continuations | `src/yt_session.c::{mine_encounter,sector_entry,attack_player,common_fatal_self,emergency_warp,random_value,shrink_three,mine_stock_loss,reload_player,session_present_text,session_sound,append_news_bytes}`, `src/yt_game.c::{yt_sector_mine_batch,yt_sector_mine_shield_result,yt_sector_mine_cloak_loss,yt_sector_mine_missile_loss,yt_sector_mine_empty_holds,yt_sector_mine_sector_overlay,yt_sector_mine_player_overlay,yt_sector_mine_explosion_row,yt_sector_mine_shields_row,yt_sector_mine_loss_row,yt_sector_mine_entry_news,yt_sector_mine_final_news}` | the ordinary caller owns the strict-positive/exact-zero-suppression admission while a positive original post-kill mine load enters the body directly. Every batch fresh-reads and overlays only sector offset 129 before output, then follows the exact positive-shield or nonpositive-shield damage partition. Surviving shields consume the scanner draw even when absent; disintegration skips it; unshielded scanner destruction uses raw MBF32 `00 00 48 00`; fighters, stocks and empty holds retain the three-draw shrink cardinality under the authorized CSPRNG-source departure. A second fresh player GET receives only touched raw fields, sound two precedes the unconditional emergency draw, destroyed ships may still warp, and final news/sector GET occur only on non-warp completion. The body returns destroyed state for each actual caller to route into common death. Native reducers pin arithmetic and untouched bytes, the exact plain body is 379 bytes, all 22 focused upstream cases pass, and all 90 body plus ten caller-continuation transfers are candidate. | `candidate` | add a complete injected session/raw FIELD/store/news/RNG transaction, TIMER/reseed metadata for the authorized RNG boundary, exact ANSI/local endpoint fixtures, corrupt-state branches and every dependency/physical failure prefix |
| Direct fighter-kill wrapper | `src/yt_session.c::{attack_player,kill_player,salvage_player,mine_encounter,common_fatal_self,reload_player,session_sound,session_02db,append_news_bytes}`, `src/yt_game.c::{yt_direct_fighter_mine_warning,yt_sector_mine_sector_overlay}` | strictly positive resulting shields take a zero-effect fresh-prompt return. Zero or negative shields dispatch selector three before a fresh victim snapshot; raw mine value and `CINT`/`LEFT$` fixed-field name are saved before death and salvage. Only a strictly positive saved mine count performs the fresh sector GET and raw offset-129 full-value add. Deployment precedes the exact paged warning, the same binary bytes are appended to news, and the anonymous mine body is called without reapplying ordinary admission even when the updated sector total is nonpositive. The caller alone routes the mine's destroyed return to common death. Native fixtures pin embedded-NUL warning composition and the exact 48-byte plain blank/B05D framing; all 20 upstream wrapper cases pass and all 15 qualified transfers are candidate. | `candidate` | add a complete injected combat/death/salvage/mine raw FIELD/store/news/RNG transaction, exact native shared-child transcripts, full 403/738-byte plain/ANSI compositions, local/pager/carrier modes and every dependency/physical failure prefix |
| Direct fighter destroyed-to-fatal cycle | `src/yt_session.c::{attack_player,mine_encounter,common_fatal_self,kill_player,session_wait,yt_session_run,quit_session,show_ship,session_02db,session_sound}`, `src/yt_score.c::yt_score_generate_progress` | a nonzero destroyed flag returned by the mine body reaches common fatal without intervening output, RNG, I/O or state normalization. The bridge inherits mine background/pager/input state, emits the fatal `02DB` row, reloads the current player, dispatches selector three, invokes self-death, and owns the five-second wait. `fatal_wait_complete` prevents a duplicate outer wait, after which `yt_session_run` enters normal exit directly without confirmation. Native presentation pins the exact plain `CRLF + fatal LFCR + BEL` splice under inherited background one and the existing normal-exit tails; all 11 upstream joined-cycle cases pass. | `candidate` | add one complete native injected 1,597/1,900-byte raw transaction, exact 133-event local tape, full Info/scoreboard/death/news/FIELD/cache state, lazy nonfatal/emergency branches and every carrier/dependency/physical failure prefix |
| Hostile-fighter menu front | `src/yt_session.c::{sector_entry,hostile_menu_help,session_0317,session_031f,session_0357,session_02db}`, `src/yt_game.c::{yt_hostile_menu_row,yt_hostile_menu_dispatch}` | fresh player hydration precedes DOUBLE ratio construction and exact foreground-three framing; exact 35-byte prompt, empty-to-`?`, exact `S`/`I`, whole-string `INSTR` aliases, invalid queue-clear/trailing-blank reprompt, nine Help rows/fresh hydration, Info fresh hydration and Sector direct gameplay-reentry ownership are represented. Score fixtures pin ratio and dispatch matrices; presentation fixtures pin canonical 73-byte Attack, 119-byte invalid and 257-byte Help ANSI streams, pager reset/counts and final local colors. | `candidate` | preserve genuine higher-precision returned DOUBLE caches; add database-backed A41C failure, complete AB36/carrier/timeout/SysOp joins and physical endpoints; downstream Quit/Attack/Bribe/Mine/Warp/Team action compositions remain separate |
| Shared Quit confirmation and caller joins | `src/yt_session.c::{sector_entry,computer_menu,command_shell,planet_menu,session_quit_confirm,session_02fc,session_0357,quit_session}`, `src/yt_input_model.c::{yt_input_yes_no_candidate,yt_input_splitter_select_source,yt_b05d_process_key}`, `src/yt_pager.c::yt_paged_row_run` | the shared foreground-seven `<Quit>` B05D row and raw confirmation loop now own all four hostile, computer, main and planet callers. Full answers reduce to their uppercased first byte; blank/N cancel, Y enters normal exit once, and invalid sets bold and clears queued typeahead before raw reprompt. Native presentation pins the exact hostile transaction, 148/118-byte computer and 206/186-byte planet cancellation cycles, ANSI/plain main `Qjunk` cancellation suffixes, complete accepted hostile/computer/planet exits, the exact accepted main prefix through its named normal-exit handoff, and a 145/103-byte computer `Q;X;Y` retry that consumes queued `X`, clears the remaining queued `Y`, emits only the raw prompt again, and accepts a separately supplied `Y` with exact ANSI/plain local and style state. Exact upper- and lowercase `Q;N;NEXT`/`Q;n;NEXT` and `Q;Y;NEXT`/`Q;y;NEXT` joins separately consume the queued answer and CR while retaining `NEXT CR`; the 155/125-byte cancellation reaches the open fresh-prompt framebuffer and the 103/83-byte confirmation reaches the normal-exit handoff with case-preserved accumulator/echo, uppercase source, exact queue, local-row and color state. Exact `Q;;NEXT` consumes only the queued submission CR, emits no answer byte, reaches the 153/123-byte cancellation boundary with an empty accumulator, and retains `NEXT CR`. Separate local and remote `CR` samples during `<Quit>` traverse the production source selector and B05D key processor, become the confirmation's sole queued input, and produce identical 147/117-byte no-echo blank cancellations with an empty final queue and exact framebuffer/style state. | `candidate` | add full raw caller entry/redraw state, carrier/terminal/raw-frame and physical failure prefixes |
| Confirmed normal-exit body and hostile/computer/planet-Q joins | `src/yt_session.c::{sector_entry,computer_menu,planet_menu,quit_session,show_ship,computer_scoreboard_progress,display_game_file,session_attention,session_wait,session_0317,session_031f,session_0357,session_02fc,session_present_text}`, `src/yt_score.c::yt_score_generate_progress` | exit now starts from the caller's cached player state without an invented reload, sets foreground one, invokes Info, then emits the direct blank, low-time/no-newline generating row, four raw progress dots, direct blank, nonstop viewer, optional attention/ten-second wait/direct blank and LFCR BBS-return row in recovered order. One native transaction composes the production Info reducer, low-time wrapper, generator framing, real 603-byte DOS-EOF scoreboard through the physical `FILE *` parser/viewer, evaluation attention and timer, and final pager row, pinning the canonical 1,390/1,460/1,284/1,330-byte standalone streams. Joined transactions carry the live hostile fighter/prompt, active-computer, or planet free-holds/prompt prefix, exact Q dispatch and accepted-Y confirmation state—with `line_count = 0` and retained `14:59` time—into that same body. The hostile join separately carries cached heading `CACHED CURRENT` alongside independently supplied later player values while pinning 1,447/1,517/1,387/1,433-byte streams; computer and planet pin 1,486/1,556/1,360/1,406 and 1,510/1,580/1,394/1,440 bytes. Exact local rows, color sequences, 25/24/25 pager samples, FILE/parser state and cleanup are carried through every joined body. The main caller separately pins its exact 92/72-byte prefix and line-count-zero/14:59 state at the oracle's named `YT:0206` handoff, whose receiving body is the same exact normal-exit owner. | `candidate` | add raw registration CINT/FIELD/cache/world/generator state, authoritative hostile/planet A41C/updater state, modes/carrier/F8, physical CLOSE/END and every failure prefix |
| Planet-menu ordinary front, inventory, exact Take actions, external/action cycles, and Computer handoff | `src/yt_session.c::{planet_menu,planet_inventory,show_ship,planet_take_one,planet_take_all,planet_rename,command_land,computer_menu,computer_activate,computer_prompt_hydrate,display_sector,display_sector_one,reload_player,planet_update,planet_update_cached,session_quit_confirm,session_0317,session_031f,session_0345,session_0357,session_036f,session_a8d2,session_02db,session_02fc,session_present_text,session_right_aligned,session_sound,quit_session}`, `src/yt_game.c::{yt_info_panel_run,yt_info_team_resolver_run,yt_planet_menu_selector_position,yt_planet_take_one_title,yt_planet_take_one_player_overlay,yt_planet_take_one_planet_overlay,yt_planet_take_all_weapon_player_overlay,yt_planet_take_all_weapon_planet_overlay,yt_planet_take_all_commodity_player_overlay,yt_planet_take_all_commodity_planet_overlay}`, `src/yt_presentation.c::yt_present_refresh_time`, `src/qb.c::qb_title_case_n`, `src/qb_format.c::{qb_str_single,qb_str_double}` | each prompt pass now resets the counter, performs the first player load, computes free holds through three ordered DOUBLE subtractions, emits the exact `0317` row and direct blank, sets foreground six, performs the independent second player load and silent updater, then emits the cached-time no-newline prompt before the editor. Blank editor input takes the direct compiled default jump to Take All. Take One emits all seven exact title payloads, derives maximum and stock validation from transient DOUBLE `Q[]`, uses the direct blank plus `031F`/`036F` amount transaction, applies stock-before-capacity `02DB` errors, and on acceptance overlays independently fresh player/planet records from cached Q before subtracting the cache and rehydrating the player. Take All retains the same updater cache, performs the fresh-player cached-Q weapon PUT before its `Taking:` and four weapon rows, overlays a fresh planet from cached Q, then transfers Equipment, Organics and Ore through independent fresh player/planet records and non-transactional PUTs. Successful linked landing enters the shared 585-byte inventory owner before its first planet prompt, matching the `YT:39B1` join. Exact whole-string Q enters the shared helper; cancellation takes a zero-byte join into a complete fresh pass, while accepted Y carries the live line-count-zero prompt/confirmation state into the complete normal-exit body. Exact whole-string D calls the same inventory body and returns with zero adapter bytes to a fresh prompt. Exact whole-string S runs the mode-one six-cache adjacent sensor and returns without a mode-zero current-sector display. Its canonical nonzero composition pins the 77-byte prompt, 3-byte editor echo, 1,119/673-byte ANSI/plain scanner child and 87/77-byte redraw as exact 1,286/830-byte cycles; it carries duplicate/self target order, rows, sounds, attention-owned sounds, 15-second pause, 39 completed local rows, color tape, terminal style/pager state and open following prompt. All seven canonical Take One selectors accept amount one through the shared prompt and editor: Ore, Organics, Equipment, Fighters, Missiles, Mines and Plasma compose 36/41/42/42/40/37/44-byte bodies and exact 193/198/199/199/197/194/201-byte ANSI/plain cycles. Each pins its shipped title punctuation, maximum 65/65/65/404/5/6/9, distinct remote and local-row hashes, common color tape, retained amount accumulator and final prompt state. Explicit amount zero composes a distinct exact 193-byte cycle with its own remote and row hashes; it leaves player Ore 10 unchanged but still performs both overlays, replacing a fresh planet selected field 999 from cached Q as 101, and retains all three compiled persistence effects. Blank amount input emits only the editor CRLF, substitutes maximum 65 without printing it, applies player Ore 10 to 75 and cached planet Ore 101 to 36, and composes a 35-byte body plus redraw into an exact 192-byte ANSI/plain cycle with its own row hash, empty accumulator and the same terminal invariants. Raw response 1e3 is echoed and retained exactly, while the uppercase-E numeric transformation clears its value and selects the same maximum; its 38-byte body composes an exact 195-byte ANSI/plain cycle with a distinct row hash and identical persistence and terminal state. The stock-shortfall response 102 composes the same 77-byte prompt and 3-byte editor with an 82/68-byte ANSI/plain rejection body and 87/77-byte redraw into exact 249/225-byte cycles, including thirteen local rows, exact color tape, queue clear, retained amount accumulator and final prompt state. The separate negative predicate accepts -1 from VAL/INT and reaches that same error owner as exact 248/224-byte cycles with an 81/67-byte ANSI/plain body, its own row hash and the same color, queue and terminal invariants. The stock-valid capacity-overflow response 66 follows the subsequent gate and composes an 80/66-byte ANSI/plain rejection body with the same redraw into exact 247/223-byte cycles with distinct error text and the same exact local and terminal invariants. Exact lowercase whole-response `n` preserves its echo while compatibility dispatch enters Rename; the protected-record branch composes the 77-byte prompt, 3-byte editor, 48/34-byte ANSI/plain body and 87/77-byte redraw into exact 215/191-byte cycles with nine local rows, exact color tape, retained command accumulator and final prompt state. Exact lowercase position-fourteen `t` preserves its echo while compatibility dispatch enters Transfer; blank selector input composes the 77-byte prompt, 3-byte editor, 131-byte selector body and 77-byte redraw into a 288-byte ANSI/plain cycle with nineteen local rows, exact color tape, empty selector accumulator and final prompt state. With all three carried commodities zero, selector C preserves the sole pre-updater effect, emits the 175/161-byte ANSI/plain no-cargo body, and joins the 88/78-byte redraw as exact 344/320-byte cycles with twenty-one local rows, queue clear, exact colors, retained C and final prompt state. With ordinary carried Cargo values 10/20/5, selector C composes the same 77-byte prompt and 3-byte editor with the exact 155-byte accepted body and 77-byte redraw into a 312-byte ANSI/plain cycle. It pins twenty-one local rows, distinct ANSI/plain color tapes, retained C and final prompt state; the implementation preserves the oracle's ordered pre-updater, three cache mutations, player PUT, planet PUT, tail updater and selector-four sound sequence, while raw dependency failure prefixes remain open. With cached Fighters 7 and amount 3, selector F adds its second prompt/editor transaction and composes 216/204-byte ANSI/plain bodies plus 87/77-byte redraws into exact 383/361-byte cycles. It pins twenty-three local rows, exact color tapes, retained amount 3 and final prompt state; the selected arithmetic yields player Fighters 4 and planet Fighters 407, and source order preserves player PUT, planet PUT, tail updater and selector-four sound. The direct B/S/M selectors compose 173/161-, 169/157- and 166/154-byte ANSI/plain bodies plus 87/77-byte redraws into exact 340/318-, 336/314- and 333/311-byte cycles. Each pins twenty-one local rows, a shared direct-success color tape, retained selector and final prompt state; cached player values 4/2/3 and cached planet Q values 9/5/6 produce planet totals 13/7/9 while independently fresh selected player fields become zero before both PUTs and the common tail. The remaining selector/result variants are joined through the fresh prompt: invalid X is an exact 289-byte no-effect cycle; admitted substring SF is an exact 290-byte tail-only accepted cycle; Fighter blank and raw 1e2/E-cleared cancellation are exact 335/338-byte cycles; and negative -1 plus over-cache 8 rejection are exact 337/336-byte cycles. They pin nineteen- or twenty-one-row local tapes, exact colors, raw accumulator retention, zero persistence for cancellation/rejection, and tail updater plus selector-four sound only for SF. Exact position-seventeen `$` with blank numeric input composes the 77-byte prompt, 3-byte editor, 153-byte Productivity body and 77-byte redraw into a 310-byte ANSI/plain cycle with thirteen local rows, exact color tape, empty accumulator and final prompt state. Exact lowercase position-sixteen `b` preserves its echo while compatibility dispatch enters Bank; blank numeric input composes the 77-byte prompt, 3-byte editor, 118-byte cancel body and 77-byte redraw into a 275-byte ANSI/plain cycle with eleven local rows, exact color tape, empty body accumulator and final prompt state. Exact position-one F composes the 77-byte prompt, 3-byte editor, 147/111-byte ANSI/plain positive Ground Forces body and 87/77-byte redraw into exact 314/268-byte cycles, including selector-four sound, eleven completed local rows, color tape and final prompt state. Exact whole-string I composes the 76-byte prompt, 3-byte canonical or 8-byte typed-ahead editor, 678/602-byte Info panel and 86/76-byte fresh redraw into exact 843/757-byte canonical and 848/762-byte `I;NEXT` cycles, with 24 completed local rows, open prompt fragment, full color tape, final style/line count and preserved `NEXT CR`. The self-captain route composes 718/642-byte panels and 883/797-byte cycles without a captain GET; the valid-other route composes 715/639-byte panels and 880/794-byte cycles with distinct validation and display GETs. Both pin 26 completed local rows and perform no overlay write. The due-refresh/promotion variant consumes four TIMER reads, emits the exact local cursor/time/color operations, validates captain record three, overlays only team `F77` through a fresh GET/PUT, emits the three promotion rows, performs the final player GET, and composes 808/732-byte Info bodies into 973/887-byte cycles with 27 local rows and final prompt state. Rename uses the physical protection equations, exact prompt/editor/canonicalizer, reserved alert, raw quoted confirmation/restart, 41-byte cap and fresh-record overlay. Help uses its exact two `0317` plus fifteen `02FC` rows; invalid input uses the queue-clearing styled `02DB` path. Position-five C is a direct logical transfer into shared Computer activation and its independent hydration/prompt. Landing creation/denial/defeat and planet Leave propagate the shared `YT:081F` sector-reentry signal. Exact position-thirteen `L` has no action body and composes its 77-byte prompt, 3-byte editor and 94/74-byte friendly gameplay re-entry into exact 174/154-byte ANSI/plain cycles with eight local rows, exact color state, no RNG and the following main prompt open. Exact position-two `!` composes the same 77-byte prompt and 3-byte editor with the complete 630/500-byte ANSI/plain scanner/controller/one-hop/re-entry transaction into exact 710/580-byte cycles, including all seven partition boundaries, 29 local rows, exact color and terminal state, selector-four sound, and the oracle's one stress RNG draw. Exact position-three `M` composes the 77-byte prompt and 3-byte editor with the ordinary movement parent's 89-byte accepted body and its 95/75-byte friendly gameplay re-entry into exact 264/244-byte ANSI/plain cycles, including four exact partitions, 14 local rows, complete color state, the final open main prompt, and the oracle's one finalizer RNG draw. Exact position-four `P` composes the 77-byte prompt and 3-byte editor with the exact-zero no-port controller branch and friendly gameplay re-entry into exact 223/179-byte ANSI/plain cycles, including four exact partitions, 11 local rows, inherited-cache-sensitive ANSI, complete terminal state, no turn debit and no RNG. Native presentation pins canonical 36-byte accepted and 82-byte stock-error Take One bodies, the 305-byte blank/Take-All/redraw cycle with exact 149-byte body, 662-byte inventory/first-prompt suffix, 206/186-byte Q cancellation cycles, 1,510/1,580/1,394/1,440-byte Q accepted exits, 742-byte D cycles, 271/205-byte all-zero S cycles, 314/268-byte positive F cycles, canonical 193/198/199/199/197/194/201-byte amount-one selector cycles and the 193-byte Ore-zero cycle, 192-byte blank-default and 195-byte E-cleared accepted, 249/225-byte stock-shortfall, 248/224-byte negative-stock and 247/223-byte capacity-rejected Take One, 215/191-byte protected-Rename, 288-byte Transfer-cancel, 344/320-byte Transfer-no-cargo and 312-byte Transfer-Cargo, 383/361-byte Transfer-Fighter, 340/318-byte Transfer-Plasma, 336/314-byte Transfer-Missiles and 333/311-byte Transfer-Mines, 289-byte invalid Transfer, 290-byte substring Transfer, 335/338-byte Fighter cancellation and 337/336-byte Fighter rejection, 275-byte Bank-cancel, 310-byte blank-Productivity, 174/154-byte Leave/re-entry, 710/580-byte accepted-Thrusters, 264/244-byte accepted-Movement and 223/179-byte no-port Docking cycles, 48/92/84-byte Rename bodies, 524-byte help, 201-byte invalid, 178/146-byte planet-C entry cycles and every ordinary live team outcome in the Info cycle. Reducer fixtures pin every Take One title/selected field, cached-Q versus fresh-field overlays, ordinary Take All transfers, corrupt negative free holds and explicit double rounding. | `candidate` | add authoritative distinct A41C/FIELD/updater/cache/RNG and physical failure-prefix state, malformed/noncanonical Take One selector/VAL raw-memory boundaries, exhaustive remaining planet substring/action-result and joined Rename fixtures, lowercase/Qjunk/typeahead and invalid-retry source fixtures, low-time/mode/carrier/editor prefixes, final local framebuffer and remaining action-result variants |
| Planet inventory/menu parent | `src/yt_session.c::{planet_inventory,planet_menu,show_ship,display_sector,planet_rename,planet_take_one,planet_take_all,planet_transfer,planet_bank,planet_productivity,planet_garrison,planet_move,command_trade,computer_menu,session_0317,session_031f,session_0357,session_02db,session_02fc,session_present_text}`, `src/yt_game.c::{yt_info_panel_run,yt_info_team_resolver_run,yt_planet_menu_selector_position}`, `src/yt_presentation.c::yt_present_refresh_time` | every bounded `YT:[39BA,3FA0)` inventory/menu edge is mapped. Native presentation pins the 585-byte inventory, ordered double-subtraction free-holds/prompt pass, 524-byte Help and 201-byte invalid redraws, blank-to-Take-All default, and the ordinary D/Q/S/C cycles. All seven canonical amount-one Take One routes are joined as exact 193/198/199/199/197/194/201-byte ANSI/plain cycles, with exact title punctuation, maximum prompt, body partition, distinct remote and row hashes, eleven local rows, color tape and following-prompt state. Ore amount zero remains a distinct exact 193-byte cycle with its own hashes. Zero leaves carried Ore unchanged while the native overlay still replaces the fresh planet selected field from cached Q. Its blank-default maximum variant is joined as an exact 192-byte ANSI/plain cycle with a 35-byte body, empty accumulator, exact 65-unit native overlays and the same local and terminal invariants. The raw-echo 1e3 variant retains its accumulator while the numeric output is E-cleared to the same default; its 38-byte body yields an exact 195-byte ANSI/plain cycle with the same overlays and terminal state. The stock-shortfall route is joined as exact 249/225-byte ANSI/plain cycles with an 82/68-byte body, thirteen local rows, color tape, queue clear and following-prompt state. The negative-amount route separately reaches the stock error as exact 248/224-byte ANSI/plain cycles with an 81/67-byte body, distinct row hash and the same local and terminal invariants. The capacity-overflow route is joined as exact 247/223-byte ANSI/plain cycles with its distinct 80/66-byte body and the same exact local and terminal invariants. The protected Rename route is joined as exact 215/191-byte ANSI/plain cycles with lowercase command provenance, 48/34-byte bodies, nine local rows, color tape and following-prompt state. The blank-selector Transfer route is joined as an exact 288-byte ANSI/plain cycle with lowercase command provenance, a 131-byte body, nineteen local rows, color tape and following-prompt state. The all-zero Cargo route is joined as exact 344/320-byte ANSI/plain cycles with 175/161-byte bodies, the sole pre-updater effect, twenty-one local rows, queue clear, color tape and following-prompt state. The ordinary accepted Cargo route is joined as an exact 312-byte ANSI/plain cycle with a 155-byte body, twenty-one local rows, distinct ANSI/plain color tapes, retained C and following-prompt state; source order preserves the pre-updater, three cache mutations, player PUT, planet PUT, tail updater and selector-four sound. The accepted Fighter amount-3 route is joined as exact 383/361-byte ANSI/plain cycles with 216/204-byte bodies, twenty-three local rows, exact color tapes, retained amount 3 and following-prompt state; cached arithmetic yields player Fighters 4 and planet Fighters 407 before the ordered tail. The direct Plasma/Missile/Mine routes are joined as exact 340/318-, 336/314- and 333/311-byte ANSI/plain cycles with 173/161-, 169/157- and 166/154-byte bodies, distinct twenty-one-row tapes, shared direct-success colors, retained selectors and following-prompt state. Invalid X and admitted substring SF are joined as exact 289/290-byte cycles; Fighter blank and E-cleared cancellation as 335/338 bytes; and negative/high rejection as 337/336 bytes. All pin exact local/color/accumulator/following-prompt state, with no effects except SF's common tail. The blank-input Productivity route is joined as an exact 310-byte ANSI/plain cycle with a 153-byte body, thirteen local rows, color tape and following-prompt state. The blank-input Bank route is joined as an exact 275-byte ANSI/plain cycle with lowercase command provenance, 118-byte body, eleven local rows, color tape and following-prompt state. The positive F route is joined as exact 314/268-byte ANSI/plain cycles with 147/111-byte bodies, selector-four sound, eleven local rows, color tape and following-prompt state. The nonzero S route is joined as exact 1,286/830-byte ANSI/plain cycles with 1,119/673-byte scanner children, exact sound/wait sequencing, 39 local rows, color tape and following-prompt state. Exact position-thirteen `L` composes its 77-byte prompt, 3-byte editor and 94/74-byte friendly gameplay re-entry into exact 174/154-byte ANSI/plain cycles, with eight local rows, exact color state, no RNG and the following main prompt open. Exact position-two `!` composes the prompt/editor with the complete accepted one-hop controller and friendly re-entry into exact 710/580-byte ANSI/plain cycles, with seven exact partitions, 29 local rows, exact color and terminal state, selector-four sound and one stress RNG draw. Exact position-three `M` composes the prompt/editor with the 89-byte accepted movement parent and 95/75-byte gameplay re-entry into exact 264/244-byte ANSI/plain cycles, with four exact partitions, 14 local rows, complete color and terminal state, and one finalizer RNG draw. Exact position-four `P` composes the prompt/editor with the exact-zero no-port controller branch and friendly re-entry into exact 223/179-byte ANSI/plain cycles, with four exact partitions, 11 local rows, inherited-cache-sensitive ANSI, complete terminal state, no debit and no RNG. Whole-string I composes exact 843/757-byte team-zero, 883/797-byte self-captain, 880/794-byte other-captain, 848/762-byte typed-ahead and 973/887-byte due-refresh/promotion cycles. All four team outcomes pin distinct captain GET/PUT tapes, 24/26/27 local rows, color tapes and final prompt state. Root `YT:39BA` and all 83 bounded qualified edges remain candidate. | `candidate` | add remaining action-result variants, authoritative distinct A41C/updater/FIELD/cache/RNG images, exhaustive dispatch raw state, all modes/local framebuffer/carrier/runtime cuts and every physical failure prefix |
| Planet Thrusters controller and one-hop movement | `src/yt_session.c::{planet_move,planet_move_hop,planet_move_friendship,build_route,display_sector,computer_prompt_hydrate,session_0317,session_031f,session_036f,session_02fc,session_02db,session_a8d2,session_present_text,session_sound,reload_player,append_news_bytes}`, `src/yt_game.c::{yt_planet_move_destination,yt_planet_move_maximum,yt_planet_move_add_cost,yt_planet_move_fighter_loss,yt_planet_move_sector_overlay,yt_planet_move_explosion_overlay,yt_planet_move_fighter_overlay,yt_planet_move_success_overlay,yt_planet_move_path_heading,yt_planet_move_summary,yt_planet_move_turns_row,yt_planet_move_explosion_row,yt_planet_move_explosion_news,yt_planet_move_loss_row,yt_planet_move_success_row}` | the `!` dispatch now owns the exact cost notice, mode-zero scanner, destination grammar, equality-before-range checks, avoid-aware route construction, mixed raw/B05D route display, repeated SINGLE cost, fresh-turn gate, raw confirmation retries, forced engagement state and concatenated movement tokens. Each hop derives the moving planet from its second source GET, preserves the Xannoron early return, occupied forced explosion, strict stress gate, zero-fighter continuation defect, two-draw fighter shrink, Wanderer random placement/requested player-sector split, mine/fighter stop tests, fresh friendship reads and raw single-lane sector/planet/player overlays. Stop jumps directly to gameplay re-entry while natural exhaustion first performs A41C. Native reducers pin all dynamic rows, arithmetic, name erasure and unrelated-byte preservation; the canonical plain one-hop controller body is exactly 350 bytes. A joined native transaction now composes the 77-byte planet prompt, 3-byte `!` editor, complete initial mode-zero scanner, accepted one-hop controller, selector-four sound and one friendly gameplay re-entry into exact 710/580-byte ANSI/plain cycles, with all seven partition boundaries, 29 local rows, complete color tapes and exact open-main-prompt state. All 42 focused upstream controller/helper cases pass, and all 109 bounded edges are candidate. | `candidate` | add one complete injected raw FIELD/store/cache/RNG transaction across the joined menu/scanner/controller/helper/re-entry path; pin every Xannor/occupied/explosion/negative-loss/Wanderer/hazard branch natively, ANSI/local/queue/pager/corrupt-state variants, TIMER reseed metadata, recursive post-re-entry hazards and every shared-runtime/physical failure prefix |
| Planet Rename action | `src/yt_session.c::{planet_rename,session_02db,session_031f,session_0345,session_a8d2,session_present_text}`, `src/yt_game.c::{yt_planet_rename_protected,yt_planet_rename_prepare_name,yt_planet_rename_overlay,yt_game_read_planet,yt_game_write_planet}`, `src/qb.c::qb_title_case_n` | Rename evaluates the three physical-record protection equations before output, then owns the exact direct blank, no-newline prompt, case-preserving editor, canonicalizer, empty and reserved gates before the 41-byte cap, raw quoted confirmation and N restart. The internal name and A8D2 response buffers admit the oracle's successful 256-byte cases. Acceptance performs a fresh planet GET and changes only the fixed 41-byte name and MBF32 length fields before PUT. Native presentation pins the canonical 48-byte protected, 92-byte reserved, 84-byte ordinary accepted, 373-byte long-name and 339-byte long-confirmation streams. The lowercase-command protected-record caller join composes the 77-byte prompt, 3-byte dispatch, 48/34-byte ANSI/plain body and 87/77-byte redraw into exact 215/191-byte cycles with nine local rows, exact color tape, retained command accumulator and final prompt state; the two long-stream SHA-256 values are `fddb01354740c58a112527060a1e84030019530d0c37ed2999952b36f8de452b` and `ebc33bc749cd4083ff032b01a660655a6177fa5ee7e15bbcb2c7cc9dd78a8ac3`. Model fixtures pin normal and fractional protection equations, normalization/reserved/cap order, and exact unrelated-byte preservation in a malformed fresh record. All 27 qualified body edges, the presentation family and root `YT:5464` are candidate; all 39 focused upstream Rename/action/full-cycle tests pass. | `candidate` | add accepted/reserved/empty joined redraw/restart/typeahead and raw current-record/frame state, modes/local framebuffer, low-time/pager/carrier/runtime cuts and every physical failure prefix; corrupt random-record coercion and the 32768-byte BRUN string overflow remain explicit boundaries |
| Planet Transfer action | `src/yt_session.c::{planet_transfer,reload_player,planet_update,planet_update_cached,session_0317,session_031f,session_0357,session_036f,session_02db,session_02fc,session_present_text,session_sound}`, `src/yt_game.c::{yt_planet_transfer_selector_position,yt_planet_transfer_cargo_empty,yt_planet_transfer_cargo_cache,yt_planet_transfer_cargo_player_overlay,yt_planet_transfer_cargo_planet_overlay,yt_planet_transfer_direct_player_overlay,yt_planet_transfer_direct_planet_overlay,yt_planet_transfer_fighter_rejected,yt_planet_transfer_fighter_player_overlay,yt_planet_transfer_fighter_planet_overlay}` | the exact selector front retains whole-response `INSTR("CSFMB", response)` admission, exact single-letter branches and unmatched admitted substring fallthrough. Cargo hydrates before its updater and exact all-zero test, applies the transient three-item catch-up loop with `floor(total) / 10 + 1`, then overlays independently fresh player and planet records. Plasma, missiles and mines use cached player amounts and cached `Q[]` against fresh selected records without validation. Fighter preserves its direct blank, DOUBLE prompt, `036F` E-clear, SINGLE `VAL`, fraction/zero acceptance, silent bounds rejection and cached selected-field arithmetic. Every accepted exact branch and admitted substring executes the fresh-player/updater/selector-four-sound tail; cancel, invalid, fighter rejection and no-Cargo do not. Native presentation pins all ten canonical 131/132/133/175/155/173/169/166/178/216-byte streams. The lowercase-command blank-selector caller join composes the 77-byte prompt, 3-byte dispatch, 131-byte body and 77-byte redraw into an exact 288-byte ANSI/plain cycle with nineteen local rows, exact color tape, empty accumulator and final prompt state. The all-zero Cargo route composes a 78-byte prompt, 3-byte dispatch, 175/161-byte ANSI/plain body and 88/78-byte redraw into exact 344/320-byte cycles; it pins the sole pre-updater effect, twenty-one local rows, queue clear, retained selector C and final prompt state. The ordinary accepted Cargo route composes a 77-byte prompt, 3-byte dispatch, 155-byte body and 77-byte redraw into an exact 312-byte ANSI/plain cycle; it pins twenty-one local rows, distinct ANSI/plain color tapes, retained selector C and final prompt state, while the implementation orders the pre-updater, three cache mutations, player PUT, planet PUT, tail updater and selector-four sound exactly as the oracle. The accepted Fighter amount-3 route composes the same prompt and dispatch with 216/204-byte ANSI/plain bodies and 87/77-byte redraws into exact 383/361-byte cycles; it pins both editors, twenty-three local rows, exact color tapes, retained amount 3 and final prompt state, while reducer arithmetic and source order preserve player Fighters 4, planet Fighters 407, both PUTs, the tail updater and selector-four sound. The direct Plasma/Missile/Mine routes compose exact 340/318-, 336/314- and 333/311-byte ANSI/plain cycles with 173/161-, 169/157- and 166/154-byte bodies; they pin all three selectors, distinct twenty-one-row tapes, shared direct-success colors and the common tail, while reducers pin cached arithmetic and fresh-record preservation. Invalid X and admitted substring SF compose exact 289/290-byte cycles, Fighter blank and E-cleared cancellation exact 335/338-byte cycles, and negative/high rejection exact 337/336-byte cycles. These joins pin all remaining canonical selector/result paths, nineteen- or twenty-one-row tapes, raw accumulator state, no writes for cancellation/rejection, and the tail-only SF effect order. Reducer fixtures pin selection, zero/nonzero Cargo, catch-up arithmetic, fresh unrelated-field preservation, cached direct weapons, fighter bounds and fractional selected overlays. All 68 qualified body edges are assigned candidate, retaining the proven dead alternate sound arm. | `candidate` | add complete raw FIELD/updater/cache/effect and every dependency/physical failure prefix, noncanonical `VAL` and corrupt runtime domains, typeahead/carrier/pager/modes, local framebuffer and physical endpoint fixtures |
| Planet Bank action | `src/yt_session.c::{planet_bank,mutate_player_credits,reload_player,write_player,session_0317,session_031f,session_036f,session_02db,session_present_text,session_sound}`, `src/yt_game.c::{yt_credit_mutation_run,yt_planet_bank_available,yt_planet_bank_remaining,yt_planet_bank_planet_overlay,yt_planet_bank_credit_argument,yt_planet_bank_credit_overlay}` | Bank uses the cached runtime planet name and cached player credit but begins with an independent fresh planet GET for its promoted bank value. It emits the exact title, explicit blank and DOUBLE-availability prompt, cancels blank/E-cleared input before `VAL`, applies `INT_D`, tests savings before the rounded `D(D(C-T)+B) < 0` credit predicate, and emits exact styled errors. Accepted zero and positive targets independently GET/overlay/PUT the planet, replace the caller cache, emit the branch-specific success, call selector-four sound, compute `S(D(B-T))`, then enter the shared owner for a fresh player GET, SINGLE add, `INT_S`, credits-only raw overlay and player PUT. Native fixtures pin the canonical 213-byte accepted, 161-byte zero, blank cancel and both error streams. The lowercase-command blank-input caller join composes the 77-byte prompt, 3-byte dispatch, 118-byte cancel body and 77-byte redraw into an exact 275-byte ANSI/plain cycle with eleven local rows, exact color tape, empty accumulator and final prompt state; reducers pin stale/fresh unrelated fields and the 16,777,217 target/tie boundaries. All 21 qualified body edges are candidate. | `candidate` | add raw cached-name/credit and FIELD/effect fixtures, every ordered dependency/physical failure prefix, noncanonical `VAL` and runtime domains, typeahead/carrier/pager/modes, local framebuffer and accepted/zero/error fresh-prompt cycles |
| Planet Productivity action | `src/yt_session.c::{planet_productivity,mutate_player_credits,reload_player,write_player,planet_update_cached,session_0317,session_031f,session_036f,session_02db,session_present_text}`, `src/yt_game.c::{yt_credit_mutation_run,yt_planet_productivity_units,yt_planet_productivity_cache,yt_planet_productivity_credit_argument,yt_planet_productivity_planet_overlay,yt_planet_bank_credit_overlay}` | Productivity performs the fresh player hydrate then silent updater before output; uses the exact double-space explanation, cached DOUBLE credit row, explicit blank and `-+>` prompt; passes blank/E-cleared/nonnumeric through `VAL` to the silent below-one branch; and applies the exact insufficient-credit row before `U=D(T/250)`. Acceptance emits its unit row before changing all three P caches, computes fighter/missile/mine/plasma deltas with the recovered SINGLE sums/divisors and exact plasma multiplier, emits only nonzero `031F` fragments in fixed order, and emits an ending blank only for a nonzero fighter delta. It then computes `-S(D(U*250))`, enters the shared fresh-player credit owner, overlays independently fresh planet base rates from `P-A` and stocks from Q, writes, performs the final player hydrate and redraws. Native fixtures pin the canonical 241-byte, all-fragment 281-byte, one-credit 213-byte and unterminated later-only-fragment streams. The blank-input caller join composes the 77-byte prompt, 3-byte `$` dispatch, 153-byte body and 77-byte redraw into an exact 310-byte ANSI/plain cycle with thirteen local rows, exact color tape, empty accumulator and final prompt state; reducers pin exact cache deltas, fresh unrelated fields and the 16,777,217 debit boundary. All 35 qualified body edges are candidate. | `candidate` | add complete raw updater/FIELD/cache/effect and every ordered dependency/physical failure prefix, noncanonical `VAL` and corrupt runtime domains, typed-ahead/carrier/pager/modes, local framebuffer, physical endpoints and accepted/error fresh-prompt cycles |
| Shared clearance sale and Earth report/front end | `src/yt_session.c::{clearance,earth_store,earth_report_row,port_owner_row,reload_player,session_fixed_width,session_0317,session_031f,session_0357,session_02db,session_present_text}`, `src/yt_game.c::{yt_clearance_candidate_needed,yt_clearance_normalize,yt_clearance_percentage,yt_earth_prices,yt_earth_affordable,yt_earth_selector_position}` | Clearance uses the exact four-item trigger/candidate predicate order, strict normalization limits, percentage calculation, direct announcement order and selector-one/trailing-blank framing while retaining the authorized platform-CSPRNG source departure. Its row-helper root `YT-SUB:4A82` and both qualified output/return transfers are candidate; all 15 focused helper oracle tests pass. Earth reads port 1, observes date then time, composes the owner row, calculates prices before mode-one clearance, preserves the one/zero report-seen redraw rules, reloads the player, and emits the exact nine-row report/menu/prompt. Dispatch applies `VAL`/SINGLE/`CINT` first, exact full-token S/I next, then whole-response `INSTR("LM0C", response)` after storing fallback zero. Reducers pin zero, ordinary and maximum-discount price vectors, all affordability values and selector substrings. Presentation pins the canonical 652-byte no-owner/plain response-0 report (SHA-256 `fc61da4bf8896888ba17405bcf571deb8654b90ea3560f8a6e92bd9609d815be`), final pager count 16, and plain/ANSI two-sale sound framing. All 53 Clearance edges and all 63 `YT:[6CB3,7401)` Earth-front edges are candidate; all 32 focused upstream oracle tests pass. | `candidate` | add all raw discount/reset/report-seen/string-temporary cells, deterministic provider-error/interleaving cuts, owner/sale/redraw/S/I joined transcripts, ANSI/mode/local framebuffer and endpoint variants, carrier/input cuts, physical GET/output/shared-error failures and every partial prefix |
| Earth Anti-Cloak transaction | `src/yt_game.c::{yt_earth_anti_cloak_run,yt_credit_mutation_run}`, `src/yt_session.c::{earth_anti_cloak,earth_anti_cloak_read_player,apply_player_credit_mutation,mutate_player_credits_observed,earth_anti_cloak_present,earth_anti_cloak_sound}` | root `YT-SUB:932E` and all 23 internal transfers are candidate. The provider-driven owner pins the two malformed shipped descriptors, activation/waves/fade color and sound order, the SINGLE `FOR` domain, live-mode `CINT` cache/name indexing, strict-positive cache admission including unordered rejection, cache-zero-before-GET mutation, dead-player silence, binary stored names, report-flag timing, no-target rows, selector-five fade, negated price, delegation to the shared owner for fresh purchaser GET, SINGLE add plus `INT`, credits-only raw overlay and PUT-before-foreground-three, fractional terminals, unrelated-byte preservation and every read/write/presentation/sound failure prefix. | `candidate` | add raw MBF/FIELD/cache/descriptor/FLAGS/frame state, BRUN random-record and CINT internals, physical output/sound/GET/PUT/flush faults and complete caller prompt/re-entry composition |
| Earth purchase actions | `src/yt_session.c::{earth_store,earth_purchase_holds,earth_purchase_supply,earth_purchase_cloak,earth_purchase_scanner,earth_purchase_spies,lottery,lottery_settle,earth_anti_cloak,earth_receipt,mutate_player_credits,apply_player_credit_mutation,clearance,computer_spies,reload_player,write_player,session_0317,session_031f,session_0345,session_036f,session_a8d2,session_02db,session_02fc,session_present_text,session_press_any_key,session_sound}`, `src/yt_game.c::{yt_credit_mutation_run,yt_earth_anti_cloak_run,yt_earth_purchase_quantity,yt_earth_receipt_amount,yt_earth_cloak_points,yt_earth_cloak_default,yt_earth_cloak_overlay,yt_earth_supply_overlay,yt_lottery_match_count,yt_lottery_award}`, `src/yt_presentation.c::yt_present_lottery_rewind` | Holds, Fighters, Cloak, Scanner, Ground Forces and Shields now own their exact blanks, prompts, validation order, messages, SINGLE overlays and common cached-player/fresh-credit/cached-owner/fresh-Earth settlement. Anti-Cloak owns the raw confirmation, malformed patch descriptors, positive-cache clearing before fresh target GET, fixed-field names, ordered colors/sounds/fade, shared-owner fresh debit and no-Earth receipt. Spies owns active-row and `more` grammar, affordability-before-cap, cap retry, negative/duplicate sector admission, active-spy rows, press UI and settlement. Lottery owns its fresh entry/limiter writes, mode-zero Clearance, raw ticket retry, six actual plus 108 position-major dummy draws, 116/117 wait topology, mode-zero backspaces, unconditional local rewinds, multiset matches, visible award, preliminary GET/sounds/cached-name news/shared-owner fresh award credit, caller wait and separate five-credit settlement. Native presentation pins all six canonical fixed-price streams, the malformed Anti-Cloak target stream, the canonical negative-sector spy/press stream, and the canonical 472-byte Lottery loss with 108 backspaces; reducers pin overlays, receipts, duplicate matching and all awards. All 27 upstream purchase-oracle tests and nine native CTest suites pass. All 151 caller-body, 83 Lottery-helper and 23 Anti-Cloak-helper edges are candidate. | `candidate` | add authoritative distinct cached/fresh FIELD/database images and every injected operation failure prefix, raw noncanonical/overflow domains, deterministic full provider tapes, positive-award ANSI/local streams and news files, all rejection/retry/low-time/pager/typeahead/carrier/mode endpoints, complete redraw joins and physical framebuffer/I/O proof |
| Hostile Attack admission | `src/yt_session.c::{sector_entry,attack_deployed,session_02fc,session_031f,session_036f,session_02db}`, `src/yt_game.c::yt_hostile_attack_admit`, `src/qb_format.c::{qb_val,qb_str_double}`, `src/qb.c::{qb_mbf32_encode,qb_mbf32_decode}` | all six position-one aliases enter the exact `<Attack>` row; the no-fighters branch reads no amount, while the other branches use the exact prompt and numeric editor, `VAL` then MBF32 `CSNG` store, strict too-many-before-sub-one predicates, exact attention row, and silent sub-one return. The enclosing loop performs the direct fresh A41C hostile-menu re-entry. Score fixtures pin strict fractional/equality routing; presentation pins the canonical 56-byte no-fighters, 80-byte too-many, and 44-byte sub-one ANSI action partitions. | `candidate` | add database-backed complete two-A41C joined transcripts, inherited/raw `DS:4D1A` and no-fighters preservation fixture, editor/carrier/pager/error-handler and physical failure prefixes; admitted sector GET, combat, surrender, persistence, death, clearance, and Bribe remain separate |
| Hostile Attack defenders-remain path | `src/yt_session.c::{attack_deployed_committed,reload_player,session_sound,session_present_text,session_02fc,sector_entry}`, `src/yt_game.c::{yt_hostile_attack_quantum,yt_hostile_attack_loses_attacker,yt_deployed_attack_player_overlay,yt_deployed_attack_sector_overlay}` | admitted Attack now performs the opening sector GET/owner-only refresh, independent combat player hydration, selector-two sound, genuinely post-tested attrition, exact common loss rows, and fresh player-then-sector GET/raw-LSET/PUT persistence. The player overlay changes only shields/fighters; the sector overlay changes fighters and clears only an owner below one, preserving every unrelated byte. DOUBLE ship/defender caches remain live across their MBF32 persistence boundaries. The post-persist blank, mandatory late draw, and direct fresh hostile-menu return remain ordered. Native fixtures pin both raw overlays plus the canonical 139-byte ANSI body and quantum/strict-.45 polarity. | `candidate` | add an injected two-draw full native WORLD/FIELD/store fixture proving exact one-iteration LA=1/LD=0 state and all failure prefixes; physical I/O and asynchronous boundaries remain open |
| Shared fighter/shield spill | `src/yt_game.c::{yt_fighter_shield_spill_step,yt_fighter_shield_spill_rows}`, `src/yt_session.c::{fighter_shield_spill,attack_deployed_committed,session_present_text}` | the pre-tested loop uses strict positive operands, quantum 100 only when both are strictly above 100, exact `.5`-to-fighters polarity, ordered CSPRNG calls, no clamping, DOUBLE fighter/SINGLE shield formatting, and two unconditional direct unpaged rows. Native fixtures pin 100-then-one, equality, MBF32 precision, 16,777,217 DOUBLE formatting, ANSI per-line reset, unchanged pager count, and local PC-color endpoint. The deployed-fighter caller performs its exact attention/blank/spill handoff before persistence. | `candidate` | connect the direct-player Attack caller; add injected RNG/failure and full caller WORLD/FIELD fixtures, corrupt numeric domains, physical output failures, asynchronous event boundaries and final framebuffer reduction |
| Hostile Attack defenders-cleared, surrender and faction-reward tail | `src/yt_session.c::{attack_deployed_committed,session_a8d2,display_sector,display_sector_one,command_shell,clearance}`, `src/yt_game.c::{yt_hostile_surrender_route,yt_hostile_defeated_row,yt_xannor_attack_bonus,yt_xannor_attack_reward_rows}` | player surrender is admitted only for owner greater than one; owner one and corrupt owners are quiet, while Xannor and Mercenary refusals retain their distinct exact rows and selector-five sound. Forced Bribe combat uses this same one-shot surrender seam. Acceptance emits joined row/sound, appends binary-safe cached-name news, computes state, then emits the surrendered count without consuming that iteration's attrition draw. Positive loss performs the independent post-persistence hydration and binary-safe stale-label news. Xannor reward uses exact SINGLE clamp order, a turns-only raw overlay/PUT, binary-safe display/news composition, optional clearance, and then the mandatory draw. Native presentation pins the complete accepted-surrender body in plain and ANSI, both faction refusal/sound bodies, the reward row/local result, and the defeated continuation. | `candidate` | add deterministic full ordinary/accepted/refused/faction native FIELD/store/news/RNG/failure fixtures; complete clearance/HQ child state, scanner adapter, physical I/O and asynchronous boundaries |
| Hostile Mercenary Bribe accepted/quiet returns | `src/yt_session.c::{bribe_deployed,sector_entry,session_0317,session_031f,session_036f,session_02db,session_sound,reload_player}`, `src/yt_game.c::{yt_bribe_ordinary_forces,yt_bribe_mercenary_forces,yt_bribe_offer_threshold,yt_bribe_offer_accepted,yt_bribe_sector_overlay,yt_bribe_player_overlay}`, `src/qb.c::qb_mbf32_truth` | ordinary-owner refusal consumes its one post-row draw and uses the strict Xannor/defender predicates. Mercenary planet truth now uses the raw MBF32 exponent and consumes zero draws when set; a false link consumes both eager precheck draws before the sticky test. Empty input returns before `VAL` and threshold RNG. Nonempty offers perform `VAL`/MBF32 `CSNG`, consume the third draw even above credits, preserve emitted DOUBLE association, and accept equality. Success emits attention and selector one before a fresh sector GET/raw owner-then-fighter zero overlay/PUT, then fresh player hydration and raw fighter-then-credit overlay/PUT. Native fixtures prove unrelated-byte preservation and intentionally stale process caches; presentation pins all four action bodies at 132/188, 35/49, 94/94, and 34/48 bytes. | `candidate` | add injected three-draw full WORLD/FIELD/cache/RNG and ordered partial-failure fixtures, corrupt sticky conversion behavior, complete scanner ANSI/local adapter and physical endpoints |
| Hostile Bribe forced sub-one and immediate-fatal paths | `src/yt_session.c::{bribe_deployed,bribe_forced_attack,sector_entry,common_fatal_self,attack_deployed_committed}`, `src/yt_game.c::yt_bribe_forced_admit` | Xannor and ordinary force store the rounded cached ship commitment without a fatal test; Mercenary life demand and threshold rejection emit their exact attention row, store commitment, then apply the original ship-DOUBLE/shield-SINGLE fatal predicate. Nonfatal rounded commitments below one return directly to a fresh hostile menu without scanner, GET, sound, wait, RNG, or persistence. Fatal paths jump without output to the shared fatal owner. Commitments at least one enter the same post-tested Attack body with surrender enabled, matching the shipped `0BA3` join. Reducer fixtures distinguish fatal, ordinary sub-one, shield-protected sub-one and near-one conversion; presentation pins the 41/55-byte life and 134/148-byte rejection bodies. | `candidate` | add deterministic injected one/two/three-draw raw process/FIELD/cache/nonmutation and fatal death/news/wait transcripts plus all failure/physical endpoints |
| Shared sector-mine body and both caller continuations | `src/yt_session.c::{command_shell,command_mines,sector_entry,display_sector,reload_player,session_031f,session_036f,session_02db,session_02fc,session_sound}`, `src/yt_game.c::yt_sector_mine_admit` | the shared body performs an independent player hydration, preserves a negative carried cache while durably repairing only the record, applies exact no-mine and cached-sector Union gates, then uses the exact direct blank/SINGLE prompt/`036F`/`VAL`/MBF32 `CSNG` sequence. Amount tests are strict and accept positive fractions and equality. Acceptance sets self-mine suppression before I/O, persists player debit before a fresh sector GET/add/PUT, leaves the process player cache stale, and only then emits the foreground-six bold/blink success row and selector-four sound. The hostile whole-string D/DW/DWT wrapper returns every ordinary result through the `081F` scanner/router; main-command D instead enters one foreground-one mode-zero current-sector scanner and the fresh main prompt without hazard routing. Presentation pins hostile-context 73/119 accepted, 44/44 cancel, 23/37 no-mine, and 51/65 Union bodies. | `candidate` | add deterministic raw player/sector FIELD preservation, negative-repair, suppression, ordered partial-write and failure-prefix fixtures; complete exact joined hostile scanner/hazard/menu and main scanner/fresh-prompt ANSI/local/RNG state |
| Direct emergency-warp wrapper and main/hostile callers | `src/yt_session.c::{command_shell,sector_entry,direct_emergency_warp,fresh_no_turn_gate,session_02db,session_02fc,session_a8d2,emergency_warp,display_sector}`, `src/yt_game.c::{yt_no_turn_gate_denied,yt_hostile_menu_dispatch}` | both main first-byte W and hostile whole-string W/WT now enter the shared `YT:[1593,161D)` owner. Its independent player hydration publishes sector/cloak caches, applies the strict positive-turn gate, emits the exact no-turn body or the two foreground-seven warnings, direct blanks, raw first-byte confirmation loop, and invokes the existing emergency-warp child only for Y. Every ordinary result enters `081F`. Native presentation pins the 167-byte plain and 223-byte ANSI N-decline bodies plus the exact no-turn body; predicate fixtures pin zero/negative denial and positive-fraction admission. | `candidate` | add deterministic accepted-child output/RNG/raw FIELD/PUT/cache/failure transcript; complete invalid retry, inherited pager/carrier/mode/physical/allocation prefixes and joined main/hostile scanner/hazard/menu/fresh-prompt state |
| Shared team-overlay loader | `src/yt_game.c::{yt_team_loader_begin,yt_team_loader_finish}`, `src/yt_session.c::team_load` | root `YT-SUB:95F4` and all 17 qualified body/return transfers are candidate. The shared cache owner pins availability true, four ascending roster clears, terminal counter five, inclusive ID range, any-nonzero roster admission, live-only stored-length name/password/captain/roster installation, nearest/floor CINT modes, embedded-NUL names, invalid/dead stale caches, and caller-cleared versus retained captain flags. The production owner performs the clear before its physical GET; Team creation's captain/roster stage now uses its documented independent fresh overlay instead of relying on a dead-loader refresh. | `candidate` | add raw MBF dirty-zero/counter/descriptor/FIELD identity, fractional random-record coercion, string allocation and physical GET failure prefixes, plus complete caller-specific joins |
| Shared player-friendship predicate | `src/yt_game.c::yt_friendship_resolve`, `src/yt_session.c::{same_team,friendship_read_player}` | root `YT-SUB:974F` and all 16 qualified body/return transfers are candidate. The provider-driven owner pins all four inclusive `2..last-player` comparisons, invalid no-I/O returns, exact self friendship with a zero team, fresh current-player GET before the teamless gate, conditional candidate GET, fractional record flooring, equal negative/nonzero teams, unequal teams, FIELD-order-equivalent read order, and both GET failure cuts. The session path now reloads the current player instead of comparing the candidate against stale session team state. | `candidate` | add raw MBF Boolean/scratch/FIELD binding and physical database adapter evidence, corrupt process/config state and complete caller-specific joins |
| Team command front, membership/resource actions, shared name/password helpers, and hostile/main choice-one continuation | `src/yt_session.c::{command_team,info_team_lines,team_create,team_join,team_quit,team_search,team_transfer,team_banish,team_pick_name,team_create_password,team_load,team_read_overlay,team_store,team_store_roster,team_audit,reload_player,write_player,append_news,session_a8d2,session_0317,session_031f,session_0345,session_0357,session_036f,session_02db,session_present_text,sector_entry,command_shell}`, `src/yt_game.c::{yt_team_choice_rejected,yt_team_transfer_apply_sector,yt_team_transfer_apply_player,yt_team_banish_apply_player,yt_team_roster_overlay,yt_team_name_overlay,yt_team_password_overlay,yt_hostile_menu_dispatch}` | the Team front uses the ordinary resolver/promotion, two post-resolver hydrations, exact option rows, cached-time prompt, case-preserving editor, MBF32 choice and ordered CINT gate; raw `1` alone exits through `081F`. Create and Join perform their independently ordered reads/writes, news/audits, and output. Quit uses exact raw first-byte confirmation, persists player team zero without clearing the process cache, performs explicit old-overlay plus loader-buffer roster mutation, optional last-member metadata clear, exact unstyled success, audit, and only then clears the process cache. Search nests two complete sector scans under every matching teammate, emits the fixed 41-byte player field and raw resource lists, and cannot append `None Found` after any teammate. Transfer uses independent player/sector snapshots, accepts positive fractional amounts, commits the sector before a fresh player GET, and changes fighter offset 61 while preserving the fresh sector field. Canonical Search is pinned at 219 bytes, its no-teammate body at 58, Transfer at 141, and zero-defense at 49. Banish reloads the player and overlay, skips only nonpositive/current-player roster slots, renders the stored fixed-name prefix, repeats the raw Y/N reader, persists the selected player's team-zero FIELD before a fresh team GET and four-slot overlay, and emits exact 39-byte reject or 80-byte accept bodies. | `candidate` | add full member/captain/promotion/invalid front streams and membership/resource raw FIELD/store/news/radio/ANSI/carrier/retry/redraw/failure evidence; complete main/hostile scanner/hazard/menu joins |
| Team name helper | `src/yt_session.c::{team_pick_name,team_read_overlay,session_present_text,session_031f,session_0345,session_02db}`, `src/yt_game.c::{yt_team_prepare_name,yt_team_name_overlay}`, `src/qb.c::qb_title_case_n` | the helper owns the direct blank, exact no-newline prompt and case-preserving editor, tests the raw byte length before normalization, abandons a short name through the typo-bearing alert, and otherwise normalizes the full input before clipping to 41. Reducers pin the rejected two-byte name, accepted three-space empty name, collapsed/mixed-case name and 50-byte normalization-before-cap result. Acceptance performs a fresh team GET and overlays only the fixed name plus MBF32 length. All eight qualified body edges and root `YT:64CD` are candidate; 93 focused Team oracle tests pass. | `candidate` | add isolated exact 64/103-byte success/short ANSI/local streams, raw cached-name/FIELD/store ordering, malformed record target, long movable-string/runtime cuts, caller redraw and every GET/PUT/physical failure prefix |
| Team password helper | `src/yt_session.c::{team_create_password,team_read_overlay,session_present_text,session_031f,session_0357,session_02db}`, `src/yt_game.c::yt_team_password_overlay`, `src/qb.c::qb_compat_upper_n` | the helper repeats its direct blank and exact no-newline prompt, visibly echoes raw input, applies compatibility uppercase, admits only transformed length four, emits the styled reminder with the disclosed uppercase password before persistence, then performs a fresh team GET and raw four-byte offset-113 overlay/PUT. The existing creation transcript pins lowercase `pass` echo followed by `PASS`; record fixtures pin that no unrelated byte changes. All seven qualified body edges and root `YT:657F` are candidate, and 93 focused Team oracle tests pass. | `candidate` | add exact inline bad-length retry and isolated 132-byte ANSI/local action streams, raw cached-password/FIELD/store ordering, malformed record target, carrier/editor/runtime cuts and every GET/PUT/physical failure prefix |
| Shared sector-mine root | `src/yt_session.c::{command_mines,reload_player,write_player,display_sector,sector_entry,session_031f,session_036f,session_02db,session_02fc,session_sound}`, `src/yt_game.c::yt_sector_mine_admit`, `src/qb_format.c::{qb_val,qb_str_single}` | root `YT:1ECC` and all 23 qualified body edges are candidate. The body independently hydrates the player, durably repairs a negative mine field while retaining the stale carried cache, applies the exact no-mine/Union gates, accepts positive fractions and equality, sets self-mine suppression before I/O, commits the player debit before the fresh sector credit, then emits the styled success and selector-four sound. Native reducer/presentation fixtures pin the gate boundaries and canonical hostile action partitions; 57 focused isolated/main/hostile oracle tests pass. | `candidate` | add a complete native raw player/sector FIELD/store/cache/suppression and ordered failure-prefix fixture, exact joined main scanner/fresh-prompt bytes, all modes/local framebuffer/carrier/runtime endpoints and physical partial-I/O proof |
| Cross-platform OpenDoors startup/exit | `src/main_yt.c`, `src/yt_door.c` | none | `explicitly deferred` | user authorization plus platform-specific init/exit and partial-cleanup tests |
| ANSI-startup true-EOF wait | `src/yt_session.c::{opening_and_date,session_wait}` | shipped `YT-SUB:2496` source, predecessor fixture, all qualified `DS:A2FC` uses, executable bytes, and corrected upstream startup-opening/world artifacts agree that `2499` copies unchanged MBF32 3; the three-second deadline is covered in `tests/test_input_model.c::test_timed_wait` | `candidate` | add exact file-row/interruption/reset/close/failure and raw/physical wait fixture |

## Persistent and external effects

Each row is an independently observable legacy boundary. `candidate` means
the named native code touches it; it does not imply exact operation order or
fault behavior.

| External identity/effect | Native owner(s) | Native evidence | Status | Missing proof |
|---|---|---|---|---|
| `YTDATA.DAT` random records | `yt_file.c`, `yt_game.c`, initializer/maintenance/session callers | structure checks in `test_clean_install`; selected record tests | `candidate` | every GET/PUT prefix, FIELD identity, record residue, mixed original/native mutation |
| `YTNAME.DAT` alias rows/replacements | `yt_names.c`, `main_ytconfig.c`, `yt_maint.c`, initializer/local/session callers | limited utility/clean-install tests | `candidate` | exact grammar, all-match behavior, fan-out order, kill-before-rename faults |
| `YTNEWS.DAT` append/current news | `yt_maint.c::yt_news_append`, session append helper, initializers | fresh-install markers | `candidate` | all 101 constructions, DOS EOF buffering, per-call durability/failures |
| `YTYNEWS.DAT` rotation | `yt_maint.c::rotate_news` | fresh-install regression | `candidate` | complete open/close/kill/rename physical result matrix |
| `YTRMSG.DAT` radio store/compaction | `yt_file.c::{yt_radio_file_open,yt_radio_file_size,yt_radio_file_get,yt_radio_file_put,yt_radio_file_next_record,yt_radio_file_close}`, `yt_init.c::write_rmt_auxiliary`, `yt_session.c::{radio_append_bytes,radio_read}`, `yt_data.c::{yt_radio_reader_decide,yt_radio_reader_mutate}`, `yt_maint.c::{maintenance_radio_append_bytes,yt_radio_compact,invalidate_radio}` | gameplay physically owns the exact 86-byte 4/4/4/74 binding, aligned writer selection, fractional-tail rejection, zero-filled partial/past-EOF reader GETs and full PUTs. RMT Genesis initialization uses the same live-handle record selector for five counter-50 broadcasts but preserves its distinct 4/4/4/72 FIELD image with two zero tail bytes. All three supported maintenance callers now share the live file-5 owner for typed LOF and numbered GET/PUT while retaining maintenance's separate 72-byte FIELD image, raw 84-byte compaction copy, and preliminary invalidation GET. Reader decision, binary presentation and counter transforms remain composed through their declared seams. | `candidate` | add raw BRUN/file-registry/FIELD state, exact accepted-prefix failure adapters and all shared physical/runtime failure endpoints |
| configured scoreboard / `YTSCORE.ASC` | `src/yt_score.c::{yt_score_generate,yt_score_generate_progress}` and `src/yt_score_format.c` | exact four-phase progress sequence, shipped score formula and precision split, dead/empty and alien-defense lanes, ascending cache PUT sweep, stable equal-score player/team ordering, denominator fallback/all-zero failure, exact `NUL` remap, 603-byte retained shape, rich 23-line readback, and exhaustive numeric formatter vectors in `tests/test_score.c` | `candidate` | add corrupt raw CINT/index domains, array/FIELD/allocator residue, every physical GET/PUT/OPEN/PRINT/CLOSE failure prefix, handler/frame state, and both raw caller joins |
| `YT.REG` registration transaction | `src/yt_startup_model.c::yt_registration_run`, `src/qb_format.c` exact raw MBF operations, `src/yt_text.c::yt_text_stream_line_input_next`, `src/yt_session.c::registration` | injected tests pin create/LOF/close/delete/reopen/read ordering, exact three-line binary grammar, all shipped raw arithmetic, extra-record ignore, registered/evaluation results, invalid/beta/anti-tamper terminals and every fallible prefix; the physical stream fixture pins record-consuming capacity failure and persistent DOS EOF, while session adapters preserve create/delete and streaming order | `candidate` | unrestricted BRUN heap/file-block/handler-frame residue, character-device variants, DOS retry classifications and physical partial failures |
| `LOCKOUT.DAT` startup transaction | `src/yt_session.c::lockout`, `src/yt_file.c::{yt_database_open,yt_database_random_lof,yt_database_random_close}`, `src/yt_startup_model.c::{yt_startup_lockout_run,yt_startup_lockout_scan}`, `src/yt_text.c::{yt_text_input_open,yt_text_input_read_line,yt_text_input_close}` | injected exact lowercase-random-open/live-handle-LOF/close/mixed-case-sequential-open/read/first-match/denial/wait/CLOSE-all/END transaction with every fallible callback prefix, exhaustive typed LOF seek/error/cursor evidence, plus native dynamic stream grammar and exact ANSI/plain denial streams | `candidate` | raw BRUN random/sequential file blocks, read-ahead/string descriptors and handler frames, operating-system partial read/close faults and physical CLOSE-all/END cleanup |
| `XANNORHQ.TXT` victory input | `yt_session.c::{xannor_victory,xannor_victory_file}`, `yt_text.c::{yt_text_input_open,yt_text_input_read_line,yt_text_input_close,yt_text_sequential_play_run}` | immutable asset checksum; a native case-resolved, dynamically sized sequential stream fixture for empty records, embedded NUL discard, CR with optional LF consumption, retained bare LF, DOS EOF and unterminated final records; a provider tape pinning initial close, open, each shipped record's immediate presentation, EOF read, final close and every callback failure prefix; and a physical generated-asset join pinning exact 230-byte plain/240-byte ANSI output plus local line/color events | `candidate` | full victory framebuffer/effect composition, raw BRUN file-block/read-ahead/error-cleanup state, operating-system read/close faults and endpoint partial failures before award/effect continuation |
| `YTOPEN.ASC` / `YTOPEN.ANS` | `yt_session.c::{opening_and_date,opening_poll_local,opening_poll_remote,opening_wait,display_game_file}`, `yt_text.c::{yt_opening_stream_run,yt_file_viewer_stream_run,yt_text_input_eof,yt_text_input_read_line}`, `yt_pager.c::yt_paged_row_run`, `yt_output.c::yt_out_opening_file`, `yt_presentation.c::yt_present_status_row` | immutable asset checksums and recovered route/ANSI/status/ASCII order; the live physical ANSI stream pins all 67 shipped rows, 68 EOF probes, 2,633 local CRLF and 2,633 remote LFCR/reset bytes, true-EOF three-second wait and ordered closes, while its 20-operation tape pins every dependency cut and both interruption exits. The physical ASCII startup component pins the exact 505-byte asset, 17 rows/18 EOF checks, 18 B05D samples, 21 local rows, full local color sequence, 544-byte ANSI and 524-byte plain streams, local-only/corrupt-mode endpoints, early Ctrl-X queue/source residue, close and restoration. The generic pager join separately pins row-23 E/NS, boundary Ctrl-X and all nested threshold cuts. | `candidate` | join both exact shipped streams into one inherited startup/framebuffer and missing/error-handler composition; add raw runtime file/device state and physical OpenDoors/OS partial faults |
| `DORINFO1.DEF` or argument | `yt_door.c`, `main_local.c` | limited LOCAL test | `candidate` | exact 12/13-line cursor/truncation/fault/SHELL boundaries |
| `ERRORS.DOR` | `src/yt_main_error.c::{yt_main_error_compose,yt_main_error_commit_fatal,yt_main_error_commit_fatal_to}` and `src/yt_text.c::yt_text_append_line` | `tests/test_presentation.c::test_main_error_model` pins the record; `tests/test_data.c::test_main_error_fatal_transaction` pins session-before-open ordering, fresh and stale-tail images, presentation failure and open failure | `candidate` | integrate the transaction into the typed session error router; add PRINT/final-close accepted prefixes, returned file-registry state and active-handler second-fault completion |
| `RMTINIT.TMP` | `main_rmt_init.c`, session Genesis path | limited standalone RMT test | `candidate` | exact producer/RUN handoff, first-line/delete ordering and failure domain |
| process, clock, entropy, serial/console, carrier and terminal state | platform/OpenDoors/session layers | injectable clock-provider order/exhaustion test plus entropy/clock smoke coverage | `candidate` | every documented external-result class and stateful adapter; remaining caller clock tapes |

## Promotion workflow

Work proceeds from shared prerequisites outward. For a bounded component:

1. identify its final analysis owner and all outgoing transfers;
2. name the exact C function(s), shared adapters, external effects and state;
3. add deterministic native tests for normal, alternate, interrupt, timeout,
   carrier, SysOp and physical-failure families that are reachable there;
4. compare remote bytes, local result, raw files/records, random draws and
   continuation state; and
5. update the root, transfer and presentation rows together. A component is
   not promoted while any reached child is replaced by an aggregate result.
